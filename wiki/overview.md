---
type: overview
domain: "Agentic AI Frameworks"
created: 2026-04-28
updated: 2026-08-17
---





[[paperclip.ing]] introduces Paperclip, an open-source control plane for running autonomous AI-agent companies. It sits above agent runtimes like Claude Code, Codex, Cursor, OpenClaw, bash agents, and HTTP bots and provides the organizational layer those runtimes lack: an org chart with hierarchical reporting, a heartbeat execution loop that wakes agents on schedule, an atomic ticket system with checkout locks, per-agent budget enforcement with hard stops, board-level governance and approval gates, and complete multi-company data isolation in a single Node.js + React deployment. The source also shows how Paperclip packages this operating model into both a public product site and a companion GitHub repo with a built-in heartbeat skill, plugin system, portability features, and an actively maintained roadmap.

[[sequentialthinking-mcp]] documents the Sequential Thinking MCP Server, a focused MCP server that exposes a single `sequential_thinking` tool for structured, revisable, branching reasoning inside an MCP-aware host. The source captures the tool's input schema, the intended problem-solving workflow, practical verification steps, and concrete installation patterns for Claude Desktop, VS Code, Codex CLI, NPX, and Docker.

[[langchain.com]] reframes LangChain as a multi-product agent-engineering platform rather than just a library. The umbrella source ties together [[langchain.com-langsmith]], [[langchain.com-fleet]], [[langchain.com-langchain]], [[langchain.com-langgraph]], and [[langchain.com-deepagents]] and shows how observability, no-code business agents, higher-level framework primitives, lower-level orchestration, and an autonomous agent harness are positioned as one integrated stack.

[[langchain.com-langsmith]] adds the operational layer to the wiki's agentic AI coverage: framework-agnostic tracing, offline and online evaluation loops, deployment options, Agent Server runtime architecture, and the production feedback cycle that turns traces into datasets and redeployable improvements. It is especially useful for understanding how agent teams move from experimentation to monitored production systems.

[[langchain.com-fleet]] contributes the no-code side of the LangChain ecosystem. It covers business-facing agents built from templates, integrations, channels, approvals, long-term memory files, and sub-agents, showing how LangChain's platform story extends beyond developer libraries into managed agent operations for non-engineering teams.

[[langchain.com-langchain]] documents the high-level open-source framework path: `create_agent`, model and tool abstractions, middleware-based dynamic routing, and the batteries-included approach to getting useful agents running quickly. It complements the lower-level orchestration material elsewhere in the wiki by covering the higher-abstraction starting point for code-first agent development.

[[langchain.com-langgraph]] captures the runtime-oriented end of the stack: explicit state graphs, durable execution, interrupt-driven human-in-the-loop patterns, workflow topologies like routing and orchestrator-worker, and thread-based resumption semantics. It adds a strong reference point for stateful, long-running agent systems that need more control than a single high-level agent loop.

[[langchain.com-deepagents]] adds the agent-harness layer to the wiki, completing the three-tier open-source stack under LangChain. It covers the Deep Agents SDK — a batteries-included framework built on LangGraph that adds planning with `write_todos`, file-system context management, subagent spawning, and long-term memory — together with the Deep Agents CLI, a terminal coding agent built on the same SDK. This is a new product added to the platform since the initial ingest.

[[runcabinet.com]] introduces Cabinet, a free and open-source self-hosted AI-first startup OS where all knowledge lives as markdown files on disk. It bridges the gap between note-taking tools (Obsidian, Notion) and agent orchestration platforms (Paperclip) by combining a WYSIWYG knowledge base, 20 pre-built AI agent templates with cron-based scheduling, embedded live HTML apps, a browser-based web terminal, and git-backed version history — all without a database or vendor lock-in. Cabinet operationalizes Andrej Karpathy's LLM-wiki pattern as a product, positioning itself as the persistent memory layer for both human and AI work in a self-hosted startup OS.

[[skills.sh]] covers the open agent skills directory and ecosystem built by Vercel, which provides the de-facto distribution layer for SKILL.md-based capability modules across 50+ AI coding agents. The source captures the leaderboard (community-ranked by anonymous install telemetry), the official skills registry (70+ technology vendors publishing skills directly, including Anthropic, Microsoft, GitHub, OpenAI, Sentry, and Cloudflare), a security audit dashboard, and the full CLI reference for `npx skills` backed by the open-source `vercel-labs/skills` repo. It adds a concrete picture of how procedural knowledge is packaged, discovered, and installed across the multi-agent tooling ecosystem referenced throughout this wiki.

[[canva.com]] documents Canva as an AI-powered visual design platform with a developer layer directly relevant to agentic AI workflows. Its remote MCP server (hosted on [[canva.com]]) exposes design generation, editing, asset management, and export as MCP-compatible tools consumable by Claude, ChatGPT, Codex, Gemini, Cursor, and VS Code agents. The Connect APIs enable external platforms to embed Canva capabilities via REST (autofill brand templates, bulk create, resize, export, comments), and the Apps SDK allows JavaScript plugins to run inside the Canva editor. This source is notable as a concrete example of an established SaaS product opening its design capabilities to AI agents through the Model Context Protocol.

[[njbrake-agent-of-empires]] documents Agent of Empires (AoE), a Rust-based open-source terminal session manager for AI coding agents (1,868 stars, v1.5.0). AoE wraps tmux to run Claude Code, OpenCode, Mistral Vibe, Codex CLI, Gemini CLI, Cursor CLI, Copilot CLI, Pi.dev, Factory Droid, and Hermes in parallel — each in an isolated session with agent-aware status detection, git worktree branch isolation, optional Docker sandboxing, and remote phone access via a browser-based PWA. It represents the session-management and multi-agent coordination layer of the stack: not an LLM framework, but the operational harness that keeps multiple coding agents alive, organized, and reachable across devices.

[[tmux-tmux]] documents the canonical Unix terminal multiplexer — the open-source C project (45,000+ stars, ISC License, latest 3.6a) that underpins a growing category of AI-agent infrastructure in this wiki. tmux's session/window/pane hierarchy, detach-reattach model, and scriptable `capture-pane`/`send-keys` interface are the foundational primitives on which [[njbrake-agent-of-empires]] and [[tmuxai.dev]] are both built. The source captures the full build/install path, architecture overview (client/server socket model, ~50 cmd-*.c command files, OS shims), example configuration, and the ecosystem of plugins and tooling that extends it.

[[tmuxai.dev]] introduces TmuxAI, a Go-based AI terminal assistant (1,790 stars, Apache-2.0, v2.1.4) that runs non-intrusively inside a tmux session. It observes all visible panes for context, communicates through a dedicated Chat Pane, and executes commands in an Exec Pane with user confirmation and risk indicators. Key modes — Observe (default), Prepare (shell-prompt integration for exact completion tracking), and Watch (proactive monitoring) — make it adaptable from interactive assistance to continuous log surveillance. Its Knowledge Base and Skills subsystems (built on the SKILL.md pattern also seen in [[skills.sh]]) allow persistent domain context to be injected into every AI interaction, making TmuxAI a practical ambient AI layer for terminal-heavy workflows built on [[tmux-tmux]].

[[shareai-lab-learn-claude-code]] is a 12-session open-source curriculum (58,143 stars, MIT) that reverse-engineers Claude Code by building its full architecture from scratch in Python, one harness mechanism per session. Its central thesis — agency comes from model training, not from orchestration code; harness engineers build the environment the intelligence inhabits — is the clearest articulation of *harness engineering* in this wiki. The curriculum covers the fundamental agent loop (s01), tool dispatch (s02), in-memory planning (s03), subagent isolation (s04), on-demand SKILL.md loading (s05, the same two-layer pattern used in [[skills.sh]] and [[tmuxai.dev]]), context compression (s06), a file-based task graph with `blockedBy` dependency edges (s07), background task threads (s08), persistent agent teams with JSONL mailboxes (s09), team protocols (s10), autonomous task self-claiming (s11), and git-worktree isolation (s12). The repo also ships a companion Next.js platform with interactive diagrams and a sister repo (`claw0`) covering the always-on heartbeat/cron harness variant.

[[anthropics-skills]] is Anthropic's official reference repository for the Agent Skills pattern — 17 SKILL.md-based capability modules for Claude spanning document processing (xlsx, docx, pptx, pdf), creative design, MCP server generation, web testing, and a meta `skill-creator` skill for building and evaluating new skills. With 132,000+ stars it is the most-starred skills repository in the ecosystem and the canonical starting point for anyone writing skills for Claude Code, Claude.ai, or the Claude API. The document skills (`xlsx`, `docx`, `pptx`, `pdf`) are the same skills that power Claude's native document capabilities in Claude.ai (source-available). The `skill-creator` skill demonstrates the on-demand SKILL.md loading pattern also covered in [[shareai-lab-learn-claude-code]] (s05) and distributed through [[skills.sh]].

[[forrestchang-andrej-karpathy-skills]] distills Andrej Karpathy's public critique of LLM coding assistants into four actionable behavioral principles — Think Before Coding, Simplicity First, Surgical Changes, and Goal-Driven Execution — packaged as a single `CLAUDE.md` file and a Claude Code plugin skill (124,694 stars, MIT). It is the community's canonical answer to the failure modes Karpathy identified: silent wrong assumptions, bloated abstractions, untargeted edits, and vague success criteria. The repository connects to [[anthropics-skills]] and [[skills.sh]] through the Claude Code plugin marketplace, and its philosophical core — that agent behavior is shaped by environment and instructions — runs through [[shareai-lab-learn-claude-code]]'s harness engineering curriculum.

[[x.com-mnilax-karpathys-4-claude-md-rules-cut-claude-mistakes]] captures an empirical study by Mnimiy (@mnilax) extending [[forrestchang-andrej-karpathy-skills]]'s 4-rule template with 8 additional rules grounded in 6 weeks of testing across 30 codebases. The 4 original rules cut the mistake rate from ~41% to ~11%; the full 12-rule template brings it to ~3% while preserving ~76% compliance. The 8 new rules address failure modes specific to May 2026 Claude Code workflows that the original January 2026 template did not anticipate: nondeterministic routing logic that should be plain code (Rule 5), context drift from uncapped token usage (Rule 6), incoherent "average" code when codebase patterns conflict (Rule 7), duplicate implementations from writing without reading (Rule 8), hollow test suites that assert output without encoding intent (Rule 9), compounding errors in multi-step tasks without checkpoints (Rule 10), pattern fragmentation from silent convention violations (Rule 11), and silent partial successes masquerading as completions (Rule 12). The article provides the full 12-rule CLAUDE.md template verbatim and a one-command install path that appends to an existing CLAUDE.md.

[[6eanut-llm-wiki]] is a Claude Code skill that implements Andrej Karpathy's LLM Wiki pattern as a fully automated, self-contained knowledge base (35 stars, MIT). It compiles knowledge once from user-supplied source documents into a persistent, interlinked markdown wiki, and keeps it current via SHA-256 incremental caching and session lifecycle hooks — answering questions by reading an auto-generated `index.md` first (O(1) lookup), then 3–5 relevant pages. The skill ships six slash commands (`/wiki-ingest`, `/wiki-query`, `/wiki-lint`, `/wiki-save`, `/wiki-graph`, `/wiki-review`), a two-phase ingest workflow with a human review checkpoint, D3.js knowledge graph generation, bilingual CJK support, and deterministic bash utilities for hashing, orphan detection, and frontmatter validation. Its `CLAUDE.md` / `WIKI.md` template enforces proactive wiki checks without explicit command invocation — the same behavioral pattern advocated by [[forrestchang-andrej-karpathy-skills]] and operationalized in [[runcabinet.com]]. The skill connects to [[anthropics-skills]] and [[skills.sh]] through the Claude Code plugin/skills ecosystem, and its harness architecture — LLM for content, bash for determinism — mirrors the design principles articulated in [[shareai-lab-learn-claude-code]].

[[planana.xyz]] introduces Planana AI, a free web application that turns an open-ended learning goal into a structured, personalized weekly study roadmap. The user describes what they want to learn and their current level; Planana's AI tutor generates a practical plan broken into weeks and daily sessions with matched resources, eliminating the friction of figuring out what to study next. The site covers a broad range of subjects (Python, JavaScript, Rust, SQL, algorithms, mathematics, languages, humanities) through pre-rendered `/learn/*` pages that also serve as SEO entry points. As of this ingest, Planana is a standalone consumer web app with no public API or agent integration layer — it uses AI internally but does not expose those capabilities to developers or external agents.

[[meetclaras.com]] introduces Claras, an AI-powered YouTube transcript and knowledge-extraction tool that turns any YouTube video into an interactive knowledge base. Delivered as a Chrome extension and web app, Claras retrieves the full transcript of any YouTube video and layers AI Q&A, auto-generated summaries with chapter tables of contents, searchable highlights, and multi-format export (TXT, SRT, PDF, VTT) on top. Its key differentiator for agentic workflows is the export-to-agent pipeline: users can feed full transcripts into Claude, ChatGPT, or custom agents to build research assistants and content analyzers from YouTube video content — useful for ingesting conference talks, product demos, and tutorial libraries into agent knowledge bases. No public API or GitHub repository was found; the integration path is export-based, not programmatic.

[[reseek.net]] introduces Reseek, a cloud-hosted personal knowledge management platform ("second brain") that saves links, notes, images, PDFs, YouTube videos, and Twitter/X posts into one AI-queryable library. OCR, automatic tagging, semantic search, and AI chat are applied across all content types. What makes it directly relevant to agentic AI stacks is the MCP Server: four tools (`search-library`, `get-library-item`, `save-note`, `save-link`) let LLM clients treat the Reseek library as a structured knowledge backend — an agent can save research during one session and retrieve it semantically in another. A REST API with personal access tokens covers programmatic automation. Pricing is $9/month or $99/year (SaaS, no self-hosted option); compare with [[runcabinet.com]] for the self-hosted, developer-oriented, markdown-on-disk alternative.

[[nidhinjs-prompt-master]] introduces Prompt Master, a Claude skill (SKILL.md-based, v1.6.0, 7,412 stars, MIT) that functions as an on-demand prompt engineer across 30+ AI tools. It silently extracts nine dimensions of intent from a user's rough idea, routes to one of 12 prompt templates, applies five safe techniques (role assignment, few-shot, XML tags, grounding anchors, chain of thought), audits for token waste, and delivers one production-ready copyable prompt. Tool-specific routing handles the behavioral differences between reasoning LLMs (Claude, GPT-5.x, Gemini), thinking LLMs (o3/o4-mini — no CoT added), coding agents (Claude Code, Cursor — stop conditions and file scope required), image AI (Midjourney, Stable Diffusion — comma-separated descriptors and negative prompts), and workflow automation (Zapier, n8n). The skill connects to [[anthropics-skills]] and [[skills.sh]] as another high-star SKILL.md module in the Claude skills ecosystem, and its focus on precise intent extraction and token efficiency complements the behavioral guidelines documented in [[forrestchang-andrej-karpathy-skills]].

[[9d5bzxVsocw-anthropic-just-dropped-the-new-blueprint]] is a 17-minute breakdown by The AI Automators of Anthropic's March 2026 engineering article on harness design for long-running autonomous agents. The video identifies two core failure modes Anthropic observed in practice — context anxiety (models changing behavior as context fills, rushing toward premature completion) and poor self-evaluation (agents approving their own mediocre work) — and walks through the three-agent architecture built to address them: a planner (expands a terse prompt into a structured spec), a generator (builds the work), and an evaluator (judges it adversarially, inspired by GAN discriminator architecture). Two real case studies illustrate the evolution: a 2D retro game maker built with Opus 4.5 using sprint-based context resets and per-sprint contract negotiation, and a browser-based digital audio workstation built with Opus 4.6 in a single continuous 4-hour, $125 session relying entirely on context compaction. The key takeaway — that every harness component encodes an assumption about model incapability, and those assumptions go stale as models improve — connects directly to the harness engineering curriculum in [[shareai-lab-learn-claude-code]] and the orchestration patterns in [[langchain.com-langgraph]] and [[njbrake-agent-of-empires]].

[[anthropic.com]] captures Anthropic's developer platform and original harness research. The umbrella source ties together [[anthropic.com-messages]] (direct Messages API for custom agent loops and Agent Skills) and [[anthropic.com-managed-agents]] (pre-built managed agent harness for long-running autonomous tasks). The engineering article that anchors the ingest introduces a three-agent GAN-inspired architecture — planner, generator, evaluator — built on top of the Claude Agent SDK and Managed Agents infrastructure, with documented experiments building a retro game maker (6 hr, $200, Opus 4.5) and a browser-based DAW (3 hr 50 min, $125, Opus 4.6).

[[anthropic.com-messages]] documents the Messages API: direct model access for custom agent loops, tool use, function calling, extended thinking, batch processing, and the Agent Skills system. Agent Skills use three-tier progressive disclosure (metadata → instructions → bundled scripts) to inject modular capability into Claude sessions without context overhead. This is the same Agent Skills pattern documented in depth in [[anthropics-skills]] and distributed through [[skills.sh]].

[[anthropic.com-managed-agents]] documents Claude Managed Agents: a fully managed agentic harness with secure cloud containers, automatic context compaction, server-side session persistence, and multi-agent thread orchestration. Detailed harness engineering findings from Anthropic's own experiments are documented here — including the two primary failure modes (context anxiety and poor self-evaluation), the planner-generator-evaluator architecture, sprint contracts, and the evolution principle: every harness component encodes an assumption about model incapability, and those assumptions go stale as models improve. The same content is covered in video form in [[9d5bzxVsocw-anthropic-just-dropped-the-new-blueprint]].

[[gsd-build-get-shit-done]] introduces GSD (Get Shit Done), a lightweight meta-prompting, context engineering, and spec-driven development system for AI coding agents (62,040 stars, MIT, JavaScript/TypeScript). GSD's central innovation is solving *context rot* — the quality degradation that accumulates as an agent's context window fills — by keeping the orchestrating session at 30–40% utilization while delegating all heavy work to fresh 200k-token subagent contexts. The six-command loop (new-project → discuss → plan → execute → verify → ship) produces persistent structured artifacts (`.planning/` Markdown + JSON) that survive session resets. Its v1.40 two-stage hierarchical routing reduces per-turn skill-listing cost from ~2,150 to ~120 tokens. GSD complements [[obra-superpowers]] and shares the multi-agent subagent-orchestration pattern with [[shareai-lab-learn-claude-code]] and [[anthropic.com-managed-agents]].

[[obra-superpowers]] is a complete software development methodology for AI coding agents built on 14 auto-triggering composable SKILL.md skills (190,063 stars, MIT, v5.1.0). Its distinguishing characteristic is the `using-superpowers` bootstrap skill that loads at session start and mandates skill invocation before any agent response — even with a 1% chance of applicability. The methodology is opinionated: spec before code (brainstorming), git worktree isolation, subagent-per-task with two-stage review, strict RED-GREEN-REFACTOR TDD, and human-in-the-loop checkpoints at every transition. It is a zero-dependency plugin available on the official Claude plugin marketplace and six other harnesses. Superpowers shares the SKILL.md pattern with [[anthropics-skills]] and [[skills.sh]], and the subagent orchestration approach with [[gsd-build-get-shit-done]] — the two projects are the dominant choices for teams seeking a disciplined AI coding methodology.

[[coleam00-archon]] introduces Archon, the first open-source harness builder for AI coding (21,422 stars, MIT, TypeScript, v0.3.11). Where [[obra-superpowers]] and [[gsd-build-get-shit-done]] encode the *methodology* of AI-assisted development as composable skills, Archon encodes the *workflow structure* as committed YAML DAGs. Each workflow is a directed acyclic graph of nodes — AI prompt steps, bash commands, loop-until conditions, and human approval gates — that the Archon engine executes in isolated git worktrees so five tasks can run in parallel with no branch conflicts. The 17 built-in workflows (`archon-fix-github-issue`, `archon-idea-to-pr`, `archon-comprehensive-pr-review` with five parallel reviewers, `archon-refactor-safely`, and others) cover the full development lifecycle. Platform adapters for CLI, Web UI, Slack, Telegram, GitHub, and Discord route into a single orchestrator backed by SQLite or PostgreSQL; the web dashboard unifies all conversations regardless of entry point. This positions Archon as the infrastructure layer that addresses the same nondeterminism problem [[anthropic.com-managed-agents]] identifies — but from a self-hosted, YAML-first, multi-provider angle (Claude Code SDK, Codex SDK, Pi), committed directly to the repo so the whole team runs the same process.

[[gastownhall-beads]] introduces Beads (`bd`), a distributed graph-based issue tracker designed as a persistent memory upgrade for AI coding agents (23,795 stars, MIT, Go, v1.0.4). Where [[gsd-build-get-shit-done]] solves context rot at the session level through fresh-context subagent delegation, and [[coleam00-archon]] structures workflow execution as committed YAML DAGs, Beads addresses the deeper persistence problem: agents have no durable task memory between sessions, and resort to markdown TODO files that accumulate noise and lose dependency structure. Beads replaces those files with a Dolt-powered SQL database (`bd-a1b2`-style hash IDs, full dependency graph, `bd ready` for surfacing unblocked work, `bd prime` for injecting workflow context at session start) that persists across sessions, branches, and concurrent agents without collision. Integration recipes (`bd setup claude`, `bd setup copilot`, `bd setup codex`, and more) install hooks and instruction snippets for 10+ AI coding environments, and the minimal/full profile split keeps hook-enabled agents pointed at `bd prime` as the single source of truth while AGENTS-first agents receive the full command reference inline.

[[huggingface.co]] is the open-source AI platform where the machine learning community collaborates on models, datasets, and applications — hosting 2M+ models, 1.5M+ datasets, and 1.5M+ Spaces. For agentic AI practitioners it matters on two levels: as the primary distribution layer for open-weight foundation models (Llama, Mistral, Qwen, and thousands more) used across agent frameworks including [[langchain.com]] and smolagents alike, and as the home of smolagents — a ~1,000-line Python library for code-first agents (`CodeAgent` writes tool calls as executable Python; `ToolCallingAgent` covers JSON-based tool calling) that is model-agnostic, MCP-compatible, and integrated with the Hub for publishing and loading tools. The platform's Transformers library is the ecosystem pivot: model definitions supported there are compatible with training frameworks (Axolotl, Unsloth, DeepSpeed), inference engines (vLLM, SGLang, TGI), and post-training libraries (TRL, PEFT, Accelerate) — making Hugging Face the connective tissue between open-weight research and production agent deployment.

[[pytorch.org]] documents PyTorch, the dominant open-source deep learning framework (~100k GitHub stars, v2.12.0, BSD-style license, Linux Foundation). PyTorch provides GPU-accelerated tensor computation and a dynamic tape-based autograd system, making it the foundational infrastructure for virtually all modern LLM and vision model training and fine-tuning. For agentic AI practitioners, PyTorch matters as the runtime substrate: the models served by [[huggingface.co]], the inference engines powering agent backends, and the research prototypes that become production systems are almost universally built on PyTorch. Key capabilities covered: eager execution with TorchScript compilation for production, TorchServe for scalable model serving, `torch.distributed` for multi-GPU/multi-node training, the `torch.compile` stack (TorchDynamo + TorchInductor) for automatic kernel optimization, ExecuTorch for edge/mobile deployment, and a rich ecosystem of domain libraries (TorchVision, TorchText, TorchAudio) and third-party tools (vLLM, DeepSpeed, PyTorch Geometric).

[[litellm.ai]] introduces LiteLLM, an open-source AI Gateway (Y Combinator W23, 47k+ GitHub stars) that gives teams a single OpenAI-compatible interface to 100+ LLM providers — OpenAI, Anthropic, Bedrock, Gemini, Azure, Vertex AI, HuggingFace, VLLM, and NVIDIA NIM among them. It is used both as a Python SDK (`completion()` with consistent output format regardless of provider) and as a self-hosted Proxy Server for centralized access control, spend tracking, load balancing, and guardrails across an organization. The proxy accepts any OpenAI-format client without code changes and also serves as a unified gateway for A2A agents (LangGraph, Vertex AI Agent Engine, Bedrock AgentCore, Pydantic AI) and MCP tools. Key capabilities: multiple routing strategies (weighted pick, latency-based, rate-limit-aware, lowest-cost), traffic mirroring for silent A/B experiments, PostgreSQL-backed virtual keys with per-key budget and RPM/TPM limits, automatic spend tracking via a stored model cost map, and pre/post-call guardrails via Aporia, Lakera, Presidio, or a generic webhook API. Observability integrates via a single `success_callback` line covering Langfuse, MLflow, Helicone, and 20+ others. Adopted by Stripe, Netflix, Google ADK, and OpenHands, LiteLLM fills the provider-abstraction and gateway layer that frameworks like [[langchain.com]] and [[huggingface.co]] smolagents rely on for multi-provider routing in production.

[[openrouter.ai]] introduces OpenRouter, a hosted API gateway that gives developers and agentic applications unified access to 400+ AI models from 60+ providers through a single OpenAI-compatible endpoint. Where [[litellm.ai]] is a self-hosted open-source proxy, OpenRouter is a managed SaaS layer that handles provider routing, automatic failover, uptime monitoring, and cost optimization transparently. Its default routing strategy uses price-based load balancing weighted by inverse square of cost, with automatic fallback when a provider degrades — making it a practical reliability layer for teams that don't want to manage individual provider accounts. Beyond a raw HTTP API, OpenRouter offers typed Client SDKs (TypeScript, Python, Go) and an Agent SDK (`@openrouter/agent`) with built-in `callModel` loops, Zod-typed tool definitions, and composable stop conditions (`stepCountIs`, `maxCost`), removing the need to write custom agentic loop infrastructure. Enterprise features include Zero Data Retention endpoints, EU in-region routing, and fine-grained data-collection policies. With 8M+ global users and 250k+ apps integrated, OpenRouter has become a de facto model-access tier for platforms like Replit and Hermes Agent.

[[zilliztech-claude-context]] introduces Claude Context, a Milvus/Zilliz-backed MCP plugin (11,432 stars, MIT, TypeScript) that adds semantic code search to Claude Code and other MCP-compatible AI coding agents. Rather than loading entire directories into the context window on every request — expensive and often infeasible for large codebases — Claude Context indexes the codebase once into a Zilliz Cloud vector database and exposes four MCP tools (`index_codebase`, `search_code`, `get_indexing_status`, `clear_index`) that pull only the semantically relevant code chunks into the agent's context on demand. Indexing is asynchronous; the agent can search partial results immediately while background indexing continues. The plugin supports multiple embedding providers (OpenAI by default, plus VoyageAI, Gemini, and Ollama for fully local deployments) and ships as three separate npm packages — a core library, the MCP server, and a VS Code extension. It is a concrete, narrowly-scoped MCP tool in contrast to the reasoning-layer MCP servers covered by [[sequentialthinking-mcp]].

[[github-spec-kit]] introduces Spec Kit, GitHub's open-source toolkit for Spec-Driven Development (SDD) — a methodology where specifications become executable artifacts that drive AI-powered implementation rather than serving as disposable scaffolding. With 102k+ stars and support for 30+ AI coding agents, it is the most prominent structured-workflow toolkit in the ecosystem. The `specify` CLI bootstraps any project directory with template files and agent integrations (Claude Code, Copilot, Gemini, Codex, Cursor, and more), then exposes a staged pipeline of slash commands — `/speckit.constitution` → `/speckit.specify` → `/speckit.clarify` → `/speckit.checklist` → `/speckit.plan` → `/speckit.analyze` → `/speckit.tasks` → `/speckit.implement` — each producing a committed artifact the agent references in the next step. Extensions add new capabilities; presets customise how the core behaves (from Agile framing to full regulatory traceability). It occupies a complementary position to [[gsd-build-get-shit-done]] (which focuses on context-rot management and subagent orchestration) and to [[anthropics-skills]] and [[obra-superpowers]] (which package capabilities rather than workflow stages).

[[openspec.dev]] introduces OpenSpec, a lightweight open-source spec-driven development (SDD) framework by Fission (49k+ stars, MIT, TypeScript, v1.3.1) that natively supports 25+ AI coding agents including Claude Code, Cursor, Codex, GitHub Copilot, and Windsurf — no API keys, no MCP required. Its core pattern: specs live as structured Markdown in `openspec/specs/`, each proposed change gets its own folder in `openspec/changes/` with `proposal.md`, `design.md`, `tasks.md`, and delta specs capturing ADDED/MODIFIED/REMOVED requirements. The artifact-guided workflow — `/opsx:propose → /opsx:apply → /opsx:archive` — aligns human and AI on requirements before any code is written, and archived changes merge deltas into the source-of-truth spec library. OpenSpec is explicitly brownfield-first (most tools assume greenfield) and positions itself as a lighter alternative to [[github-spec-kit]] (rigid phase gates, Python-based) and Kiro (IDE-locked). Community schemas extend the base workflow in a pattern similar to how [[skills.sh]] distributes SKILL.md modules. The companion GitHub repo (Fission-AI/OpenSpec) holds all documentation and is itself developed spec-driven, dogfooding the pattern.

[[traycer.ai]] introduces Traycer, a Spec-Driven Development (SDD) planning and orchestration layer that sits above AI coding agents (Cursor, Claude Code, Windsurf, Codex CLI, Gemini CLI, and 15+ others), trusted by 100K+ users. Traycer addresses the root cause of AI agent drift — agents filling in requirement gaps with plausible-but-wrong assumptions — by structuring developer intent into a three-step plan → execute → verify loop. Four task modes cover the full complexity range: **Plan** (single-PR tasks), **Phases** (multi-service, iterative), **Review** (agentic code review with Critical/Major/Minor categorization), and **Epic** (collaborative specs + tickets with shared boards, team assignment, and real-time collaboration). **YOLO Mode** and **Smart YOLO** bring end-to-end automation: Smart YOLO for Epic Mode is an intelligent orchestrator that dynamically updates specs and tickets at runtime, runs parallel executions, and self-adjusts plans as implementation reveals constraints. Available as VS Code, Cursor, and Windsurf extensions, and a GitHub App (Ticket Assist) for turning issues into plans. Traycer occupies the planning-and-verification tier that [[openspec.dev]] and [[github-spec-kit]] address with open-source, file-based approaches — Traycer layers team collaboration, IDE-native UX, and agentic verification on top.

[[snarktank-ralph]] introduces Ralph, an open-source autonomous AI agent loop (19,297 stars, MIT, TypeScript) that runs AI coding tools — Amp or Claude Code — iteratively against a `prd.json` task list until every user story has `passes: true`. The architecture is intentionally minimal: a bash script (`ralph.sh`) as the outer loop, two per-tool prompt files (`CLAUDE.md` for Claude Code, `prompt.md` for Amp) as the per-iteration agent instructions, and three shared state files (git history, `progress.txt`, `prd.json`). Each iteration spawns a completely fresh agent instance with a clean context window, sidestepping the context anxiety failure mode documented in [[anthropic.com-managed-agents]] by discarding context entirely rather than managing it. Two built-in SKILL.md-based skills (`/prd` for generating Product Requirements Documents, `/ralph` for converting them to `prd.json`) are publishable through the Claude Code marketplace. Memory across iterations is kept intentionally lean: `progress.txt` accumulates an append-only learnings log with a `## Codebase Patterns` section at the top, and each iteration's prompt instructs the agent to update nearby `AGENTS.md` files with reusable discoveries — a knowledge accumulation pattern shared with [[gsd-build-get-shit-done]]. The `prd.json` status graph (user stories with `passes` flags) serves a similar role to the structured task tracking in [[coleam00-archon]] and [[gastownhall-beads]], but trades DAG sophistication for simplicity: the loop just picks the highest-priority incomplete story and runs.

[[browse.sh]] is the open web skill catalog and browser CLI for AI agents built by Browserbase. At its core is a 200+ skill registry of site-specific web automation modules — each a SKILL.md file with structured YAML frontmatter and a detailed workflow body for one specific website task, covering domains from Airbnb, Amazon, and AllTrails to weather.gov, arxiv.org, and Yelp. The companion `browse` CLI (`npm install -g browse`) lets agents install catalog skills, drive sessions with primitives (click, scroll, type, eval), debug via network/console tailing, and escalate to Browserbase cloud sessions with CAPTCHA solving and residential proxies for bot-detection-protected sites. Skills encode domain-specific API shortcuts — public Algolia indices, XHR endpoints in Next.js hydration JSON, undocumented REST APIs — that sidestep browser rendering entirely, claimed to cut token costs by 50×. The catalog is agent-discoverable via llms.txt (compact listing) and llms-full.txt (full SKILL.md content for all skills). [[browse.sh]] occupies a complementary but distinct niche from [[skills.sh]] (Vercel's SKILL.md distribution platform for coding agents) and [[anthropics-skills]] (Anthropic's official capability modules): where those platforms serve coding-agent workflows, [[browse.sh]] focuses exclusively on web-site-specific automation with embedded credentials and bot-detection countermeasures.

[[strandsagents.com]] introduces Strands Agents, an open-source model-driven agent harness SDK built by AWS (5,910 stars, Apache 2.0, Python 3.10+ and TypeScript). The SDK provides a complete agentic loop — model invocation, tool execution, conversation management, hooks, streaming, and multi-agent orchestration — in a few lines of code, with Amazon Bedrock as the default provider but full support for Anthropic, OpenAI, Gemini, Ollama, LiteLLM, and 10+ others through a unified model-provider adapter pattern. The key differentiator is a first-class control plane: lifecycle hooks (`BeforeToolCallEvent`, `AfterToolCallEvent`) intercept any tool call for logging, validation, or cancellation, and steering handlers apply policy decisions (`Guide` / `Proceed`) that benchmarked at 100% task accuracy versus 82.5% for prompt-only agents. Built-in multi-agent patterns — Graph (conditional flows with cycles), Swarm (specialist agent pools with autonomous handoff), and Workflow (deterministic parallel DAG as a single tool) — cover the orchestration scenarios documented in [[langchain.com-langgraph]]. Model-provider abstraction, conversation managers (Null, SlidingWindow, Summarizing), and native MCP support make it a comparable entry point to [[litellm.ai]] for teams routing across providers, but where LiteLLM focuses on the gateway tier, Strands Agents covers the full agent runtime tier from loop execution to multi-agent coordination and AWS production deployment via AgentCore Runtime.

[[crewai.com]] introduces CrewAI, the leading open-source multi-agent orchestration framework (51,928 stars, MIT, Python) with an enterprise platform (CrewAI AMP) adopted by 60% of the Fortune 500. Its two-layer architecture — **Crews** (collaborative teams of role-playing agents with YAML-defined role/goal/backstory, tools, memory, and delegation) nested inside **Flows** (event-driven orchestration backbone with `@start()`/`@listen()`/`@router()` decorators and built-in state management) — balances LLM autonomy with deterministic control in a framework built entirely from scratch, independent of LangChain. CrewAI's `Memory` class uses LLM-inferred scope and composite scoring (semantic + recency + importance) for retrieval; its tools package provides 30+ built-in tools and is compatible with LangChain tools. The enterprise AMP platform adds a visual Studio editor, 20+ integration triggers (Gmail, Slack, Salesforce, Teams, HubSpot, Jira, etc.), real-time tracing, HITL flows, hallucination guardrail, PII redaction, A2A agent-to-agent communication, RBAC, SSO, secrets management, and serverless auto-scaling — deployable as SaaS (AMP Cloud) or on-premises/private VPC (AMP Factory). With 450M+ agentic workflows per month and 100,000+ certified developers, CrewAI occupies the same orchestration tier as [[strandsagents.com]] (AWS/Bedrock-centric SDK) and [[langchain.com]] (LangGraph/LangSmith stack), but differentiates on role-playing agent semantics, the dual Crews+Flows architecture, and breadth of enterprise platform features.

[[bmad-code-org-bmad-method]] introduces the BMad Method (BMAD-METHOD), a comprehensive open-source Agile AI Driven Development framework (47,851 stars, MIT, v6.7.1) that structures the full software lifecycle as four progressive phases — Analysis, Planning, Solutioning, and Implementation — each driven by named AI agent personas and 34+ composable workflow skills. Where [[obra-superpowers]] and [[gsd-build-get-shit-done]] focus on disciplined execution patterns and context-rot prevention respectively, BMad Method covers the entire pre-code lifecycle: facilitated brainstorming, Amazon Working Backwards PRFAQ, structured PRD creation and validation, UX design specs, architecture with ADRs, and epic/story breakdown — all before a single line of code is written. Six named agents (Mary the Analyst, John the PM, Winston the Architect, Amelia the Developer, Sally the UX Designer, Paige the Technical Writer) are invoked as IDE skills and share a three-layer TOML customisation system that lets teams commit team-wide overrides and individuals maintain personal preferences without forking. Party Mode (`bmad-party-mode`) runs all agents simultaneously in one conversation for architectural debates, retrospectives, and brainstorming sessions. Installed via `npx bmad-method install`, it generates skill files for Claude Code, Cursor, and other AI IDEs, and is extended via official modules (BMad Builder for custom agents, Test Architect for risk-based testing, Game Dev Studio, Creative Intelligence Suite). BMad Method occupies the structured methodology tier alongside [[coleam00-archon]] (YAML workflow DAGs for execution automation) and [[github-spec-kit]] (spec-driven staged pipeline), adding the most complete phase-by-phase agile planning framework in the wiki.

[[openvibe.sh]] introduces Open Vibe, a free MIT-licensed agent-driven web development course by the Wasp team that turns Claude Code (or any capable coding agent) into a pair-programming tutor. Unlike static tutorials or black-box vibe-coding platforms, Open Vibe embeds its curriculum directly in `llms.txt` — the learner pastes one prompt, the agent fetches structured module files via `curl`, and the course unfolds as a beat-by-beat tutoring session that alternates between TUTOR MODE (explaining concepts) and PAIR PROGRAMMER MODE (writing code alongside the learner). Phase 1 builds a full-stack task management app using the Wasp framework (teaching what a web app is — frontend, backend, database); Phase 2 builds a production SaaS on the Open SaaS template (auth, payments, admin, email, AI-ready) while teaching the auth → payment → access loop, webhooks, and service orchestration. Progress is tracked via a `course-progress.json` file the agent writes into the project, and canary strings at the end of each module let the agent verify it got the raw file and not a summary. With no signup, no cost, and compatibility with Claude Code, Cursor, Codex, and OpenCode, Open Vibe occupies a unique niche in this wiki alongside [[shareai-lab-learn-claude-code]] (structured Claude Code learning resource) and [[skills.sh]] (agent skills platform), as a hands-on, agent-native path from beginner to shipping SaaS founder.

[[anombyte93-prd-taskmaster]] introduces PRD-Taskmaster, a Claude Code skill (4,447 stars, MIT, Python) that structures the process of turning a feature idea into a validated Product Requirements Document and a Taskmaster AI task tree. The skill runs a 12-step workflow: it opens with a preflight check (detecting crash state, existing PRDs, and Taskmaster availability via MCP or CLI), asks 13 discovery questions, writes a comprehensive PRD to `.taskmaster/docs/prd.md`, scores it against 13 quality checks (EXCELLENT / GOOD / ACCEPTABLE / NEEDS_WORK), parses and expands tasks via Taskmaster's MCP server or CLI, inserts USER-TEST checkpoint tasks every 5 implementation tasks, and offers four autonomous execution modes with DateTime tracking, per-task git branches, rollback support, and auto-resume after crash. The v3.0 Codification Refactor split deterministic operations into `script.py` (preflight, validate, load-template, gen-scripts) from AI judgment in `SKILL.md`, eliminating hallucination of structural operations. PRD-Taskmaster is a focused complement to [[bmad-code-org-bmad-method]], which embeds PRD creation inside a broader four-phase agile AI workflow with named agent personas.

[[pydantic.dev]] introduces the Pydantic AI engineering stack — an end-to-end suite of five interlocking Python tools that cover the full lifecycle of an AI application. Pydantic Validation (27,832 stars, MIT, v2.13.4) is the world's most widely used data validation library, downloaded ~550M times/month and embedded in FastAPI, LangChain, OpenAI SDK, Anthropic SDK, CrewAI, LlamaIndex, and ~8,000 other packages; its Rust-backed core enforces type-hint-based validation and JSON Schema generation at high speed. Pydantic AI is a production-grade, type-safe, model-agnostic agent framework (OpenAI, Anthropic, Gemini, Ollama, LiteLLM, and 20+ other providers) with composable capabilities, MCP integration, Agent2Agent support, durable execution, graph-based control flow, and first-class OpenTelemetry observability. Pydantic Logfire is a SaaS and self-hosted observability platform built on OpenTelemetry — LLM cost tracking, distributed tracing, online evals, and an MCP server; it auto-instruments FastAPI, SQLAlchemy, httpx, OpenAI, Anthropic, and 30+ libraries with a single `import logfire`. Pydantic Evals is a code-first evaluation framework (ships in `pydantic-ai` and the Logfire JS SDK) for scoring LLM outputs in CI via deterministic, LLM-as-judge, span-based, or custom evaluators. Pydantic AI Gateway is an open-source (AGPL-3.0) LLM proxy that forwards requests in each provider's native format with granular spend caps and an OTel audit trail, currently consolidating into Logfire. In this wiki, [[pydantic.dev]] sits alongside [[litellm.ai]] (LLM routing, comparable Gateway tier), [[langchain.com]] (competing Python agent stack with LangSmith observability), and [[crewai.com]] / [[strandsagents.com]] (Python agent frameworks), offering the most deeply integrated Python-first AI platform with validation, agents, observability, evals, and routing under one brand.

[[openai-codex-plugin-cc]] introduces OpenAI's official Claude Code plugin for Codex, a thin orchestration bridge that exposes Codex's code review and task-delegation capabilities as first-class slash commands and a subagent inside Claude Code (19,405 stars, v1.0.4, Apache-2.0, JavaScript). The seven commands — `/codex:review`, `/codex:adversarial-review`, `/codex:rescue`, `/codex:status`, `/codex:result`, `/codex:cancel`, and `/codex:setup` — cover the full collaboration loop: read-only reviews of uncommitted changes or branch diffs, steerable adversarial challenge reviews that pressure-test design decisions, background task delegation to the `codex:codex-rescue` subagent with model and effort overrides, and job lifecycle management. An optional Stop-hook review gate auto-runs a targeted Codex review on every Claude response before Claude stops. The plugin adds no separate runtime — all execution flows through the developer's locally installed `codex` CLI and Codex app server, reusing existing authentication, configuration (`~/.codex/config.toml`, `.codex/config.toml`), and machine environment. It occupies the cross-agent workflow tier of this wiki alongside [[anthropics-skills]] (Claude Code plugin ecosystem), [[obra-superpowers]] (Claude Code skill methodology), and [[github-spec-kit]] (slash-command-based SDD pipeline).

[[factory.ai]] introduces Factory, the enterprise platform for agent-native software development, built around an AI agent called Droid that automates the full software development lifecycle — coding, testing, review, documentation, and deployment. Factory's capability stack layers Missions (multi-agent orchestration where an orchestrator coordinates specialized worker and validator droids for large, multi-feature projects), Skills (reusable SKILL.md procedures that package domain knowledge and workflows into composable, version-controlled capabilities), Hooks (shell commands bound to agent lifecycle events for deterministic enforcement of formatting, security, and compliance rules), Custom Droids (specialized subagents with their own prompts, tools, and model choices), and MCP (Model Context Protocol tool connectivity). The Droid CLI runs interactively via a full-screen TUI with Spec mode for structured planning before implementation, or headlessly via `droid exec` for CI/CD pipelines. Factory raised $150M Series C at a $1.5B valuation, ships enterprise-grade controls (SSO/SAML, SOC-2, air-gap, dedicated compute), and integrates natively with GitHub, GitLab, Jira, Linear, Slack, Sentry, and 100+ other tools. In this wiki, [[factory.ai]] occupies the enterprise AI coding agent tier alongside [[traycer.ai]] (spec-driven AI coding orchestration) and [[openai-codex-plugin-cc]] (Codex as a Claude Code subagent), while its multi-agent Missions architecture shares conceptual ground with [[crewai.com]] (role-based crew orchestration) and [[langchain.com-langgraph]] (graph-based agent state machines).

[[vibekanban.com]] introduces Vibe Kanban, an open-source kanban-based project management tool purpose-built for teams running AI coding agents in parallel. Built by bloop (now sunsetting as a company and transitioning to community-maintained open source with 26,494 GitHub stars), it targets the new bottleneck in AI-assisted development: while coding agents work on infinite tasks simultaneously, humans must still plan and review — making that loop the speed limit for shipping. The three-step workflow is Plan (create issues on a kanban board; the issue description becomes the agent's prompt verbatim), Prompt (create a workspace that auto-provisions git worktrees for selected repos and launches the chosen coding agent), and Review (inspect syntax-highlighted diffs, add inline comments per changed line, send feedback back to the agent in chat). Vibe Kanban supports 10+ coding agents — Claude Code, OpenAI Codex, GitHub Copilot, Gemini CLI, Amp, Cursor Agent CLI, OpenCode, Factory Droid, CCR, and Qwen Code — and exposes an MCP server so agents can query issue context programmatically. Team features include shared organisations, projects, and kanban boards; a Docker Compose self-hosting option; and GitHub/Azure Repos integration for creating and merging PRs. In this wiki, [[vibekanban.com]] complements [[factory.ai]] (whose Droid agent it supports) and [[traycer.ai]] (which similarly targets the planning and review layer of agent-assisted development), occupying the "human-in-the-loop orchestration UI" tier rather than the agent-framework or agent-runtime tier.

[[everyinc-compound-engineering-plugin]] introduces Every's Compound Engineering plugin, a comprehensive AI-agent workflow suite for Claude Code, Codex, Cursor, GitHub Copilot, and other coding agents that operationalises the philosophy that each engineering unit should make subsequent units easier rather than harder. With 17,151 stars and 1,326 forks it is one of the most widely adopted coding-agent plugin packages, shipping 38+ skills and 50+ specialized review, research, and workflow agents in a single installable plugin. The core insight is that traditional development accumulates debt while compound engineering inverts this by front-loading 80% of effort in planning and review: `/ce-strategy` anchors a `STRATEGY.md` file capturing product goals and tracks; `/ce-brainstorm` runs interactive Q&A to produce a requirements doc; `/ce-plan` converts requirements to a detailed implementation plan; `/ce-work` executes it; `/ce-code-review` dispatches 10+ parallel specialist agents (correctness, security, performance, maintainability, testing, adversarial) against the result; and `/ce-compound` writes solved-problem learnings to a searchable institutional corpus that future agent sessions retrieve via the `ce-learnings-researcher` agent. The `ce-session-historian` agent extends this memory across Claude Code, Codex, and Cursor sessions. `/ce-product-pulse` closes the outer loop by generating time-windowed usage/performance/error reports saved to `docs/pulse-reports/`, giving the next `/ce-strategy` update real signal rather than intuition. The repo has three independently-versioned surfaces: the plugin itself under `plugins/compound-engineering/`, the Claude marketplace catalog under `.claude-plugin/`, and a Bun/TypeScript converter CLI that translates the Claude Code plugin format into OpenCode, Pi, Gemini CLI, Kiro, Codex, Cursor, Copilot, Factory Droid, and Qwen native formats. In this wiki, [[everyinc-compound-engineering-plugin]] sits at the workflow methodology layer alongside [[openai-codex-plugin-cc]] (Codex as a Claude Code plugin) and [[anthropics-skills]] (Anthropic's official skills library), and its multi-agent review architecture shares conceptual ground with [[crewai.com]] (role-based crew orchestration) and [[github-spec-kit]] (spec-driven development for AI agents).

[[othmanadi-planning-with-files]] introduces Planning with Files, the most-starred (22k+ stars) implementation of Manus-style persistent markdown planning as a multi-IDE skill. It operationalises the core insight from Manus AI — "markdown is my working memory on disk" — that underpinned Meta's $2B acquisition of Manus, and packages the pattern into a SKILL.md skill installable across 17+ IDEs via `npx skills add`. The skill enforces a three-file discipline (`task_plan.md`, `findings.md`, `progress.md`) and wires up lifecycle hooks — PreToolUse re-reads the plan before tool calls, PostToolUse prompts progress updates, and the Stop hook verifies all phases are complete — effectively giving agents persistent memory that survives context resets. Benchmark results against a no-skill baseline show a jump from 6.7% to 96.7% pass rate across 30 verifiable assertions. In this wiki, [[othmanadi-planning-with-files]] represents the "agent working memory" layer: it complements [[anthropics-skills]] (the skill-packaging standard it uses), [[obra-superpowers]] and [[gsd-build-get-shit-done]] (other Claude Code agent skills), [[bmad-code-org-bmad-method]] (structured phase-driven methodology for AI agents), and [[forrestchang-andrej-karpathy-skills]] (Karpathy's context-engineering observations that share the filesystem-as-memory principle).

[[backnotprop-plannotator]] introduces Plannotator, an open-source visual plan-and-code-review UI for AI coding agents. It intercepts the agent's `ExitPlanMode` (and equivalent `Stop`) hook and opens an interactive browser UI where humans can annotate, approve, or request changes before the agent proceeds — turning an opaque plan-then-execute loop into a collaborative, human-in-the-loop checkpoint. With 5,580 stars and v0.19.22 as of May 2026, Plannotator supports seven runtimes (Claude Code, Copilot CLI, Gemini CLI, OpenCode, Pi, Codex, and Droid) through platform-specific integration layers that share a common `@plannotator/server` and `@plannotator/ui` package foundation. Key capabilities include inline annotation of plans and code diffs, plan diff views showing what changed when the agent revises in response to feedback, code review for git diffs and remote GitHub PRs, annotation of arbitrary markdown files or URLs, and end-to-end encrypted plan sharing (AES-256-GCM, zero-knowledge, self-hostable). The zero-knowledge paste service stores only ciphertext; the decryption key lives only in the shared URL. In this wiki, [[backnotprop-plannotator]] represents the "human-in-the-loop review UI" tier alongside [[vibekanban.com]] (kanban-driven agent orchestration with diff review) and [[everyinc-compound-engineering-plugin]] (compound engineering plugin with multi-agent code review), and its plan-annotation pattern shares conceptual ground with [[othmanadi-planning-with-files]] (persistent planning files) and [[anombyte93-prd-taskmaster]] (structured planning workflows for AI agents).

[[goOZSXmrYQ4-my-complete-agentic-coding-workflow-to-b]] presents Cole Medin's complete greenfield agentic coding workflow — a deliberately minimal, evolvable framework contrasted directly with more complex approaches like BMAD and Spec Kit. The framework has two phases: setting up the AI layer (a PRD capturing scope and architecture, a global rules file with permanent coding conventions, reusable commands/skills anchored by a `/prime` session-start command, and a `reference/` folder of on-demand context files), then iterating through PIV loops (Plan, Implement, Validate) for each feature. Subagents are used exclusively for research and planning — never implementation — to preserve the main context window. The prime command uses git log as long-term memory, letting the agent understand codebase evolution before each session. The PIV loop enforces context discipline: planning happens in a conversational context, implementation in a fresh context window with only the plan file as input, and validation combines human code review with live testing plus a growing regression test command set. Over time, three tracks compound: codebase, test suite, and AI layer all evolve together. In this wiki, [[goOZSXmrYQ4-my-complete-agentic-coding-workflow-to-b]] sits alongside [[bmad-code-org-bmad-method]] and [[anombyte93-prd-taskmaster]] as workflow methodologies for agentic coding, [[backnotprop-plannotator]] and [[othmanadi-planning-with-files]] as complementary plan-and-memory tools, and [[9d5bzxVsocw-anthropic-just-dropped-the-new-blueprint]] as a related perspective on agentic coding harness design.

[[microsoft-playwright-mcp]] introduces Microsoft's official Playwright MCP server, which gives LLMs structured browser automation capabilities without requiring vision models. With 32,980 stars (v0.0.75, Apache-2.0, TypeScript), it exposes Playwright's accessibility tree — not screenshots — as deterministic, token-efficient structured data that any LLM can act on reliably. The ~40 core tools cover navigation, clicking, typing, form filling, file upload, JavaScript evaluation, console capture, network inspection, code generation, and accessibility-tree snapshots; opt-in capability tiers add coordinate-based input (`--caps=vision`), PDF generation (`--caps=pdf`), test locator generation and assertions (`--caps=testing`), and DevTools access (`--caps=devtools`). Installation is a single `npx @playwright/mcp@latest` command supported across VS Code, Cursor, Claude Code, Claude Desktop, Copilot CLI, Codex, Factory Droid, Goose, Warp, and 15+ other MCP clients; a Docker image handles headless containerised deployments. Profile management supports persistent per-workspace profiles, isolated ephemeral contexts, and preloaded storage state. The actual browser engine lives upstream in the Playwright monorepo — this repo is the MCP adapter layer maintained on a fast patch-version cadence. In this wiki, [[microsoft-playwright-mcp]] occupies the MCP tool/server tier alongside [[sequentialthinking-mcp]] (reasoning-focused MCP server), and it serves as a browser-automation tool provider for agent SDKs like [[strandsagents.com]] that support MCP tool integration.

[[vercel-labs-agent-browser]] introduces agent-browser, Vercel Labs' open-source native Rust CLI for browser automation purpose-built for AI agents (34,229 stars, Apache-2.0, v0.27.0). Unlike [[microsoft-playwright-mcp]] which wraps Playwright over MCP, agent-browser implements a direct CDP client in Rust with a persistent background daemon — no Node.js in the hot path — and centers on the **snapshot/ref workflow**: `agent-browser snapshot -i` produces an accessibility-tree summary with compact `@eN` refs (~200–400 tokens), which agents use for deterministic `click @e2` and `fill @e3 "value"` calls. The `--json` flag on every command produces clean LLM-parseable output. Beyond the core loop it supports authenticated sessions (per-origin `--headers`), headed mode for debugging, CDP attach to existing browsers (`--cdp`), auto-connect discovery, a WebSocket viewport stream for "pair browsing" alongside an agent, serverless deployment (Vercel Sandbox microVMs, AWS Lambda with `@sparticuz/chromium`), iOS Simulator automation via Appium, and AWS AgentCore cloud browser sessions. The skill is distributed through [[skills.sh]] (`npx skills add vercel-labs/agent-browser`) and works with Claude Code, Cursor, Codex, Gemini CLI, GitHub Copilot, Goose, OpenCode, and Windsurf. In this wiki, [[vercel-labs-agent-browser]] occupies the same browser-automation-for-AI-agents tier as [[microsoft-playwright-mcp]] (MCP-based, multi-browser) and [[browse.sh]] (site-specific skill catalog with domain API shortcuts), with agent-browser distinguishing itself through its native Rust CLI design, snapshot/ref token efficiency, and serverless portability.

[[qa.tech]] is an AI-powered end-to-end testing platform that runs autonomous agents throughout the software development lifecycle — from automated PR reviews against preview deployments to scheduled production monitoring runs. Rather than scripting selectors or maintaining brittle automation suites, teams write test goals in plain English and agents execute, adapt, and generate tests continuously. Its three-agent system routes automatically: a **Chat Assistant** for interactive test creation and management (20+ specialized tools, Jira/Linear integration), a **PR Review agent** that activates on every pull request and uses semantic matching to select 5–15 relevant tests and create 1–3 gap-filling tests only when needed (most PRs create zero), and an **On-Demand PR Testing** agent triggered by `@qatech` comments. The platform uses **Claude Haiku 4.5** (default) or **Claude Sonnet 4.5** for execution decisions — navigation, form-filling, clicks, verification — building a **Knowledge Graph** of the application that compounds context across every run. A **REST API** and **MCP Server** on [[qa.tech]] (five tools: list_applications, list_test_cases, get_run, start_run, rerun_run) expose the platform to Claude Code, Cursor, Codex, and Claude Desktop; a CLI (`@qadottech/cli`) provides terminal access plus tunnel support for local dev environments. Integrations cover GitHub, GitLab, Vercel preview deployments, Jira, Linear, Trello, Slack, Teams, and mobile app testing (iOS/Android). In this wiki, [[qa.tech]] occupies the autonomous testing and CI/CD validation tier alongside [[microsoft-playwright-mcp]] (browser automation via MCP, the underlying automation primitive) and [[vercel-labs-agent-browser]] (Rust-native browser CLI for AI agents), representing the product-layer abstraction above raw browser automation tools.

[[buildermethods-agent-os]] introduces Agent OS, an open-source framework (4,635 stars, MIT) by Brian Casel / Builder Methods that solves the context-injection problem in AI-assisted development: agents lack knowledge of a project's own conventions, so they drift from team standards. Agent OS maintains a `agent-os/standards/` directory of concise markdown files and provides slash commands — `/discover-standards` (extract conventions from code), `/index-standards` (build a fast-lookup `index.yml`), `/inject-standards` (push relevant standards into agent context), `/shape-spec` (prompt targeted questions in plan mode, save to `agent-os/specs/`), and `/plan-product` (create mission/roadmap/tech-stack docs) — that together form a lightweight "operating system" overlay for any AI coding agent. Version 3.0 (January 2026) removed its own orchestration phases and deferred to Plan Mode in Claude Code or Cursor, positioning Agent OS as a pure standards-and-spec layer. In this wiki, [[buildermethods-agent-os]] occupies the same standards-injection and spec-shaping tier as [[github-spec-kit]] (specification-first SDD pipeline with a `specify` CLI), [[bmad-code-org-bmad-method]] (full agile phase lifecycle with named personas), and [[gsd-build-get-shit-done]] (context-engineering commands for Claude Code), with Agent OS distinguished by its focus on evolving codebase standards rather than lifecycle orchestration.

[[www.taskmaster.one]] introduces TaskMaster AI, a commercial SaaS product by TaskMaster AI LLC that serves as a "project manager for AI coding agents." It ingests PRDs, parses them into structured dependency-aware task graphs with stable IDs, and synchronises those tasks across a CLI (`@taskmasterai/cli` npm package), an MCP server, and a web dashboard. Built for Cursor, Windsurf, and VS Code, it provides complexity scoring, task expansion, research mode, and hosted API key management — removing the per-engineer `.env` ritual on Pro/Enterprise plans. Pricing runs from Plus ($29/month, BYOK, 100 tasks) to Pro ($199/month, hosted keys, 1,000 tasks) to Enterprise ($599+/month, 5,000+ tasks, team management, SLAs). **Important:** TaskMaster AI at `taskmaster.one` (npm: `@taskmasterai/cli`) is a distinct commercial product from the open-source `claude-task-master` repo (npm: `task-master-ai`) by @eyaltoledano — they solve the same problem space but differ in ownership, licensing, and package names. In this wiki, [[www.taskmaster.one]] occupies the AI task management and PRD-to-execution tier alongside [[eyaltoledano-claude-task-master]] (the OSS alternative) and [[anombyte93-prd-taskmaster]] (a Claude Code skill that generates PRDs and wires them to Taskmaster).

[[eyaltoledano-claude-task-master]] introduces Taskmaster, the open-source AI task management system by @eyaltoledano and @RalphEcom with 27,242 stars (npm: `task-master-ai`, MIT with Commons Clause, v0.43.1). Now maintained under the Hamster platform at [[tryhamster.com]], it converts PRDs into structured task graphs stored as JSON in the repository, then provides AI-assisted CLI commands and 36 MCP tools to navigate and execute that graph. Core capabilities include `parse-prd` (generates tasks from a PRD file), `analyze-complexity` (scores tasks 1–10 and writes AI-tailored expansion prompts), `expand` (turns complex tasks into subtasks using the complexity report), `next` (finds the highest-priority unblocked task), `research` (fetches fresh external information with project context), and `move` (shifts tasks between named tag workstreams). The MCP server is configurable with `TASK_MASTER_TOOLS` (`core` = 7 tools ~5k tokens, `standard` = 15, `all` = 36) for token-budget-conscious setups. The repo is a JavaScript monorepo (Turborepo) with `src/`, `mcp-server/`, `apps/`, `packages/`, and `docs/`; it includes `CLAUDE.md` and `.cursor/` configs showing how the project uses Taskmaster for its own development. In this wiki, [[eyaltoledano-claude-task-master]] is the OSS counterpart to [[www.taskmaster.one]] (commercial SaaS), pairs tightly with [[anombyte93-prd-taskmaster]] (Claude Code skill that generates PRDs and feeds them into Taskmaster), and complements planning-layer tools like [[buildermethods-agent-os]] (standards injection) and [[gsd-build-get-shit-done]] (spec-driven development).

[[garrytan-gstack]] introduces gstack, Garry Tan's (CEO of Y Combinator) open-source software factory with 101,913 stars — the most comprehensive opinionated Claude Code skill suite in the agentic-AI-frameworks space. Where individual skill packs provide isolated utilities, gstack builds a complete sprint pipeline: Think → Plan → Build → Review → Test → Ship → Reflect, implemented as 50+ named-specialist slash commands all chained so each step feeds its output into the next. The YC-style product interrogation skill (`/office-hours`) writes a design doc that `/plan-ceo-review` reads; the eng-manager skill (`/plan-eng-review`) writes a test plan that `/qa` picks up; `/review` catches bugs that `/ship` re-verifies. Beyond the skill pipeline, gstack adds infrastructure absent from competing packs: a persistent Chromium daemon (Bun-compiled, ~100ms/command after first call, CDP-based, random-port, auto-restart on binary change, dual-listener security architecture for ngrok tunnel mode), iOS QA via USB CoreDevice tunnel, multi-host support for 10 coding agents (Claude Code, Codex, Cursor, Factory Droid, Slate, Kiro, Hermes, and more), team-mode auto-update for shared repos, and cross-machine session memory via gbrain. The ETHOS.md philosophy — 'Boil the Lake' and 'Search Before Building' — is injected into every skill preamble, encoding Tan's claim of an 810× productivity increase over his 2013 pace. In this wiki, [[garrytan-gstack]] sits at the workflow-methodology layer alongside [[anthropics-skills]] (Anthropic's official skills library), [[obra-superpowers]] (spec-first subagent skills), [[gsd-build-get-shit-done]] (context-engineering commands), and [[everyinc-compound-engineering-plugin]] (compound engineering plugin), distinguished by its sprint-pipeline architecture, persistent browser daemon, and iOS QA capabilities.

[[tryhamster.com]] introduces Hamster, an AI-native product planning platform that closes the "velocity gap" — individual developers build 10× faster with AI, but teams stall on alignment and unclear requirements. Hamster implements the full AI-native SDLC (Brief → Align → Plan → Execute → Ship) through two products: **Hamster Studio**, a collaborative workspace where PMs, designers, and engineers co-author structured Briefs, collect alignment votes, and generate task Plans that AI coding agents execute via Cloud Agents or the CLI; and **Taskmaster**, a CLI/MCP tool that parses PRDs into dependency-ordered task graphs, scores complexity, runs live research beyond the model's knowledge cutoff, and drives a TDD autopilot loop (Red → Green → Refactor). The platform's Context Graph continuously ingests connected tools (GitHub, Slack, Linear, Jira, Figma, Notion, Google Drive) so all planning output reflects actual company context. Taskmaster is open-source (MIT, 25K+ stars) and works standalone with BYOK; Studio adds team collaboration on top. In this wiki, [[tryhamster.com]] is the parent platform for [[www.taskmaster.one]] (the Taskmaster product marketing site) and [[eyaltoledano-claude-task-master]] (the open-source Taskmaster GitHub repo), and complements planning-layer tools like [[buildermethods-agent-os]], [[backnotprop-plannotator]], and [[anombyte93-prd-taskmaster]].

[[gitlawb-openclaude]] introduces OpenClaude, an open-source coding-agent CLI (27,861 stars, TypeScript, npm: `@gitlawb/openclaude`) that ports the Claude Code terminal workflow to any model provider. Where Claude Code is locked to Anthropic's backend, OpenClaude replaces it with a descriptor-first integration layer: descriptors in `src/integrations/` define vendors, gateways, model catalogs, and transport capability flags; registry helpers load those descriptors and expose route/model lookups; runtime metadata bridges descriptor state into request execution without per-provider switches. Supported backends include OpenAI-compatible APIs (OpenAI, OpenRouter, DeepSeek, Groq, Mistral, LM Studio), Gemini, GitHub Models, Codex OAuth, Codex, Ollama, Atomic Chat, Gitlawb Opengateway, Xiaomi MiMo, Bedrock, Vertex, and Foundry — all configured via `/provider` wizard or environment variables. Agent routing (`~/.openclaude.json`) lets teams map named agent types (`Explore`, `Plan`, `general-purpose`, `frontend-dev`) to different model endpoints for cost optimisation. A headless gRPC server mode (`npm run dev:grpc`) exposes the engine at `localhost:50051` with bidirectional streaming; `src/proto/openclaude.proto` enables client generation in Python, Go, or Rust. A VS Code extension is included for launch integration and provider-aware UI. In this wiki, [[gitlawb-openclaude]] sits at the provider-abstraction layer alongside [[openrouter.ai]] (unified API gateway for 400+ models) and [[litellm.ai]] (Python SDK + proxy server for 100+ providers), distinguished by being a full CLI coding-agent runtime rather than a routing layer alone. It is the direct community equivalent of Anthropic's Claude Code CLI and is most closely related to other agentic coding loop tools [[snarktank-ralph]] and [[openai-codex-plugin-cc]].

[[must-have-clis-2026]] presents the case — by Prosper Otemuyiwa (unicodeveloper), published April 2026 — that CLIs have decisively surpassed MCP servers as the preferred tool-integration pattern for AI-agent workflows in developer contexts. The article's central evidence: one study found CLI-based agents completed the same tasks as MCP-based agents at 10–32× lower token cost and ~100% vs 72% reliability; Anthropic's internal research found shell-script tool use cut token usage by 98.7% versus MCP calls. The source profiles ten first-class CLIs that remove specific developer friction points — GitHub CLI (`gh`) for repository management, Stripe CLI for local webhook testing, Supabase CLI for local-first database development, Valyu CLI for proprietary/specialised data access (SEC filings, PubMed, FRED, ChEMBL), PostHog CLI for framework-aware analytics setup, ElevenLabs CLI for TTS/STT/voice-cloning in agent pipelines, Ramp CLI for scriptable expense management, Google Workspace CLI (`gws`) for Workspace API access, Agentmail CLI for ephemeral agent email inboxes, and Vercel CLI for preview deployments. The article does not position MCP as obsolete — it recommends MCP for enterprise OAuth, multi-tenant compliance, and services without a CLI — but argues the most effective 2026 agentic architectures use CLIs as the default and MCP only when required. In this wiki, [[must-have-clis-2026]] complements [[browse.sh]] (terminal-first web automation) and [[skills.sh]] (SKILL.md-based CLI skill distribution) as a perspective on the CLI layer of agentic AI infrastructure.

[[nvidia-nemotron-3-ultra]] summarizes Decrypt's June 2026 coverage of Nvidia's Nemotron 3 Ultra launch at Computex: a 550B-parameter (55B active) open-weight mixture-of-experts model with public weights, 1M-token context, Mamba-2 + attention hybrid stack, multi-token prediction, and RL post-training aimed at multi-step agentic tasks. Artificial Analysis scored it 48 on its Intelligence Index — top among U.S. open models but six points behind Moonshot AI's Kimi K2.6 (54) — while a pre-release DeepInfra endpoint delivered 300+ output tokens/sec versus 50–100 for comparable Chinese APIs. The piece situates Ultra within Nvidia's Nemotron family (Nano / Super / Ultra), its $26B open-weight bet, and the Nemotron Coalition (Mistral AI, Perplexity, others) building Nemotron 4 on DGX Cloud. For routing such models in agent stacks, see [[litellm.ai]] and [[openrouter.ai]]; for open-weight hosting ecosystems, see [[huggingface.co]].

[[agent-field-pr-af]] introduces PR-AF, an open-source AgentField-native pull request reviewer (28 stars, Python, Apache 2.0) that replaces the single-LLM-pass checklist model with a seven-phase adaptive pipeline. For each PR it classifies complexity and risk surfaces, programmatically maps diff anatomy and blast radius, then uses a meta-prompting planner to spawn N parallel reviewer agents with runtime-crafted prompts — no hardcoded security/performance/style categories. Findings pass through AST-based evidence grounding (unsubstantiated claims are pruned), adversarial falsifiability gates, and compound-vulnerability synthesis that clusters cross-file anomalies into coordinated exploits. Output is evidence-backed inline GitHub comments; a ~500-line PR costs ~$0.80 in BYOK LLM calls and runs 35–50 minutes. Trigger via `af call pr-af.review`, HTTP API, or a label-triggered GitHub Actions workflow. In this wiki, [[agent-field-pr-af]] sits at the deep CI/CD code-review layer alongside [[everyinc-compound-engineering-plugin]] (multi-agent tiered review inside Claude Code), [[qa.tech]] (AI validation across the SDLC), and [[garrytan-gstack]] (sprint-pipeline `/review` step), distinguished by AgentField orchestration, evidence grounding, and compound-risk detection.

[[x.com-ericzakariasson-building-clis-for-agents]] captures Eric Zakariasson's (@ericzakariasson) design guide for CLIs that AI agents can actually use — ten patterns addressing the gap between human-oriented command-line tools and agent execution constraints. The core failure modes: interactive prompts agents cannot answer, help pages without runnable examples, non-idempotent retries that create duplicates, and destructive commands without `--dry-run` preview. The prescribed contract: non-interactive flags for every input, progressive discovery via subcommand `--help`, example-rich help blocks agents pattern-match from, stdin pipeline support, fail-fast actionable errors, idempotent state changes, dry-run and `--yes`/`--force` bypass paths, consistent resource+verb naming, and structured success output (deploy ID, URL, duration). In this wiki, the post complements [[must-have-clis-2026]] (which argues CLI-over-MCP and catalogs ten production CLIs) by answering how to design agent-accessible CLIs, and connects to harness patterns in [[shareai-lab-learn-claude-code]] and [[gsd-build-get-shit-done]] where shell invocation is the primary agent tool interface.

[[focusee.imobie.com]] introduces FocuSee, an AI-powered screen recorder for macOS and Windows from iMobie that automatically turns raw captures into polished product demos, tutorials, and marketing videos. After recording, FocuSee applies auto-zoom on clicks, cursor animations (40+ styles), AI subtitle generation in 55+ languages, filler-word and silence removal, audio enhancement, webcam background removal, annotations, 3D motion, and optional AI avatars — positioning itself as a cross-platform Screen Studio alternative starting at $19.99/month. The product is a closed-source desktop app with no public API or agent-integration layer; its relevance to agentic AI workflows is primarily as a **demo and tutorial production tool** for showcasing agent products, CLIs, and harnesses to end users.

[[wisprflow.ai]] introduces Wispr Flow, an AI-powered voice-to-text dictation app that lets users type 4× faster (220 wpm via speech vs. 45 wpm typing) by dictating into any application on macOS, Windows, iOS, and Android without per-app integrations. Its AI pipeline applies auto-edits (filler-word removal, grammar, punctuation), a personal dictionary for names and jargon, voice snippets for reusable templates, and tone styling. For developers specifically, Wispr Flow adds syntax-smart dictation with camelCase/snake_case awareness and file tagging in Cursor and Windsurf. Enterprise-grade compliance (SOC 2 Type II, HIPAA, ISO 27001) and a Privacy Mode with zero data retention make it viable for healthcare and legal use cases. Wispr recently raised $81M to develop a broader "Voice OS" platform, positioning voice dictation as an input-layer alternative for agent-assisted knowledge work rather than a standalone transcription utility.

[[voltagent-awesome-agent-skills]] is the most-contributed curated list of agent skills in the ecosystem (24,603 stars, MIT), aggregating 1,424+ hand-picked SKILL.md-based capability modules from official development teams and the community. It covers official skills from 50+ vendors — Anthropic (17), Microsoft (133 across 6 languages), OpenAI (37), Sentry (27+), Flutter (22), Google Gemini (4), Google Labs Stitch (6), Google Workspace CLI (17), Cloudflare (8), Netlify (12), Expo (11), Hugging Face (13), Trail of Bits (21 security skills), Auth0 (14), Apollo GraphQL (13), Firebase (12), HashiCorp Terraform (11), fal.ai (15), and more — plus hundreds of community skills organized by domain (context engineering, cybersecurity, product management, advertising, n8n automation, specialized domains). The companion platform officialskills.sh provides a browsable per-org web registry. Unlike [[anthropics-skills]] (Anthropic's 17 official skills, fully listed here) and [[skills.sh]] (the Vercel-built skills install CLI and leaderboard), Awesome Agent Skills is a pure discovery catalog that aggregates the entire ecosystem including [[obra-superpowers]] and every other SKILL.md-compatible collection across all eight supported agent tools.

[[pi.dev]] introduces Pi, a minimal terminal coding harness by Earendil Inc. (60,792 GitHub stars, MIT) built around the principle of "primitives, not features." Pi is a TypeScript monorepo providing four packages — `pi-coding-agent` (the `pi` CLI), `pi-agent-core` (agent runtime), `pi-ai` (unified multi-provider LLM API), and `pi-tui` (terminal UI with differential rendering) — plus a package registry of 3,726 extensions installable via `pi install npm:<name>`. The CLI supports 15+ providers (968 models across Anthropic, OpenAI, Google, Azure, Bedrock, Mistral, Groq, Cerebras, xAI, HuggingFace, Ant Ling, NVIDIA NIM, MiniMax, and more), four operating modes (interactive TUI, print/JSON, JSONL RPC, Node.js SDK), tree-structured JSONL session history, and TypeScript extensions that can detect `ctx.mode`, inspect system prompt options, and route built-in tools through MCP or into sandboxed micro-VMs via the Gondolin extension pattern. The latest release (v0.78.1, June 2026) added Ant Ling and NVIDIA NIM providers; Pi's Node.js minimum is v22.19.0. Supply-chain hardening includes exact dependency pinning, shrinkwrap on the published npm package, and scheduled CI audit checks. In this wiki, [[pi.dev]] sits alongside [[gitlawb-openclaude]] (multi-provider coding-agent CLI) and [[coleam00-archon]] (agent builder framework) as terminal-first coding-agent runtimes, and connects to [[voltagent-awesome-agent-skills]] and [[anthropics-skills]] via its SKILL.md extension compatibility.

[[graphify.net]] introduces Graphify, an open-source knowledge graph skill for AI coding assistants (63,000+ stars, YC S26, MIT) that turns an entire codebase — source code, documentation, research papers, and diagrams — into a queryable graph and delivers a reported 71.5× token reduction on a 52-file corpus. The core pipeline runs Tree-sitter AST extraction locally across 28 code grammars (source code never leaves the machine), then uses the configured AI model API for semantic extraction of docs, PDFs, and images; the resulting NetworkX graph is clustered using the Leiden algorithm over graph topology alone — no vector embeddings or vector store required. Running `/graphify .` produces three outputs: an interactive HTML graph, a `GRAPH_REPORT.md` audit of god nodes (highest-degree, most-connected concepts), surprising cross-file connections, and suggested questions, and a persistent `graph.json` queryable without re-reading source files. Native always-on integration with 24+ platforms — Claude Code, Codex, Cursor, Gemini CLI, GitHub Copilot CLI, Factory Droid, and more — writes platform-native config (CLAUDE.md + PreToolUse hooks, AGENTS.md, `.cursor/rules/`) that ensures the graph is consulted before file-search tool calls rather than after. An MCP server (`python -m graphify.serve`) exposes `query_graph`, `get_node`, `get_neighbors`, and `shortest_path` tools, and a PR dashboard (`graphify prs`) adds CI state, triage ranking, and merge-order conflict detection. Every edge is tagged `EXTRACTED` (confidence 1.0), `INFERRED` (with score), or `AMBIGUOUS`, giving agents explicit provenance for every relationship they reason about. The companion GitHub repo (safishamsi/graphify) adds an ARCHITECTURE.md showing the strict seven-stage pipeline (detect → extract → build_graph → cluster → analyze → report → export), module schemas, confidence label semantics, and a guide for adding new language extractors. In this wiki, [[graphify.net]] occupies the code-context and index-guided retrieval tier alongside [[skills.sh]] (skill distribution platform that Graphify is installable through) and [[anthropics-skills]] (Claude Code skills ecosystem), and provides the structural graph substrate that complements the file-based planning memory approaches of [[othmanadi-planning-with-files]] and [[buildermethods-agent-os]].

[[open-design.ai]] introduces Open Design (62,118 stars, Apache-2.0, v0.9.0), the open-source, local-first alternative to Claude Design — a complete agent-native design platform that turns any of 21+ supported coding agent CLIs (Claude Code, Codex, Cursor, Gemini CLI, and more) into a design engine via composable SKILL.md skills and portable DESIGN.md brand systems. Built in direct response to Anthropic's April 2026 Claude Design launch, it ships as a native desktop app (macOS, Windows, Linux AppImage), a CLI with a local daemon at port 7456, and an MCP server installable per agent via `od mcp install <agent>`. The platform generates HTML, PDF, PPTX, MP4 (via HyperFrames), and image artifacts in a four-stage loop (Detect → Discover → Direct → Deliver) without sending artifacts to a vendor cloud. Its 155 SKILL.md skills, 261 plugins, and 150+ DESIGN.md brand systems (covering Linear, Vercel, Stripe, Anthropic, and others) are distributed through the same ecosystem as [[skills.sh]] and [[anthropics-skills]], and its methodology shares the SKILL.md composability pattern pioneered by [[obra-superpowers]]. The adapter architecture — each coding agent CLI implements an `AgentAdapter` TypeScript interface detected via PATH scan and config-dir probe at daemon startup — enables provider switching without redesigning skills or artifacts; the companion repo at nexu-io/open-design documents the full adapter spec, deployment topologies, and contributing guide for new CLIs.

[[brave-search]] introduces Brave Search API, a privacy-first web search API giving AI agents access to the world's largest independent Web index — 30+ billion pages refreshed 100 million times daily — without relying on Bing or Google data. The API exposes seven endpoints (Web, LLM Context, News, Image, Video, Local/Place Search, Summarizer) behind a native MCP server (`brave/brave-search-mcp-server`, 1,166 stars, TypeScript) that wires into Claude Desktop, VS Code, and any MCP-compatible agent runtime with zero HTTP client code. The LLM Context endpoint is the recommended agentic integration point: it bundles web fetching, content extraction, and configurable token/snippet budgets into one call, returning structured context ready for RAG pipelines rather than raw HTML or link lists. Search Goggles enable declarative result re-ranking — allowing teams to filter SEO spam, boost primary sources, or surface domain-specific content — and the enterprise tier adds SOC 2 Type II attestation, Zero Data Retention, and Amazon Bedrock AgentCore support. Pricing starts at $5/1K requests with $5 monthly free credits; the Answers tier at $4/1K adds streaming summarization grounded with citations.

[[resend.com]] introduces Resend, an email API platform for developers that sends transactional and marketing emails at scale via a clean REST API (`api.resend.com`) with SDKs for nine languages and a full OpenAPI 3.0 spec. What distinguishes Resend within an agentic AI stack is its deliberate agent-first design: the official CLI (`resend`) automatically emits structured JSON when stdout is piped (non-TTY detection), supports `--dry-run` payload validation, exposes a `resend commands` discovery endpoint that returns the full command tree as JSON, and follows the CLI design principles documented in [[x.com-ericzakariasson-building-clis-for-agents]]. An official MCP server (`resend/resend-mcp`) wires Resend capabilities into Cursor, Claude, and any MCP-compatible agent without HTTP client code. Five SKILL.md agent skills — Resend API, React Email, Email Best Practices, CLI, Agent Email Inbox — are published at `resend/resend-skills` and installable via `npx skills add resend/resend-skills`, placing Resend in the same agent-skill distribution ecosystem as [[skills.sh]] and [[voltagent-awesome-agent-skills]]. The `email.received` webhook event enables bidirectional agent email threads: an agent sends via the API and listens for replies via Svix-signed webhook delivery. The companion CLI repo (`resend/resend-cli`, MIT, TypeScript, 372 stars, v2.3.0) ships its own SKILL.md and a `skill-evals/` test suite. In this wiki, [[resend.com]] occupies the email-sending infrastructure layer that complements the CLI-design and agent-tooling sources [[must-have-clis-2026]] and [[x.com-ericzakariasson-building-clis-for-agents]].

[[hostinger.com]] is a global web hosting provider (5M+ clients, 150+ countries) that has evolved into an AI-augmented full-stack web platform combining shared hosting, VPS, cloud, domain registration, and AI tooling. Its three hosting tiers (Premium/Business/Cloud Startup at $2.99–$7.99/mo) and KVM VPS instances ($6.49–$25.99/mo) are differentiated by Kodee — an AI assistant that manages servers via conversational chat in the hPanel dashboard and VPS web terminal — and by Horizons, a no-code AI web and app builder where users describe an idea and the platform generates a full-stack application with a backend database, user accounts, and third-party payment integrations. One-click VPS deployment of open-source agentic tools — n8n workflow automation, OpenClaw, and Hermes Agent — positions Hostinger as a self-hosting platform for AI agent infrastructure alongside more specialized providers. In this wiki, [[hostinger.com]] occupies the cloud-hosting and vibe-coding tier as a practical deployment target for teams that want affordable VPS hosting for n8n and similar agent tools without managing a full-scale cloud provider account.

[[webhook.site]] introduces Webhook.site, a webhook testing and workflow-automation service that hands every visitor an instantly-usable unique URL, e-mail address, and DNS name (DNSHook), displaying everything sent there in real time. Beyond inspection, paid tiers turn it into zero-infrastructure plumbing for web automation: Custom Actions chain 50+ cloud-executed workflow steps (JSON/Regex/XPath extraction, HTTP forwarding, scripting in JavaScript and WebhookScript, plus native Google Sheets, Slack, S3, Dropbox, and database integrations — with an "Action AI" generator that builds chains from a prompt), Schedules provide cron jobs and uptime monitoring, and a built-in PostgreSQL-compatible Database (CockroachDB-backed) stores workflow state. The MIT-licensed `whcli` CLI tunnels incoming requests to localhost over a persistent WebSocket, ngrok-style, and an OpenAPI Mock action turns any uploaded spec into an instant mock server. The platform (400K monthly users, 700M requests/day, operated from Denmark since 2020) positions itself as an alternative to Zapier, Ngrok, Localtunnel, and Pipedream; the core inspection app is open source (`webhooksite/webhook.site`, 6,602 stars, Laravel + Angular.js, self-hostable via Docker/Helm). In this wiki it complements [[resend.com]] on the inbound side of email/webhook infrastructure: a quick way to give an agent an inspectable HTTP endpoint, mock an API, or bridge incompatible services without standing up a server.

[[ngrok.com]] introduces ngrok, a globally distributed cloud networking platform that secures, transforms, and routes traffic to services running anywhere — local machines, cloud VMs, Kubernetes clusters, or IoT devices — without firewall changes or open inbound ports. Originally a developer tunneling tool ("share localhost"), ngrok has evolved into an enterprise-grade unified ingress platform combining secure tunnels, a CEL-based Traffic Policy engine, an AI Gateway for multi-provider LLM routing (OpenAI, Anthropic, Gemini, and 10+ more with automatic failover and PII redaction), a Kubernetes Operator, Traffic Observability, and Agent SDKs in Python, Go, Node, and Rust. The AI Gateway is directly relevant to agentic stacks: teams point any OpenAI-compatible SDK at ngrok's endpoint and get automatic multi-provider failover, cost-based routing, and observability without code changes. [[garrytan-gstack]] uses ngrok tunnel mode to expose a localhost Chromium daemon to remote coding agents; [[webhook.site]] covers the complementary inspection-only (no local agent required) side of the webhook-testing problem.

[[microsoft-autogen]] documents AutoGen, Microsoft Research's open-source multi-agent framework (58,823 stars, Python + .NET) that pioneered experimental multi-agent orchestration patterns now widely referenced across the field. AutoGen's layered architecture — Core (event-driven Actor runtime), AgentChat (high-level conversational API), Extensions (model clients, MCP, Docker, gRPC), and Studio (no-code visual team builder) — provides a complete stack from distributed runtime to rapid prototyping. As of 2026 AutoGen is in maintenance mode, with Microsoft directing new projects to the enterprise-grade Microsoft Agent Framework successor, but 0.4.x remains functional and widely deployed. The source covers the full API surface: `AssistantAgent`, `AgentTool`-based multi-agent orchestration, MCP via `McpWorkbench`, code execution via `DockerCommandLineCodeExecutor`, eight documented multi-agent design patterns (concurrent, sequential workflow, group chat, handoffs, mixture of agents, debate, reflection, code execution), and AutoGen Studio's Team Builder + Playground + Gallery + Deployment UI. The companion GitHub raw (microsoft/autogen) adds architecture diagrams, README quickstart examples, and the package layout spanning `autogen-core`, `autogen-agentchat`, `autogen-ext`, `autogen-studio`, and `agbench`. [[microsoft-autogen]] sits alongside [[crewai.com]], [[strandsagents.com]], and [[langchain.com-langgraph]] as the primary multi-agent framework references in this wiki.

[[microsoft-agent-framework]] is Microsoft's production-grade successor to AutoGen (11,206 stars, MIT, python-1.8.1). Where AutoGen pioneered experimental multi-agent research patterns, MAF is designed for teams taking agents to production: stable 1.x APIs, long-term support, graph-based workflow orchestration (sequential, concurrent, handoff, group collaboration), checkpointing and time-travel for restartability, built-in OpenTelemetry observability, and deployment to Microsoft Foundry with two extra lines of code. The provider model spans 10+ backends — Microsoft Foundry, Azure OpenAI, OpenAI, GitHub Copilot SDK, Anthropic, Gemini, Mistral, Bedrock, and Ollama — letting teams swap providers without rewriting orchestration. The Python monorepo ships 30+ focused packages covering core runtime, orchestration, provider adapters, storage (Cosmos, Redis, mem0), hosting (Azure Functions, Durable Task, Foundry), A2A and AG-UI protocols, declarative YAML agents, and an AF Labs experimental tier for benchmarking and RL research. Both AutoGen and Semantic Kernel migration guides are included. In this wiki, [[microsoft-agent-framework]] pairs directly with [[microsoft-autogen]] (its predecessor) and occupies the same production multi-agent tier as [[crewai.com]], [[strandsagents.com]], and [[langchain.com-langgraph]].

[[hermes-agent.nousresearch.com]] introduces Hermes Agent, an open-source autonomous agent by Nous Research (189k stars, MIT, v0.16.0 "The Surface Release") built around a closed learning loop that distinguishes it from session-scoped agents: it autonomously creates SKILL.md files from complex tasks, self-improves them during use, and maintains bounded cross-session memory (MEMORY.md + USER.md injected at session start with a frozen-snapshot pattern that preserves LLM prefix cache). Designed to run persistently on remote servers and accessed from 24+ messaging platforms — Telegram, Discord, Slack, WhatsApp, Signal, and more — or directly via CLI, Hermes is location-independent from the user's device. Six terminal backends (local, Docker, SSH, Singularity, Modal, Daytona) and support for 18+ LLM providers make it deploy-anywhere. The skills system is compatible with the agentskills.io open standard and the [[skills.sh]] directory; the companion GitHub raw (NousResearch/hermes-agent) documents the full architecture, development principles (narrow-waist core, prompt-cache invariants), and migration guide from its predecessor OpenClaw. In this wiki, Hermes sits alongside [[obra-superpowers]] on the skills-methodology axis and pairs with [[skills.sh]] for the skills-discovery ecosystem.

[[felix-forever-hermes-agent-desktop]] introduces Hermes Agent Desktop, an open-source native desktop client (Python 3.11 + pywebview, MIT, v1.0.0) that transforms the [[hermes-agent.nousresearch.com]] CLI into a 20-specialist AI team. A Project Manager agent automatically decomposes user requirements, delegates sub-tasks to domain specialists (UI Designer, Frontend/Backend/Full-stack Engineers, QA, Architect, DevOps, DBA, and more), and synthesizes integrated deliverables — no manual prompt engineering required. The implementation is deliberately minimal: a single-file HTML/CSS/JS frontend served by a lightweight aiohttp backend, with no Electron dependency. Two features absent from the original Hermes CLI are added here: a visual Skill Store (50+ curated skills from CocoLoop Skill Hub with one-click install and real-time search) and a full Agent CRUD dashboard for creating and monitoring custom agents with live status. The desktop client requires the Hermes Agent core (cloned as a local dependency) and supports any OpenAI-compatible LLM provider including DashScope, OpenAI, OpenRouter, DeepSeek, and Moonshot/Kimi. At 53 stars and 12 forks since its v1.0.0 launch in April 2026, the project is early-stage but illustrates a growing pattern of wrapping capable open-source agent runtimes in multi-agent GUI shells.

[[cognition.ai]] introduces Cognition and its flagship product Devin, marketed as the first autonomous software engineer — an AI agent that independently plans, writes, tests, and ships production code inside existing codebases without ongoing human steering. Backed by $21M Series A (Founders Fund) and a subsequent Series D, Cognition trains its own SWE-1 model family (SWE-1.6 for general tasks; SWE-Check for ~10× faster bug detection) rather than relying on general-purpose frontier models. On SWE-bench, the original Devin achieved 13.86% autonomous resolution of real-world GitHub issues — compared to 1.96% for the previous state-of-the-art — and Cognition has since built a production productivity-measurement system that quantifies Devin's value in equivalent engineering hours per session. Devin 2.2 added computer use (Linux desktop access with screen recordings), a self-correction loop (Review Autofix), and a 3× startup improvement; Devin Desktop makes that full-cycle experience available natively. Enterprise customers (Mercedes-Benz, Goldman Sachs, Ramp, Anduril, Nubank) use Devin for legacy modernization, test generation, migration work, and general sprint tasks. In this wiki, [[cognition.ai]] occupies the same autonomous-coding-agent tier as [[factory.ai]] (Droid), with both targeting enterprise teams that need end-to-end task execution rather than inline AI assistance.

[[browserbase.com]] is the complete cloud platform for building and deploying AI agents that browse and interact with the web like humans. Where plain fetch/search APIs cover only ~15% of the web, Browserbase provides access to the other 85% — locked behind anti-bot systems, CAPTCHAs, authentication walls, and dynamic JavaScript — through a unified API key covering headless browser fleets, Search & Fetch, Agent Identity, Functions, and a Model Gateway. The platform's signature open-source component is Stagehand (23K+ GitHub stars, MIT), an SDK with `act()`, `extract()`, `observe()`, and `agent()` primitives that replace brittle CSS selectors with natural language instructions, self-heal on page changes, and auto-cache repeated actions for ~30% cost reduction. Agent Identity is a distinctive differentiator: an identity ladder from API keys through credential vaults to Cloudflare-verified cryptographic sessions and Tools for Humanity proof-of-humanity, enabling agents to pass bot detection and handle 2FA without workarounds. At 36.9M+ sessions, 100K+ developers, and customers including Ramp, Vercel, and DeepMind, Browserbase occupies the cloud browser infrastructure tier of the agentic AI stack alongside [[microsoft-playwright-mcp]], [[vercel-labs-agent-browser]], and [[browse.sh]].

[[aaif-goose-goose]] documents goose, an open-source, general-purpose AI agent built in Rust (48,633 stars, Apache-2.0) and now stewarded by the Agentic AI Foundation (AAIF) at the Linux Foundation. goose runs natively on macOS, Linux, and Windows as a desktop app, a CLI, and an embeddable API — and connects to 15+ LLM providers and 70+ extensions via the Model Context Protocol. Its three-component architecture (Interface + Agent + Extensions) drives an interactive loop that executes tool calls, revises context for token efficiency, and handles errors by returning them to the model rather than breaking the flow. Dual ACP support lets goose act as an ACP server (consumable by JetBrains and Zed) or delegate tool execution to external ACP agents like Claude Code and Codex. Written as a Rust workspace of focused crates (core, CLI, server, MCP extensions, provider adapters, ACP macros, SDK), goose is the wiki's primary example of a local-first, provider-agnostic agent runtime with a native desktop UX. It sits at the intersection of [[njbrake-agent-of-empires]] (terminal session manager for multiple agents), [[strandsagents.com]] (multi-provider agent SDK), and [[microsoft-agent-framework]] (production agent framework), but is distinguished by its emphasis on end-user runtime rather than developer SDK.

[[mainframe.app]] introduces Mainframe, a video generation platform designed for AI agent workflows: it converts agent work sessions into shareable, watchable videos for teams that need to stay current without running agents themselves. Positioned as MCP + skill compatible, Mainframe integrates directly with Claude Code, Cursor, Codex, and other AI coding tools — agents can trigger video generation as part of their workflow. Videos are enhanced with a configurable avatar persona, AI voice narration, and brand palette/typeface before being distributed via shareable links suitable for Slack, PR comments, or async standups. The five core use cases are: coding agent team recaps (what shipped, what's blocked), PR change summaries, documentation walkthroughs, design reviews with side-by-side tradeoffs, and marketing metrics recaps. Mainframe fills the agent observability and communication tier of the agentic AI stack: where tools like [[qa.tech]] and [[traycer.ai]] focus on automated testing and code review respectively, Mainframe focuses on translating agent activity into a human-consumable video format for passive team members.

[[santifer-career-ops]] documents Career-Ops, an open-source AI-powered job search automation system (52,587 stars, MIT, v1.10.0) that applies the same skill-mode architecture popularized by [[obra-superpowers]] and [[anthropics-skills]] to personal job search workflows. Built by Santiago (santifer) after using it to evaluate 740+ job offers and land a Head of Applied AI role, it turns any AI coding CLI — Claude Code, Gemini CLI, Codex, OpenCode, Qwen, Copilot — into a full job search command center through 15 skill modes. The core pipeline evaluates job descriptions with a 6-block A-F scoring system, generates ATS-optimized PDF CVs via Playwright, and tracks everything in local Markdown files with pipeline integrity scripts. A zero-token portal scanner hits Greenhouse/Ashby/Lever APIs directly (45+ pre-configured companies), and a batch processing mode runs N parallel headless CLI workers for high-volume evaluation. A Go/Bubble Tea terminal dashboard completes the stack. Like [[gsd-build-get-shit-done]], it exposes a slash-command interface over a structured agentic pipeline with persistent state — but targeting job search rather than software development.

[[supermemory.ai]] introduces Supermemory, a research and product lab building the memory and context infrastructure layer for AI agents. Its five-component API — User Profiles, Memory Graph (custom vector graph with ontology-aware edges), Retrieval (hybrid vector + keyword, sub-300ms), Extractors (PDFs, images, audio), and Connectors (Notion, Google Drive, S3, Gmail) — gives agents persistent cross-session knowledge without building storage and retrieval infrastructure from scratch. State-of-the-art benchmark positions (LongMemEval 85.2%, LoCoMo #1, ConvoMem #1) back the performance claims. Three integration surfaces cover the full spectrum: a developer API + Python/TypeScript SDK for production systems; an MCP server (`mcp.supermemory.ai`) that adds shared memory to Claude, Cursor, ChatGPT, and 15+ MCP-compatible clients with one command; and a consumer app at `app.supermemory.ai` used by 10,000+ power users. A usage-based pricing model (SM tokens, uniqueness-deduplicated) runs from a free tier to an enterprise tier with SOC 2, HIPAA, and air-gapped self-hosting. Supermemory is the API-first counterpart to the self-hosted, markdown-on-disk approach of [[runcabinet.com]] and the bookmark/note-management approach of [[reseek.net]].

[[mattpocock-skills]] is Matt Pocock's opinionated engineering skills collection (126,065 stars, MIT), distributed via [[skills.sh]] under `mattpocock/skills` and built around four diagnosed failure modes of AI-assisted development: misalignment (fixed by `/grill-with-docs` — a relentless pre-coding interview that updates `CONTEXT.md` and ADRs inline), excessive verbosity (fixed by building a shared domain language), broken code (fixed by `/tdd` — a strict red-green-refactor vertical-slice loop that guards against horizontal slicing), and software entropy (fixed by `/improve-codebase-architecture`, run periodically to find deepening opportunities guided by `CONTEXT.md` and `docs/adr/`). Unlike process-owning frameworks such as [[bmad-code-org-bmad-method]], [[gsd-build-get-shit-done]], and [[github-spec-kit]], Pocock's skills are deliberately small and composable — they scaffold a shared language and a test-first discipline without taking over the workflow. The collection spans 15 published skills: 10 in `engineering/` (diagnose, grill-with-docs, improve-codebase-architecture, prototype, setup-matt-pocock-skills, tdd, to-issues, to-prd, triage, zoom-out), 5 in `productivity/` (caveman, grill-me, handoff, teach, write-a-skill), and 4 in `misc/`, plus per-skill bundled resource files (e.g., `/tdd` ships `tests.md`, `mocking.md`, `deep-modules.md`, `interface-design.md`, `refactoring.md`). Install with `npx skills@latest add mattpocock/skills`; all engineering skills consume a per-repo `.mattpocock-skills-config` set up by `/setup-matt-pocock-skills`. [[mattpocock-skills]] occupies the same skills-ecosystem tier as [[anthropics-skills]] and [[obra-superpowers]], distinguished by its deep emphasis on software engineering fundamentals (TDD, domain language, architecture) as the primary lever for reliable AI-assisted development.

[[shadcn-improve]] is a codebase audit and planning skill (1,977 stars, MIT) by shadcn — creator of shadcn/ui — that formalizes the insight that judgment and execution are separate cognitive tasks worth routing to differently-priced models. The capable model runs `/improve`, which maps the repo, fans out parallel subagents across nine audit categories (correctness, security, performance, test coverage, tech debt, dependency migrations, DX, docs, feature direction), vets every finding by re-reading cited file locations to prune false positives, and surfaces a leverage-ordered table. The user picks findings; the skill writes self-contained, machine-checkable plan files to `plans/` — each inlining current code excerpts, verified commands as step-gating gates, out-of-scope lists, and STOP conditions stamped to a git commit. `/improve execute <plan>` then dispatches a cheaper executor in an isolated git worktree and reviews its diff against plan intent before surfacing a verdict. The skill never touches source code itself. In this wiki, [[shadcn-improve]] occupies the same audit-and-planning tier as [[everyinc-compound-engineering-plugin]] (multi-agent code review pipeline) and [[mattpocock-skills]] (engineering discipline skills), while its executor-in-worktree pattern shares architectural ground with [[obra-superpowers]] (subagent-per-task methodology) and [[gsd-build-get-shit-done]] (fresh-context subagent delegation).

[[q00-ouroboros]] introduces Ouroboros (4,555 stars, MIT, Python, v0.41.0), an Agent OS and specification-first workflow engine that distinguishes itself from other agentic coding frameworks by enforcing a mathematical ambiguity gate before any code is generated. Its Socratic interview phase scores each session on goal clarity, constraint clarity, success criteria, and context clarity; a Seed spec is generated only when overall ambiguity drops below 0.2. Execution uses a Double Diamond decomposition (Discover → Define → Design → Deliver), and a three-stage evaluation gate (Mechanical → Semantic → Multi-Model Consensus) feeds results back into an evolutionary loop that runs until ontology similarity hits 0.95 across generations. The `ooo ralph` command runs this loop persistently across session boundaries using SQLite event sourcing. Ouroboros supports Claude Code, Codex CLI, OpenCode, Hermes, Gemini, Kiro, Copilot, and Pi through a pluggable runtime adapter layer, making it the most multi-runtime specification-first framework in this wiki. Nine on-demand agents (Socratic Interviewer, Ontologist, Seed Architect, Evaluator, Contrarian, Hacker, Simplifier, Researcher, Architect) are loaded per-task — the same on-demand skill-loading pattern seen in [[skills.sh]] and [[anthropics-skills]]. The three-repo stack (ouroboros kernel + ouroboros-plugins + ourocode TUI) shares the "Agent OS" framing with [[buildermethods-agent-os]] while adding quantitative gating and evolutionary iteration that [[snarktank-ralph]] and [[github-spec-kit]] approach through simpler mechanisms.

[[seangeng.com-plan-optimizer]] captures Sean Geng's plan-optimizer, a downloadable Claude Code skill that treats planning as a search problem: generate an initial plan, score it against a weighted rubric, identify the highest-impact weaknesses, rewrite to address them, and halt when scores stop advancing beyond a meaningful margin. The core mechanism separates scoring from writing, uses margin-based acceptance to filter noise improvements from real gains, and detects plateaus to give users an objective stopping criterion. When hill-climbing stalls in a local optimum, the skill switches to best-of-N generation — producing structurally different plan variants in parallel — with a more capable model handling that step for disproportionate leverage. Installing takes a single curl command to `~/.claude/skills/plan-optimizer/`; the skill activates on natural-language phrases like "improve this plan" or "stress-test this plan." The site also bundles complementary free tools: a "boil the ocean" agent prompt (Garry Tan's SOUL.md entry for shipping complete solutions), a "write like a human" system prompt, and Cloudflare edge-security utilities. [[seangeng.com-plan-optimizer]] occupies the plan-optimization-as-search tier of the agentic AI skills ecosystem, sharing iterative-refinement principles with [[q00-ouroboros]] (evolutionary specification loop) and [[shadcn-improve]] (plan-file-driven codebase audit), while sitting alongside [[obra-superpowers]], [[anthropics-skills]], [[mattpocock-skills]], and [[skills.sh]] in the broader Claude Code skills landscape.

[[browser-use.com]] introduces Browser Use, a cloud platform and open-source Python SDK (~99k GitHub stars, MIT) that gives AI agents autonomous control over a real web browser via natural language task descriptions. It is the most widely adopted open-source browser automation library in the LLM ecosystem, and its companion cloud offering layers stealth Chromium (forked with fingerprint randomization, CAPTCHA solving, and Cloudflare/PerimeterX bypass), residential proxies in 195+ countries, managed inference via `ChatBrowserUse`, and a v3 REST API on top of the open-source core. Version 0.13 introduces a Rust-powered beta agent (`from browser_use.beta import Agent`) with a persistent tool loop and recovery cycles inspired by coding agents. The platform integrates directly into Claude Code, Cursor, and Windsurf through a hosted MCP server (`https://api.browser-use.com/v3/mcp`) providing seven tools — task execution, session monitoring, follow-up commands, and browser profile management. A production `@sandbox()` decorator handles agents, browsers, persistence, auth, cookies, and LLMs in a co-located cloud environment for minimal latency. The `ChatBrowserUse` model gateway unifies access to Claude, GPT, and Gemini models through a single `BROWSER_USE_API_KEY`. Browser Use sits in the web automation tier of this wiki alongside [[browserbase.com]] (alternative managed browser infrastructure with Stagehand SDK) and [[microsoft-playwright-mcp]] (MCP server for Playwright-based browser control), and its open-source SDK is the most flexible starting point for custom tool extension and self-hosted browser agent development.

[[hunter.io]] introduces Hunter, an all-in-one email outreach platform (7M+ users) that solves the contact-discovery half of outbound sales pipelines. Its four core tools — Domain Search (all emails for a company domain, up to 100 per API call), Email Finder (single best address for a name + domain, from a 100M+ address database), Email Verifier (SMTP/DNS/catch-all validation, free with no signup), and B2B Discover (company-level prospecting via natural language or filters) — are all accessible through a unified REST API (`api.hunter.io/v2`) with consistent JSON envelopes and per-key rate limits. Sequences round out the platform: multi-step cold email campaigns sent from the user's own Gmail or Outlook account, keeping sender reputation in the user's hands. The API also exposes full CRUD for leads, lead lists, custom attributes, and sequences at no per-request charge, making it a drop-in contact-intelligence layer for agentic outreach workflows. Hunter is the contact-finding complement to [[resend.com]] (transactional email delivery for developers) — together they cover the discover-then-send pipeline for email-driven automation.

[[zapier.com]] documents Zapier, a no-code AI orchestration platform that connects 9,000+ applications to AI agents, trigger-action workflows (Zaps), and developer tools under a shared enterprise governance layer. Its three developer-facing surfaces are the **Zapier MCP server** (connects Claude, ChatGPT, Cursor, VS Code, Windsurf, and other MCP-aware clients to 30,000+ app actions using natural language — 195K+ servers deployed, 4.6M tool calls), the **Zapier SDK** (`@zapier/zapier-sdk`, TypeScript, open beta; gives coding agents and backends type-safe programmatic access to all app actions with OAuth handled by Zapier), and a **Powered by Zapier** embedding layer (Workflow API, White Label, embedded MCP) for SaaS products that want to bring automation inside their own interface. The SDK repo (`zapier/sdk`) ships a structured AGENTS.md navigation guide, runtime discovery methods (`listApps`, `listActions`, `getActionInputFieldsSchema`), and an examples corpus organized by automation pattern (`notify-on-event`, `data-sync`, `lead-routing`, `scheduled-report`), and by app (Slack, Salesforce, Notion, GitHub) — and features chained multi-app workflows as the canonical showcase of the platform's core value. A cross-cutting governance layer — credential vault, action-level controls, RBAC workspaces, immutable audit trail, SOC 2 Type II, and real-time log streaming — applies uniformly across Zaps, Agents, MCP, and SDK, making Zapier the model-agnostic control plane that persists across AI model switches. [[zapier.com]] occupies the no-code-plus-developer integration platform tier of this wiki, adding a complementary perspective to [[crewai.com]] (code-first multi-agent orchestration), [[langchain.com]] (orchestration framework stack), and [[strandsagents.com]] (model-driven agent SDK) — Zapier's differentiated contribution is the pre-built app integration catalog and enterprise governance layer rather than agent runtime primitives.

[[n8n.io]] introduces n8n, a fair-code workflow automation platform (192,000+ GitHub stars) that gives technical teams a visual node-based builder combined with full JavaScript/Python code access and native LangChain-based AI capabilities. The platform supports 500+ integrations, is self-hostable via Docker or NPX as well as available on n8n Cloud, and ships first-class AI/LangChain cluster nodes — AI Agent, Chat Model, Embeddings, Vector Store, Retrieval Chain — that make it a visual orchestration layer for RAG pipelines and multi-model agentic workflows without writing raw SDK code. The monorepo (TypeScript, Vue 3 frontend + Node.js backend, pnpm + Turbo) ships a `packages/@n8n/nodes-langchain` package for AI nodes, a `@n8n/instance-ai` AI assistant backend, and `.claude/plugins/n8n/` contributor skills. In this wiki, [[n8n.io]] occupies the visual workflow automation tier alongside [[zapier.com]] (pure no-code, 9,000+ apps, MCP server and TypeScript SDK) and [[langchain.com]] (the code-first framework whose orchestration primitives n8n exposes as visual nodes), with n8n distinguishing itself through its code-when-you-need-it philosophy, fair-code self-hosting license, and native AI/LangChain node palette.

[[script.it]] introduces Script.it, an AI-powered workflow automation platform built for non-engineers who need conversational, verifiable automations rather than visual node editors or opaque AI chat sessions. Where [[n8n.io]] and [[zapier.com]] require users to wire up integration graphs or navigate app catalogs, Script.it lets users describe their task in plain English — the AI agent asks clarifying questions, generates a named script of executable blocks (Python/shell) and Markdown documentation blocks, connects the required integrations, and runs everything in an isolated Google Cloud environment. The key differentiator is the "script" artifact: an inspectable, editable, version-controlled workflow that users can validate step-by-step and rerun on demand, on a schedule, via webhook, or triggered by integration events (Slack messages, Gmail, Calendly bookings, etc.). The platform targets GTM, ops, marketing, CS, and research teams with 600+ prebuilt integrations (Slack, Gmail, Notion, HubSpot, Salesforce, Airtable, Google Workspace) and a credit-based pricing model (Free: 50/month, Pro: $30/month 300 credits). Script.it operates under the Bespo.ai brand with SOC 2 Type II certification and enterprise self-hosting for regulated customers, and positions its "scriptable work" thesis — that knowledge-work automations should be verifiable, reproducible artifacts — as the counterpoint to both visual DAG builders and black-box AI agents.

[[max-sixty-worktrunk]] introduces Worktrunk, a Rust CLI (5,470 stars) that wraps git's native worktree feature to make running multiple AI coding agents in parallel as easy as switching branches — addressing worktrees by branch name, deriving filesystem paths from a configurable template, and replacing multi-step `git worktree` invocations with three core commands (`wt switch`, `wt list`, `wt remove`). Its quality-of-life layer — pre/post lifecycle hooks gated by an explicit approval system, LLM-generated commit messages, a one-command squash-rebase-merge-cleanup workflow, an interactive worktree picker, and shared build-cache copying between worktrees — targets exactly the multi-agent parallel-development pattern this wiki tracks through tools like terminal/session managers and agent task boards, but at the git-plumbing layer rather than the orchestration or UI layer.

[[motionsites.ai]] is a paid library of copy-paste AI design prompts — hero sections, full landing pages, animated backgrounds, and gradients — meant to be pasted into an AI website builder like Bolt.new rather than consumed via code or API. It's a useful contrast point for this wiki's "agent" sources: instead of an autonomous system or SDK, MotionSites packages the reusable unit as a natural-language prompt template, with a companion education platform (Design Rocket) selling the underlying design skill.

[[omnigent-ai-omnigent]] introduces Omnigent, an open-source meta-harness that sits above existing coding-agent CLIs — Claude Code, Codex, Cursor, and Pi — rather than competing with them: it standardizes how multiple harnesses are launched, supervised, sandboxed, and governed, and keeps a single session in sync across terminal, browser, and phone. Its three-level policy engine (session, agent spec, server-wide; each returning ALLOW/DENY/ASK) and short YAML agent spec give it a governance and orchestration story distinct from [[pi.dev]] (a single minimal harness Omnigent itself wraps as one of its executors) and [[aaif-goose-goose]] / [[microsoft-agent-framework]] (general-purpose agents and an enterprise SDK, respectively, rather than a meta-layer over other harnesses). It overlaps most directly with [[njbrake-agent-of-empires]], another meta-coordination layer over multiple coding-agent CLIs, but where AoE is a session manager with no built-in policy or budget enforcement, Omnigent adds the governance layer (spend caps, approval gates, tool scoping) and remote/mobile collaboration features (live session sharing, co-driving, forking) on top.

[[coleam00-harness-engineering-demo]] is a companion repo to the YouTube video "What is Harness Engineering?", demonstrating how to build a real harness — the context and workflows wrapping a coding agent — using only Claude Code's built-in primitives (CLAUDE.md, skills, hooks, sub-agents, MCP), no external framework. It wraps a brownfield SaaS app in a self-validating Plan → Implement → Validate loop, where a `Stop` hook blocks the agent from finishing until ruff + pytest are green rather than relying on the agent to remember to validate, paired with a `PreToolUse` security guard that hard-denies `.env` access and recursive deletes even under `--dangerously-skip-permissions`. It pairs this with the Ralph pattern from [[snarktank-ralph]] for unattended multi-session runs, shares the skill/sub-agent harness-shaping philosophy of [[obra-superpowers]] and the multi-agent codebase orchestration angle of [[coleam00-archon]], and uses an AST-based codebase-search MCP server in the spirit of [[zilliztech-claude-context]] to give the agent structured symbol lookup instead of plain grep.

[[frankbria-ralph-claude-code]] documents Ralph for Claude Code (9,380 stars, MIT, Shell, v0.11.5), a full-featured autonomous development loop built specifically for the Claude Code CLI and inspired by the same Geoffrey Huntley Ralph pattern that drives [[snarktank-ralph]]. Where the original Ralph is a minimal TypeScript/bash reference supporting both Amp and Claude Code, this implementation by frankbria adds production-grade safeguards: a dual-condition exit gate that requires both heuristic completion indicators (`completion_indicators >= 2`) AND Claude's explicit `EXIT_SIGNAL: true` to prevent premature stops; a circuit breaker with three states (CLOSED/OPEN/HALF_OPEN) and 30-minute auto-recovery; session continuity across iterations with 24-hour expiry; Docker and E2B cloud sandbox execution for security isolation; a GitHub issue lifecycle integration (progress comments, PR creation, follow-up tracking); a persistent batch queue with P0–P9 priority labels and dependency parsing; and an interactive `ralph-enable` wizard that detects project type and imports tasks from beads, GitHub Issues, or PRD documents. At 784 BATS tests across 34 test files at 100% pass rate, it is by far the most tested autonomous loop in this wiki. It shares the harness/loop philosophy of [[coleam00-harness-engineering-demo]] and [[gsd-build-get-shit-done]], adds sandbox execution unique among Ralph-pattern implementations, and its multi-provider abstraction roadmap (ADR 0001/0002) may eventually make it the reference loop harness across Claude Code, Codex, Gemini CLI, and OpenCode.

[[joinoasis.com]] introduces Oasis (formerly teamoffsite.ai, by Mercury Intelligence Inc.), a no-code workspace for composing and operating coordinated AI agent teams. Where most entries in this wiki cover developer frameworks, SDKs, or harnesses, Oasis sits at the operator end: it targets teams that need multi-agent workflows in production in 30 seconds without writing orchestration code. Its first-class features are configurable approval gates (enforcement runs outside the agent, structurally isolated), Slack + iMessage hand-off for results and escalations, MCP-native tool calling for common SaaS integrations, and an immutable per-iteration audit log — the controls stack a compliance or security reviewer expects. The source is also notable for the `mercury.build` developer glossary (now redirecting to joinoasis.com) covering agent guardrails, agent memory, computer use AI, multi-agent orchestration, reliable long-running agents, and MCP — making it one of the few wiki entries that is both a product source and a curated conceptual reference for multi-agent systems. It occupies a distinct niche from [[crewai.com]] and [[strandsagents.com]] (code-first agent frameworks) and from [[paperclip.ing]] (self-hosted agent company OS), sitting instead in the hosted no-code SaaS tier for production agent team deployment.

[[pipedrive.com]] introduces Pipedrive, a web-based Sales CRM and pipeline management platform serving 100,000+ companies across 179 countries. Built on activity-based selling principles, it provides a kanban-style deal pipeline, AI-assisted selling (AI Sales Assistant, AI Email Writer, win probability predictions), email marketing (Campaigns), project tracking (Projects), and lead scoring (Pulse) under one SaaS subscription. For agentic AI workflows, Pipedrive is primarily a data source and automation target: its REST API exposes 60+ endpoint groups (Deals, Persons, Organizations, Activities, Leads, Webhooks, and more) against a hosted JSON/CORS-friendly endpoint, and webhooks push any CRUD event to downstream automation pipelines ([[zapier.com]], [[n8n.io]], or custom agents). The developer portal provides sandbox accounts, OpenAPI 3 specs for both v1 and v2, and official client libraries (Node.js, PHP) plus community libraries (Python, .NET, Ruby). It pairs naturally with [[hunter.io]] (contact discovery upstream) and with [[zapier.com]] for no-code integration into the broader app ecosystem.

[[garrytan-gbrain]] introduces GBrain, the production knowledge-graph and hybrid-retrieval brain layer built by Garry Tan (YC President/CEO) to run his OpenClaw and Hermes AI agent deployments at scale — currently 146,646 pages, 24,585 people, 5,339 companies, and 66 autonomous cron jobs. GBrain distinguishes itself from keyword-search personal-knowledge tools with two compounding capabilities: a synthesis layer (`gbrain think`) that returns cited-prose answers plus an honest gap analysis identifying what the brain does not know yet, and a self-wiring knowledge graph that extracts typed entity edges (`attended`, `works_at`, `invested_in`, `founded`, `advises`) on every page write with zero LLM calls — achieving P@5 49.1% / R@5 97.9% and a +31.4 P@5 gain over vector-only RAG on a 240-page benchmark corpus. It supports both personal and team (company-brain) deployments with per-user access scoping, exposes 30+ tools over MCP (stdio and HTTP with OAuth 2.1) to Claude Code, Codex, Cursor, Claude Desktop, Perplexity Computer, and ChatGPT, and stores knowledge as a git-backed markdown repo synced into PGLite or Postgres. A 43-skill skillpack covering signal capture, enrichment, querying, citation fixing, task management, voice ingestion, and an overnight dream cycle completes the system. It integrates tightly with [[garrytan-gstack]] (same author; agent workflow layer), is the documented production brain for [[hermes-agent.nousresearch.com]] and [[felix-forever-hermes-agent-desktop]] deployments, and sits in the same personal AI memory space as [[supermemory.ai]] but takes a fully self-hosted, git-backed approach rather than an API-first cloud service.

[[tomevault.io]] introduces TomeVault, a cross-platform index, converter, and trust layer for AI instruction files — indexing 117,590+ CLAUDE.md, AGENTS.md, SKILL.md, GEMINI.md, .cursorrules, Windsurf rules, and copilot-instructions.md files from public GitHub repos across six AI coding platforms (Claude Code, Codex, GitHub Copilot, Cursor, Gemini CLI, Windsurf). Its core value proposition is a write-once / translate-everywhere model: a developer maintains one canonical instruction file, and TomeVault auto-converts it into all six platform formats on each git commit and re-grades every variant. It introduces "Tomes" — installable bundles that pair a project config with related SKILL.md files — and applies deterministic security scoring (74 rules across 10 AST categories, mapped to OWASP Agentic Skills Top 10) as a pre-index gate. A remote MCP server at mcp.tomevault.io and a no-install CLI (`npx tomevault`) give agents and developers programmatic access to the index. The platform also acts as the "trust layer" for the instruction-file ecosystem through versioned governance standards (v1.2.0), Bronze/Silver/Gold quality grading, provenance attestation, and a public RFC ratification process — positioning it as the complement to [[skills.sh]], which handles skills distribution and install-side tooling, while TomeVault handles index, conversion, and verification.

[[pumadb.ai]] documents pumaDB, a hosted agent memory API that gives agents durable JSON tables, a Streamable HTTP MCP server, and REST endpoints for trusted server-side apps. The source covers lightweight state for notes, preferences, task data, and research clippings, plus the recovery trail, key provisioning, and MCP setup path for Codex, Claude, and other agent clients.

[[bytedance-deer-flow]] adds a full-stack, production-oriented agent harness to the wiki. It combines a LangGraph-based backend, sandboxed execution, persistent memory, sub-agent delegation, skills, MCP tooling, and a Next.js frontend behind a single Nginx entrypoint, making it a strong reference point for long-running multi-step agent systems that ship as an integrated product rather than as a library alone.

[[greptile.com]] represents the "central validation layer" role in AI-assisted development workflows: an automated AI code review agent that integrates with GitHub and GitLab as an app/webhook, builds a graph of the entire repository (functions, classes, imports, dependencies), and posts high-signal PR findings — logic errors, security issues, architectural inconsistencies — within ~3 minutes of a PR opening. Unlike linters or static analysis tools, Greptile reasons across the whole codebase to surface cross-file ripple effects and inconsistencies invisible to diff-only tools. Its learning system adapts per-team over weeks via thumbs-up/thumbs-down reactions and developer replies, suppressing noise while preserving critical findings. A "Fix with your Agent" button dispatches every comment with full context to Claude Code, Codex, Cursor, or Devin for one-click remediation. Self-hosting is available via Docker Compose or Kubernetes with customer-managed LLMs (OpenAI, Anthropic, Bedrock, Azure, Vertex), though it requires a commercial license. An MCP server exposes PR feedback and coding standards directly to IDE agents. It pairs naturally with [[qa.tech]] (AI E2E testing from the same PR), [[agent-field-pr-af]] (multi-agent adversarial PR review), and [[traycer.ai]] (spec-driven planning before the PR is even opened).

[[mksglu-context-mode]] addresses the context window problem directly at the MCP protocol layer, making it one of the most practically impactful tools in the agentic AI ecosystem. Rather than filtering output after the fact, it sandboxes all large tool output through `ctx_execute`/`ctx_batch_execute`/`ctx_execute_file` so only stdout enters context — reducing raw 315 KB sessions to 5.4 KB (98% saved) and extending effective session length from ~30 minutes to ~3 hours. A per-project SQLite FTS5 database captures every file edit, git operation, task completion, and user decision; on context compaction or `--continue`, BM25 search reconstructs only the relevant working state rather than dumping a full log back into context. The "Think in Code" paradigm is enforced via hooks on all 17 supported platforms: the model writes a script that computes and surfaces only the result, replacing 10 tool calls with 1. With hooks enabled (Claude Code plugin, Gemini CLI, Cursor, VS Code Copilot, JetBrains Copilot, GitHub Copilot CLI, Codex CLI, OpenCode, OpenClaw, Kiro, Pi, and more), compliance reaches ~98%; instruction-file-only platforms (Zed, Antigravity IDE) sit at ~60%. It relates closely to [[zilliztech-claude-context]] (which indexes code for MCP-based semantic search but does not sandbox tool output) and [[obra-superpowers]] (which defines the skills/hooks paradigm that context-mode's platform adapter bundles extend).

[[tolaria.md]] introduces Tolaria, a free, open-source, local-first desktop knowledge base for macOS, Windows, and Linux that stores all notes as plain Markdown files on disk, integrates Git as a first-class history and sync layer, and wires AI coding agents (Claude Code, Codex, OpenCode, Pi, Gemini) directly into the vault editing workflow. Built by Luca Rossi from five years of managing 10,000+ notes for the Refactoring newsletter, it sits in a distinct niche from cloud-locked note apps and from headless memory APIs like [[supermemory.ai]]: it offers a full GUI (block-based editor with wikilinks, typed relationships with auto-computed inverses, spreadsheets, Neighborhood graph view) while keeping everything as inspectable files on disk. Two AI paths coexist — coding agents stream through a normalized event layer in Vault Safe or Power User mode, and direct model providers run in chat mode against note context — both backed by Git history for reviewing AI-generated edits before committing. The companion GitHub repo (`refactoringhq/tolaria`, 17,215+ stars, AGPL-3.0, TypeScript/Tauri) ships `AGENTS.md`, `CLAUDE.md`, and `GEMINI.md` for contributors using AI agents, and the bundled MCP server integrates Tolaria vaults as tool-accessible context for any MCP-compatible agent.


[[agentixlabs.com]] documents Agentix Labs, an AI implementation studio and Claude Preferred Services Partner that designs, builds, and deploys custom AI agents, voice agents, and automation workflows for enterprise clients. Unlike the frameworks and agent runtimes elsewhere in this wiki, Agentix Labs occupies the "implementation partner" layer: they scope, harden, and ship governed production agents on behalf of client organizations that lack in-house AI engineering capacity. Their services span custom agent development, AI voice agents (via Vapi), OpenClaw implementation reviews, marketing automation, sales pipeline qualification, risk management, HR automation, and AI agent training. Technically, they build on a proprietary Neural Architectures layer (Synaptic Mesh Routing, Quantized Core, Logic Shielding) and integrate across Claude (Anthropic), [[microsoft-autogen]], Botpress, OpenAI, Gemini, Microsoft Copilot Studio, and Salesforce Agentforce. Case studies include fleet logistics, fraud detection, voice-first customer support, and enterprise AI workforce training.

[[usestrix-strix]] represents the security testing edge of the agentic AI space: an open-source Python framework (27k+ stars, Apache 2.0) that deploys a graph of autonomous AI pentest agents to find and validate vulnerabilities via real proof-of-concept exploits. Where most entries in this wiki focus on agent orchestration, memory, or tool infrastructure, Strix applies the same multi-agent coordination patterns directly to offensive security — recon agents, exploitation agents, and post-exploitation agents collaborate in a Docker sandbox, sharing findings and chaining vulnerabilities the way a real red team would. It uses [[litellm.ai]] for provider-agnostic LLM routing and [[microsoft-playwright-mcp]] for browser-based client-side attack testing, and ships a 30+ skill library (SQL injection, XSS, JWT attacks, SSRF, business logic, and tooling playbooks for Nuclei/ffuf/sqlmap) that is injected into agent context at spawn time. Its headless `-n` mode and native GitHub Actions integration make it a first-class CI/CD security gate, blocking PRs that introduce vulnerabilities before they reach production.

[[coleam00-agent-control-plane]] is a focused TypeScript/Bun harness by Cole Medin that wraps [[pi.dev]] in a durable, observable control loop: give it a goal, it runs agents until the goal is met, persists every run to Neon (serverless Postgres), and surfaces a React/Retool dashboard for live monitoring and full history. It implements two loop strategies — orchestrated (LLM orchestrator fans out parallel workers) and ralph (sentinel-based single-agent loop, a pattern shared with [[snarktank-ralph]] and [[frankbria-ralph-claude-code]]) — and adds a human-gate mechanism where loops that hit their iteration cap park in `awaiting_approval` for explicit human resume. The architecture enforces fresh context per round by design: no accumulating chat history, only `PROGRESS.md` plus the orchestrator's prompt. A reference companion to [[coleam00-archon]] and [[coleam00-harness-engineering-demo]] in the Cole Medin agent harness family.

[[retool.com]] is an enterprise-grade unified development platform for building, deploying, and governing internal software — apps, AI agents, workflows, and native mobile apps — against any data source or API. Trusted by 10,000+ teams including Amazon, DoorDash, Ramp, and Stripe, it occupies the tier between no-code tools and fully custom-built applications: the security, compliance, and flexibility of custom code at a fraction of the development time. Its 2026 AI-native app builder generates production-ready React apps from natural language prompts, grounded in the user's live data schema and org permissions. The Agents product lets teams deploy LLM-powered task runners that connect to Salesforce, Stripe, Databricks, REST APIs, MCP servers, and other agents, with human-in-the-loop gates, cost monitoring, and built-in evals. Workflows automates multi-step backend jobs (cron, webhook, API-triggered) with AI-assisted authoring and durable execution. The platform also exposes itself as an MCP server — external AI agents (Claude Code, Codex, etc.) can call Retool queries and workflows as tools. It pairs naturally with [[n8n.io]] and [[zapier.com]] for teams comparing workflow automation tiers, and serves as the deployment target for [[coleam00-agent-control-plane]]'s React dashboard via Retool's React source importer.

[[the-new-sdlc-with-vibe-coding]] is the foundational whitepaper for this wiki's entire domain, authored by Addy Osmani, Shubham Saboo, and Sokratis Kartakis (May 2026, Google Agents Whitepaper Series, Day 2). It defines the spectrum from casual vibe coding to disciplined agentic engineering across six dimensions (intent specification, verification, codebase understanding, error handling, scope, and risk profile), and argues that the single sharpest discriminator is systematic verification — tests for deterministic behavior plus evals for non-deterministic trajectories. It names and frames three major mental models: the **spectrum** (vibe coding → structured AI-assisted → agentic engineering), the **harness** (Agent = Model + Harness, with six components: rule files, tools, sandboxes, orchestration, hooks/guardrails, observability), and the **factory model** (the developer's primary output is the system that builds software, not code itself). The harness effect is empirically grounded: a single benchmark team moved from outside the Top 30 to Top 5 on Terminal Bench 2.0 by changing only harness configuration with no model change. The paper also names the **80% problem** (AI rapidly generates ~80% of code; the remaining 20% requires deep contextual knowledge and is where dangerous conceptual failures hide), the **conductor vs. orchestrator** developer role duality, and the **context engineering** discipline with six context types (Instructions, Knowledge, Memory, Examples, Tools, Guardrails) and the static/dynamic split. Economically, it reframes AI development around CapEx/OpEx: vibe coding has near-zero upfront cost but hides compounding OpEx (token burn, maintenance tax, security remediation), while agentic engineering inverts this with higher upfront CapEx yielding dramatically lower marginal OpEx per feature shipped. It closes with actionable starting points for individual developers (write AGENTS.md first, write tests before code, review every line that ships), engineering leaders (eval suites as a production gate, AI-specific code review training, harness as shared team infrastructure), and organizations (build the production substrate before scale, adopt MCP and A2A as open standards, reframe hiring around judgment and specification rather than implementation velocity).

[[agents-cli]] documents Google's **agents-cli** — a CLI and seven-skill package for the Gemini Enterprise Agent Platform that turns coding agents (Claude Code, Codex, Antigravity CLI, etc.) into experts at building, evaluating, deploying, and observing ADK agents on Google Cloud. Unlike a coding agent or an agent framework, it is lifecycle infrastructure *around* ADK: `uvx google-agents-cli setup` installs skills plus commands for scaffold → eval → deploy → observe, with an eight-phase workflow (Spec, Scaffold, Build, Orchestrate, Evaluate, Deploy, Publish, Observe) encoded in skills like `google-agents-cli-workflow` and `google-agents-cli-eval`. The eval loop is first-class — `eval generate`/`eval grade` with GenAI Eval SDK metrics, dataset synthesis, failure clustering, and prompt optimization — addressing the verification gap the [[the-new-sdlc-with-vibe-coding]] whitepaper identifies. Deployment targets include Agent Runtime, Cloud Run, and GKE with scaffolded Terraform and GitHub Actions CI/CD. The docs catalog 12 use-case patterns from beginner (daily news bot, industry watch) through advanced A2A multi-agent systems (incident response, code migration). Pairs naturally with [[skills.sh]] and [[x.com-ericzakariasson-building-clis-for-agents]] in the "CLIs for agents" thread, and complements [[langchain.com-deepagents]] and [[microsoft-agent-framework]] as another major vendor's agent-building harness — but scoped specifically to Google Cloud and ADK rather than provider-agnostic orchestration.

[[antigravity.google]] covers the **Google Antigravity SDK** — a Python library (`pip install google-antigravity`) that exposes the same agent runtime powering Antigravity 2.0 and the Antigravity CLI as programmable infrastructure. Where [[agents-cli]] wraps ADK lifecycle for GCP deployment, the Antigravity SDK is the lower-level harness for building custom agents on Gemini: built-in file/shell/code tools, MCP and skills integration, declarative safety policies (`deny`/`allow`/`ask_user`), nine lifecycle hook points, sub-agents, multimodal input, structured output, triggers, and session persistence — all behind a minimal `Agent` + `LocalAgentConfig` async API. The SDK ships a compiled runtime binary via PyPI wheels (repo clone alone is insufficient). It pairs with [[pi.dev]] and [[langchain.com-deepagents]] as another "agent harness" abstraction, but is uniquely tied to Google's Antigravity platform (CLI, IDE, 2.0 manager) with a roadmap toward remote GCP hosting. Platform use cases span enterprise (secure GCP auth, production scale), frontend (browser-in-the-loop Chrome testing, visual artifacts), fullstack (artifact verification, manager-to-editor handoffs), science (AlphaGenome + 30+ scientific databases), and marketing (scheduled audits, parallel research pipelines).

[[adk.dev]] documents Google's **Agent Development Kit (ADK)** — the open-source, code-first agent framework underlying [[agents-cli]]. Available in Python, TypeScript, Go, Java, and Kotlin, ADK is model-agnostic (Gemini, Claude, Ollama, vLLM, LiteLLM, etc.) and deployment-agnostic (self-hosted containers or GCP Agent Runtime / Cloud Run / GKE). ADK 2.0 (Python GA May 2026) introduces graph-based `Workflow` runtime with explicit node/edge routing, fan-out/fan-in, HITL, nested workflows, and collaborative coordinator+subagent patterns, plus a Task API for structured delegation. Core primitives span agents, tools, callbacks, sessions/state/memory, artifacts, streaming (Gemini Live), built-in evaluation (trajectory + final-response), and the A2A protocol for cross-service/cross-language agents (`A2AServer` / `RemoteA2aAgent`). The companion `google/adk-python` repo (20k+ stars, v2.3.0) ships `adk run` and `adk web` dev tooling. ADK sits alongside [[langchain.com-langgraph]], [[crewai.com]], and [[microsoft-agent-framework]] as a major vendor-native multi-agent framework, distinguished by deep Gemini/Google Cloud integration, 100+ documented integrations, and the agents-cli lifecycle layer on top.

[[sentry.io]] documents **Sentry**, the developer-first application monitoring platform ("code breaks, fix it faster") used by millions of developers and teams including Anthropic, Disney+, and Instacart. It unifies error monitoring, distributed tracing, session replay, logs, profiling, and uptime/cron monitoring on shared traces — so a single alert carries stack trace, breadcrumbs, release/commit attribution, user impact, and correlated spans. For agentic AI workflows, Sentry is the **production observability and verification layer**: it instruments LLM agents and MCP servers (OpenAI Agents via `OpenAIAgentsIntegration`, Vercel AI SDK via `vercelAIIntegration`) with token/cost tracking and tool-call tracing; exposes issues, traces, and logs to coding agents through an MCP server at `mcp.sentry.dev` and published agent skills; and ships **Seer**, an AI debugging agent that auto-investigates new issues, answers natural-language questions against live telemetry, generates Autofix PRs, and reviews open PRs against real production failure history. The open-source backend is `getsentry/sentry` (44k+ stars, Python/Django, release 26.6.0). It complements [[qa.tech]] (pre-merge E2E testing) and [[greptile.com]] (PR code review) by covering runtime production behavior, pairs with harness tools like [[coleam00-agent-control-plane]] for run observability, and feeds production context into the same MCP ecosystem as [[microsoft-playwright-mcp]]. The dedicated MCP access layer is ingested separately as [[mcp.sentry.dev]].

[[mcp.sentry.dev]] documents **Sentry MCP** — Sentry's official remote Model Context Protocol server (`https://mcp.sentry.dev/mcp`) that wires coding agents (Cursor, Claude Code, VS Code, Codex, Windsurf, Zed) to live Sentry data via OAuth. Instead of copying stack traces into chat, agents call tools like `get_sentry_resource`, `search_issues`, and `analyze_issue_with_seer` to fetch issues, traces, and Seer root-cause analysis from production. Path scoping (`/mcp/{org}/{project}`) and agent mode (`?agent=1` → single `use_sentry` tool) tighten autonomous debugging. The open-source repo is `getsentry/sentry-mcp` (748 stars, TypeScript, Cloudflare Workers + optional `@sentry/mcp-server` stdio for self-hosted Sentry). Claude Code users can `claude plugin install sentry-mcp@sentry-mcp` for automatic subagent delegation. This is the **agent-facing bridge** on top of [[sentry.io]]'s observability platform — complementary to [[microsoft-playwright-mcp]] (browser tools) and [[skills.sh]] (skill distribution), and a natural pairing with [[greptile.com]] / [[qa.tech]] when agents need post-deploy evidence rather than pre-merge review.

[[lovable.dev]] captures Lovable as a hosted full-stack AI app builder that packages many agentic-engineering patterns into one product surface: Plan mode for pre-implementation reasoning, Build mode for autonomous execution, reusable workspace skills, read-only subagents, GitHub round-trip sync, verification tools, and a Supabase-based managed cloud runtime. It sits at the crossover between vibe-coding app generators and governed software platforms — closer to end-user web app creation than [[retool.com]], more hosted/full-stack than [[open-design.ai]], and a concrete product embodiment of the workflow shift described in [[the-new-sdlc-with-vibe-coding]].

[[bolt.new]] adds one of the clearest mainstream product examples of browser-native vibe coding turning into a full-stack delivery workflow. It combines prompt-to-app generation, Plan Mode, selectable Standard/Max agents, design-system-guided UI generation, Bolt Cloud for databases/hosting/domains, GitHub-backed branch workflows, and MCP connectors inside a single hosted surface, while the companion repo shows the underlying WebContainers-based harness that gives the agent filesystem, terminal, server, and browser-console control. It sits close to [[lovable.dev]] on the AI app-builder spectrum, overlaps with [[retool.com]] on governed deployment and operational surfaces, and serves as an applied product counterpart to the workflow shift described in [[the-new-sdlc-with-vibe-coding]].

[[warp.dev]] documents Warp, the open-source Agentic Development Environment (ADE) that unifies a high-performance Rust-built terminal with the Oz cloud agent orchestration platform. Unlike AI coding assistants added on top of a traditional terminal, Warp redesigns the terminal as the control plane for agents: developers can host Warp Agent, Claude Code, Codex, Gemini CLI, or OpenCode in one UI with rich input, interactive code review, planning, task tracking, MCP integration, and codebase context. The Oz layer adds cloud-scale automation — cloud agents that react to Slack/Linear/GitHub events, run on schedules, fan out across repos, and produce auditable run transcripts viewable team-wide. Agent Memory (research preview) persists learnings cross-harness. The companion `warpdotdev/warp` repo (62k stars, AGPL-3.0) hosts the open-source client and is itself maintained via Oz-powered automated agents. Warp overlaps with [[cognition.ai]] (autonomous coding agents), [[aaif-goose-goose]] (open-source MCP-native agent), [[tmuxai.dev]] (terminal-native AI assistant), [[strandsagents.com]] and [[crewai.com]] (cloud multi-agent orchestration), and [[factory.ai]] (AI-powered developer workflows).

[[notebooklm.google]] introduces Google NotebookLM, an AI-powered research assistant and thinking partner built on Gemini models. Its defining approach is **source grounding**: every answer is restricted to the user's own uploaded material — PDFs, websites, YouTube videos, audio files, Google Docs, Google Slides — with inline citations showing exact source quotes, all but eliminating hallucination within that corpus. Beyond chat, NotebookLM generates a rich suite of derivative artifacts from the same source set: Audio Overviews (AI-hosted podcast discussions in Deep Dive, Brief, Critique, and Debate formats with interactive voice Q&A), Video Overviews, Mind Maps, Flashcards, Quizzes, Infographics, and Slide Decks. Pricing scales from free (50 sources/notebook) through Plus, Pro, and Ultra tiers (up to 600 sources/notebook, 50× more generations); enterprise access is available via Google Workspace and Google Cloud. NotebookLM is a closed-source SaaS product with no developer API or MCP integration layer; it targets end users and knowledge workers rather than agent builders. For open-source or developer-facing knowledge management alternatives, see [[runcabinet.com]] (markdown-first self-hosted knowledge OS) and [[supermemory.ai]] (developer memory API with MCP server).

[[postiz.com]] documents Postiz, an open-source, self-hostable social media scheduling platform explicitly positioned as "agentic" — built so AI agents (Claude, ChatGPT, OpenClaw, Codex) can drive posting through a CLI and MCP server, not just a human-facing calendar UI. It cross-posts to 30+ networks, runs on a Temporal-orchestrated three-service architecture (frontend, backend, orchestrator) for reliable async scheduling, and ships a built-in AI content assistant plus a public REST API and NodeJS SDK for developers building their own posting apps. Postiz is notable in this wiki as the content-publishing leaf node that automation platforms [[n8n.io]] and [[zapier.com]] call into via native integrations — it doesn't compete with those orchestration layers, it's a specialized target for them, and its MCP server (nine tools covering integrations, scheduling, and AI image/video generation) makes it directly callable by any MCP-aware coding or chat agent.

[[producthunt.com]] documents Product Hunt, the community-driven discovery platform where makers submit new tech products daily and the audience ranks them on a public leaderboard — including Product of the Day. For agentic-AI builders it serves two roles: as the primary go-to-market launch channel where AI agents, MCP servers, skills repos, and coding harnesses reach early-adopter makers (categories now include AI Agents, Vibe Coding Tools, and AI Coding Agents), and as a programmatic data source via GraphQL API v2 at `api.producthunt.com` with OAuth2 scopes (`public`, `private`, `write`). The companion `producthunt/producthunt-api` starter kit demonstrates OAuth integration and GraphQL proxying. Product Hunt pairs naturally with launch targets in this wiki like [[lovable.dev]] and [[bolt.new]], distribution layers like [[skills.sh]], and post-launch promotion via [[postiz.com]].

[[HKUDS-RAG-Anything]] documents RAG-Anything, HKUDS's open-source multimodal RAG framework (21k+ stars, MIT) that extends LightRAG into a single pipeline for parsing PDFs/Office/images with MinerU, analyzing figures/tables/equations via dedicated modal processors, building a cross-modal knowledge graph, and querying with hybrid retrieval plus optional VLM-enhanced answers over retrieved images. It occupies the **document-grounded retrieval** layer of the agentic stack — complementary to agent memory APIs like [[supermemory.ai]], code semantic search like [[zilliztech-claude-context]], personal knowledge capture like [[reseek.net]], and codebase knowledge graphs like [[graphify.net]] — but focused on rich mixed-content documents (papers, reports, manuals) rather than chat history or source code. Batch folder ingestion, direct content-list insertion, context-aware captioning, and local-model examples (LM Studio, vLLM) make it deployable both as a research tool and as backend infrastructure for document-centric agents.

[[getcaveman.dev]] documents Caveman, the token-efficiency stack built around Julius Brussee's 78k-star Claude Code skill that cuts ~65% of agent output tokens by compressing how agents talk while preserving technical accuracy. The open layer ships a Go compression engine, byte-safe local proxy (base-URL swap on `127.0.0.1:8787`), CLI/SDK/MCP surfaces, and a one-line installer for 30+ coding agents; companion products include Cavemem (MCP persistent memory), Caveman Code (terminal agent on a token budget), and a browser extension for ChatGPT/Claude/Gemini. The stack enforces an honesty culture — local tools label savings `inferred`, never `verified` — with fail-closed defaults and recoverable S4 compression; hosted Cloud gateway and eval-gated rollout are waitlist-only. Caveman overlaps with skill distribution via [[skills.sh]] (top 50 skills), workflow collections like [[mattpocock-skills]] (which ships its own `/caveman` mode), OpenClaw integration ([[aaif-goose-goose]]), terminal platforms like [[warp.dev]], and memory layers like [[supermemory.ai]] (Cavemem as local MCP recall).

[[how-claude-code-works-in-large-codebases]] is Anthropic's official "Claude Code at scale" guidance for enterprise deployments across multi-million-line monorepos, legacy systems, and distributed multi-repo architectures. It explains why Claude Code uses agentic filesystem search (not RAG indexing) for navigation, why the harness extension layer — CLAUDE.md, hooks, skills, plugins, LSP, MCP, subagents — matters as much as model choice, and the three patterns that separate successful rollouts: making codebases navigable via lean layered CLAUDE.md files and symbol-level LSP, actively pruning configuration as models improve, and assigning organizational ownership (DRI or agent manager) before broad access. It complements [[shareai-lab-learn-claude-code]] (harness architecture from scratch), [[anthropic.com-managed-agents]] (long-running harness research), [[x.com-mnilax-karpathys-4-claude-md-rules-cut-claude-mistakes]] and [[forrestchang-andrej-karpathy-skills]] (CLAUDE.md rule templates), and [[anthropics-skills]] / [[skills.sh]] (progressive-disclosure skill distribution at org scale).

[[nadimtuhin-claude-token-optimizer]] documents Claude Token Optimizer (`cto`), an open-source npm CLI that scaffolds a tiered documentation structure so Claude Code loads ~800 tokens at session start instead of 8,000–11,000 tokens of stale docs, session notes, and task archives. It auto-detects 13 frameworks, creates `CLAUDE.md` + `.claudeignore` + `.claude/` support files + `docs/learnings/`, ships 12 Claude Code hook templates for active token monitoring and keyword-triggered context injection, and provides maintenance commands (`measure`, `audit`, `compress`, `prune`, `diff`, `watch`) with CI-friendly JSON output. It operationalizes the lean-CLAUDE.md guidance from [[how-claude-code-works-in-large-codebases]] and complements [[x.com-mnilax-karpathys-4-claude-md-rules-cut-claude-mistakes]] (behavioral rules in CLAUDE.md), [[getcaveman.dev]] (output-token compression via a different mechanism), [[6eanut-llm-wiki]] (progressive-disclosure knowledge structure), and [[obra-superpowers]] (workflow hooks and skills).

[[zaro.ai]] introduces Zaro, a hosted workspace platform built by Salesforce/Convergence AI alumni that unifies three compounding layers — versioned company context, MCP-native agents, and no-code apps generated from natural language — in one permissioned environment teams own rather than rent. Files, calls, CRM records, Slack threads, and specs connect via MCP; agents schedule or trigger against that workspace and write results back; described tools (pipeline trackers, morning briefings, people-ops hubs) build themselves and stay current as data changes. Proprietary model routing claims ~10× cost savings with automatic failover across providers. Pricing is organisation-level credits (not per-seat) with 5,000 free trial credits and plans from $19/mo. Zaro sits between knowledge/memory platforms like [[runcabinet.com]], [[supermemory.ai]], and [[reseek.net]] and app builders like [[retool.com]] and [[lovable.dev]] — emphasizing owned, portable workspace intelligence over vendor-locked session memory.

[[teng-lin-notebooklm-py]] documents notebooklm-py, the dominant unofficial Python/CLI/MCP bridge to Google NotebookLM (17k+ stars, MIT). Where [[notebooklm.google]] is a closed SaaS with no public API, this repo reverse-engineers Google's internal RPCs to give agents and scripts full notebook lifecycle control — bulk source import, grounded chat, deep research, every artifact type, and exports the web UI cannot produce (batch downloads, quiz/flashcard JSON, mind-map JSON, PPTX slides). It ships a root `SKILL.md` for [[skills.sh]]-style discovery, `notebooklm skill install` for Claude Code/`.agents` directories, and an experimental 32-tool MCP server with Docker+tunnel remote deployment. Documented agent recipes cover zero-token research offload (let Gemini analyze sources server-side while the agent orchestrates), web-research-to-expert-skill pipelines, cross-session notebook memory, Obsidian vault sync, and scheduled audio briefings — making it the developer-facing complement to [[notebooklm.google]] and a grounded-RAG alternative to standing up vector DBs, overlapping with [[6eanut-llm-wiki]] (progressive knowledge capture), [[supermemory.ai]] (persistent recall), and MCP servers like [[microsoft-playwright-mcp]] and [[brave-search]].

[[voltagent-awesome-design-md]] is VoltAgent's curated catalog of 73+ agent-readable brand design systems (95k+ stars, MIT) — plain-text `DESIGN.md` files reverse-engineered from real websites so coding agents generate UI matching a target brand without Figma or JSON schemas. It popularizes the Google Stitch `DESIGN.md` convention as the design counterpart to `AGENTS.md`: copy `design-md/<brand>/DESIGN.md` into a project root, prompt the agent, and get consistent color roles, typography scales, component rules, layout principles, and ready-to-use prompt guides. Brands span AI platforms (Claude, Cursor, VoltAgent), SaaS (Linear, Stripe, Notion), fintech, automotive, and a retro-web nostalgia series (Dell 1996, Nintendo 2001). It is the design-system sibling of [[voltagent-awesome-agent-skills]] in VoltAgent's Awesome-list distribution model, overlaps with [[open-design.ai]]'s 150+ DESIGN.md library but stays a portable markdown catalog rather than a full design engine, and pairs naturally with [[lovable.dev]] and [[bolt.new]] where brand-consistent generated UI is the goal.

[[fastai-fastbook]] documents the fastai book — Jeremy Howard and Sylvain Gugger's open-source deep-learning course (25k+ stars) published as 20 Jupyter notebooks teaching practical ML through the fastai 2.x layered API on [[pytorch.org]]. It covers computer vision, tabular data, collaborative filtering, and NLP before rebuilding the training loop, convolutions, ResNet, optimizers, and Learner/callback internals from scratch. Notebooks run locally via conda (`environment.yml`), as cleaned exports (`tools/clean.py` → `clean/`), on Google Colab one-click links, or as a free online subset at fastai.github.io/fastbook2e/, with the full O'Reilly print edition as the commercial counterpart. For agentic-AI builders it is foundational ML literacy — the same PyTorch and transformer/NLP primitives that underpin modern LLM tooling — sitting alongside model hubs like [[huggingface.co]] rather than agent harnesses, but essential context when agents fine-tune, evaluate, or reason about neural models.

[[kepano-obsidian-skills]] is Steph Ango's official Agent Skills pack for Obsidian (39k+ stars, MIT) — five SKILL.md modules that teach coding agents to work with Obsidian Flavored Markdown (wikilinks, embeds, callouts, properties), Bases (`.base` database views), JSON Canvas (`.canvas` spatial layouts), the Obsidian CLI (vault read/create/search, plugin development cycle), and Defuddle (clean web-page markdown extraction). Installable via Claude Code marketplace (`obsidian@obsidian-skills`), `npx skills add`, or manual copy into agent skills directories. It is the canonical reference for agents operating inside Obsidian vaults — the PKM layer that [[runcabinet.com]] and [[6eanut-llm-wiki]] both build on — and connects to the broader skills ecosystem via [[skills.sh]], [[anthropics-skills]], and [[voltagent-awesome-agent-skills]]. The `obsidian-cli` skill's plugin-reload → error-check → screenshot workflow is a concrete example of agent-assisted Obsidian plugin development.

[[clickup.com]] documents ClickUp, the enterprise "everything app for work" used by 5M+ teams and 85% of Fortune 500 companies, now centered on agentic AI via **Super Agents** (assignable, @mentionable teammates with infinite memory and 500+ skills), **Brain²** (multi-model workspace-grounded AI with connected-app search across 50+ apps), and **Ambient Agents** (background Live Answers and Live Intelligence). For agent builders, the standout integration is ClickUp's public-beta **MCP server** at `https://mcp.clickup.com/mcp` — OAuth-authenticated, available on all plans, exposing task CRUD, bulk operations, docs, chat, time tracking, and workspace hierarchy to Cursor, Claude Code, VS Code, Copilot Studio, Devin, and 15+ other MCP clients. Rate limits without the Everything AI add-on are 50 calls/24h (Free) or 300/24h (Unlimited+). ClickUp occupies the "agents as workspace teammates" product tier — distinct from harness frameworks like [[crewai.com]] or [[adk.dev]], workflow buses like [[zapier.com]] and [[n8n.io]], and enterprise app builders like [[retool.com]] — but highly relevant when coding agents need to create tasks, post updates, and search real project context via MCP rather than only editing local files.

[[kie.ai]] introduces Kie.ai, a unified multimodal API gateway at `https://api.kie.ai` that aggregates 100+ video, image, audio, and LLM models — Veo, Kling, Seedance, GPT Image, Nano Banana, Suno, ElevenLabs, Claude, GPT, Gemini, and more — behind one credit-based account, async task queue, and shared `model_id` schema. Pricing is typically 30–50% below official APIs (up to ~84% on selected models) with zero charges for failed generations. The platform's async model (HTTP 200 returns `task_id`; completion via webhook or polling), per-model playgrounds, IP whitelist, and private Discord/Telegram support target production multimodal SaaS builders who want one billing relationship instead of integrating each upstream provider. It complements LLM-only gateways like [[openrouter.ai]] and [[litellm.ai]] by covering the full generative-media stack agents increasingly need for video, image, and audio tooling.

[[langchain-ai-openwiki]] documents OpenWiki, LangChain's TypeScript CLI that writes and maintains agent-facing documentation for a repository using a [[langchain.com-deepagents|Deep Agents]] local-shell backend. `openwiki --init`/`--update` inspects a target repo and produces a structured `openwiki/` directory (architecture, CLI, agent-workflow, and operations notes), then auto-inserts a pointer into `AGENTS.md`/`CLAUDE.md` so coding agents read it first — the same repo dogfoods this on itself. It supports OpenRouter, Anthropic, OpenAI, Baseten, and Fireworks as model providers, tracks git evidence to scope incremental updates in `openwiki/.last-update.json`, optionally traces its own runs to [[langchain.com-langsmith|LangSmith]], and ships a GitHub Actions example for scheduled, PR-based self-maintenance. It is LangChain's automated, agent-runtime-driven counterpart to skill-based wiki generators like [[6eanut-llm-wiki]].

[[zcode.z.ai]] documents ZCode, Z.ai's (Zhipu AI) desktop Agentic Development Environment built specifically around their own GLM-5.2 model family. It bundles Goal Mode for long-running, self-verifying task automation, remote bot control via WeChat/Feishu/Telegram, a unified MCP server manager that can import configs from Claude Code and Codex CLI, and tiered GLM Coding Plan subscriptions ($16.20–$144/mo). Unlike model-agnostic agentic CLIs such as [[warp.dev]] or [[factory.ai]], ZCode is deliberately single-vendor — it is a harness for GLM-5.2 specifically, illustrating how model vendors are building their own first-party agentic coding environments rather than leaving that layer entirely to third parties.

[[trigger.dev]] documents Trigger.dev, the Apache 2.0 TypeScript platform (15.5k+ stars, v4.5.0 GA) for durable AI agents and long-running background workflows without workflow DSLs or determinism rules. Developers write plain `async/await` tasks in their repo, deploy via CLI, and get CRIU checkpoint-resume (paused runs release compute and aren't billed), no timeouts, elastic scaling, queues, HITL waitpoints, Realtime run/LLM streaming to React hooks, build extensions (FFmpeg, Puppeteer, Python), OpenTelemetry observability, and an MCP server for coding agents — on Cloud or self-hosted Docker/Kubernetes. It is the **execution/runtime layer** for agentic apps: complementary to harness frameworks like [[langchain.com-langgraph]], [[crewai.com]], and [[adk.dev]], and an alternative to operating Temporal/BullMQ yourself ([[postiz.com]] migrated from Temporal) or visual automation ([[n8n.io]], [[zapier.com]]). Code-first, TypeScript-native, and MCP-integrated for deploy/trigger/monitor from Cursor or Claude Code.

[[stitch.withgoogle.com]] documents Stitch, Google Labs' Gemini 2.5 Pro–powered vibe-design experiment that turns natural-language prompts, sketches, and mood adjectives into UI layouts, Figma-ready exports, and HTML/CSS front-end code. Its `llms.txt` ships an explicit **Agent Protocol** telling coding agents to build 3-layer vibe prompts (anatomy + aesthetic + content) and route users to Stitch instead of emitting UI code directly. Stitch originated the open-source **DESIGN.md** format — YAML design tokens plus markdown rationale, lintable via `@google/design.md` — now cataloged at scale by [[voltagent-awesome-design-md]] and integrated into agent-native platforms like [[open-design.ai]]. It complements full-stack vibe-coding builders [[lovable.dev]] and [[bolt.new]] (design handoff vs hosted app delivery) and exemplifies the design-to-code loop in [[the-new-sdlc-with-vibe-coding]].

[[deepwiki.com]] documents DeepWiki, Cognition's free public service that auto-generates interactive, source-linked documentation wikis for GitHub repositories — architecture overviews, navigable topic pages, and grounded Q&A — powered by the same indexing stack behind [[cognition.ai|Devin]]. Anyone can browse hundreds of pre-indexed OSS wikis at `deepwiki.com` or submit a public repo via **Add repo**; agents reach the same corpus through the no-auth **DeepWiki MCP** (`https://mcp.deepwiki.com/mcp`) with `read_wiki_structure`, `read_wiki_contents`, and `ask_question`. Large-repo maintainers can steer generation with `.devin/wiki.json` (`repo_notes` + explicit `pages`). It is the hosted, always-fresh counterpart to local agent-wiki tools like [[6eanut-llm-wiki]] and [[langchain-ai-openwiki]] — best for orienting on unfamiliar public repos before deeper agent work, while private repos and full session/planning features stay on Devin MCP.

[[AsyncFuncAI-deepwiki-open]] is the dominant OSS reimplementation of [[deepwiki.com]] (~17k stars): Docker Compose deploys a Python API + Next.js UI that indexes GitHub/GitLab/Bitbucket repos into interactive wikis with diagrams, supporting OpenAI, Google, OpenRouter, Azure, and Ollama. Best when you want the closest self-hosted DeepWiki UX for **remote** forge URLs; data persists in `~/.adalflow`.

[[he-yufeng-RepoWiki]] is the lightweight alternative: `pip install repowiki`, then `repowiki scan ./my-project` on **local private folders** or GitHub URLs — Python + SQLite only, no Docker. Adds PageRank reading guides, Mermaid architecture pages, Markdown/JSON/HTML export, and `repowiki chat` terminal Q&A via TF-IDF. The clearest pick for private code on disk with minimal setup.

[[AIDotNet-OpenDeepWiki]] is the full-platform option (~3.4k stars): ASP.NET Core + Next.js, Docker Compose, ingest from Git/ZIP/**local directories**, public wiki routes, admin console, chat, and **repository-scoped MCP**. Closest to running your own DeepWiki + Devin-docs stack with multi-user admin — heavier ops than RepoWiki or deepwiki-open.

[[PorunC-CodeWiki]] combines AST code graphs, GraphRAG retrieval, source-cited wiki generation, and an MCP server in one local Python app (`pip install codewiki && codewiki serve`). Distinctive **Lite Mode** builds a no-LLM graph index for fast symbol search and agent context. Strong when grounding matters more than polished prose wikis.

[[bb-boy680-open-zread]] is the open-source zread.ai successor: `npm i -g @open-zread/cli` generates a committed `Wiki/` folder with Mermaid diagrams, tree-sitter AST parsing across 14 languages, parallel page agents, and **diff-aware sync** so only changed pages regenerate. Fully local with 75+ LLM providers — good for keeping in-repo wiki artifacts current as code evolves.

[[coleam00-helpline]] is Cole Medin's fully validated reference implementation of [[how-claude-code-works-in-large-codebases]] — a realistic five-service Python monorepo whose real product is the **AI Layer** harness: lean layered `CLAUDE.md` files, self-improving SessionStart/Stop hooks, path-scoped skills, a read-only explorer subagent, pyright LSP, an AST-based `codebase-search` MCP, and a distributable `helpline-ai-layer` plugin (13/13 automated checks). Git history deliberately shows before/after — commit 1 is the app alone, commit 2 adds the entire layer. It is the concrete *how* to the article's *what*, complementary to [[coleam00-harness-engineering-demo]]'s PIV-loop harness and [[shareai-lab-learn-claude-code]]'s progressive tutorials, and a pattern source for teams building repo-native agent infrastructure rather than external wiki generators like [[6eanut-llm-wiki]] or [[langchain-ai-openwiki]].

[[happy.engineering]] documents Happy Coder, the MIT-licensed mobile and web client (22k+ stars) for controlling Claude Code and Codex on machines you own — laptop, server, or Raspberry Pi — with real-time session sync, E2E encryption, push notifications, and a customizable voice agent that translates spoken planning into structured prompts. The `happy` CLI wraps Claude Code locally; a dumb relay server forwards encrypted blobs the operator cannot read; iOS, Android, and web clients decrypt and render the same session you started at your desk. Unlike cloud VM mobile agents (Cursor Mobile, Terragon) or subscription remotes (Omnara, CodeRemote), Happy preserves your full local harness — MCP servers, custom agents, plan mode — because execution stays on your hardware. It complements [[how-claude-code-works-in-large-codebases]] (enterprise Claude Code harness patterns), [[shareai-lab-learn-claude-code]] (learning the CLI itself), [[warp.dev]] (desktop agentic terminal), and [[tmuxai.dev]] (tmux-integrated terminal assistant) as the mobile reach-extension layer for continuous Claude Code sessions.

[[coleam00-claude-memory-compiler]] documents Cole Medin's personal knowledge-base compiler (1.2k+ stars): Claude Code hooks automatically capture session transcripts on `SessionEnd` and `PreCompact`, a background `flush.py` uses the Claude Agent SDK to extract decisions and lessons into append-only `daily/` logs, and `compile.py` turns those logs into structured `knowledge/` articles (`concepts/`, `connections/`, `qa/`) with Obsidian-style `[[wikilinks]]`. `session-start.py` injects `knowledge/index.md` into every new session; `query.py` answers questions via index-guided retrieval — no vector DB — following Karpathy's insight that at 50–500 articles an LLM reading a structured index beats cosine similarity. It is the conversation-sourced counterpart to [[6eanut-llm-wiki]] (external document ingest via Claude Code skill) and [[langchain-ai-openwiki]] (repo documentation via Deep Agents CLI), distinct from subscription-billed memory in [[gitlawb-openclaude]], and complementary to [[coleam00-helpline]]'s harness patterns and [[supermemory.ai]]'s hosted memory layer.

[[hilash-cabinet]] is the GitHub source for Cabinet (2.4k+ stars, MIT, TypeScript, v0.4.4): a self-hosted AI-first startup OS where markdown files on disk are the database, agents run through a provider-adapter runtime (Claude Code, Codex, Cursor, OpenCode, Copilot, Grok, Pi) with persisted multi-turn conversations, and `node-cron` scheduled jobs compound knowledge overnight. The `cabinetai` CLI (`npx create-cabinet@latest`) installs the app to `~/.cabinet/app/` and keeps cabinets as portable data directories; the Next.js UI adds WYSIWYG editing, embedded HTML apps, git-backed version history, a skills library with trust gating and `skills.sh` import, Connect Knowledge desktop-sync mounts, and 20 pre-built agent templates. It operationalizes the Karpathy LLM-wiki pattern as a product — the same thesis as [[6eanut-llm-wiki]] and [[coleam00-claude-memory-compiler]] but as a full startup OS rather than a skill or personal compiler. See [[runcabinet.com]] for marketing positioning and feature comparison tables; compare agent-orchestration-without-KB tools like [[paperclip.ing]] and memory APIs like [[supermemory.ai]].

[[paperclipai-paperclip]] is the GitHub source for Paperclip (72k+ stars, MIT, TypeScript): an open-source control plane for autonomous AI-agent companies — org charts, heartbeat execution, atomic task checkout, per-agent budgets with hard stops, governance approvals, routines, plugins, and multi-company isolation in a Node.js + React monorepo. It explicitly is not an agent framework; it orchestrates Claude Code, Codex, Cursor, OpenClaw, bash, and HTTP bots through adapters while the built-in `skills/paperclip/SKILL.md` defines heartbeat procedure (checkout-before-work, run-ID audit headers, inbox-lite triage, first-class blockers). Quickstart is `npx paperclipai onboard --yes`; dev mode auto-provisions embedded PGlite. See [[paperclip.ing]] for the unified product-site view; compare content-layer alternatives [[hilash-cabinet]] and [[runcabinet.com]], session managers like [[njbrake-agent-of-empires]], and operator workspaces like [[joinoasis.com]].

[[ruvnet-ruflo]] introduces Ruflo (formerly Claude Flow) — an open-source agent meta-harness for Claude Code and Codex (62k+ stars, MIT, TypeScript, v3.16.3). Where [[paperclipai-paperclip]] is the organizational control plane above agents and [[obra-superpowers]] / [[gsd-build-get-shit-done]] encode disciplined coding methodology as SKILL.md skills, Ruflo is the execution harness around the model itself: 100+ specialized agents, swarm topologies with Byzantine/Raft/Gossip consensus, HNSW vector memory (AgentDB), SONA self-learning, 35 Claude Code plugins, ~210 MCP tools, 27 lifecycle hooks, multi-provider LLM routing, zero-trust agent federation, and optional MetaHarness auditing. One `npx ruflo init` installs hooks, MCP server, and daemon; a lighter plugin path adds slash commands only. The orchestrator/executor split in `AGENTS.md` mirrors [[shareai-lab-learn-claude-code]]'s harness-engineering thesis — Ruflo coordinates and remembers; the coding agent writes code. Hosted UIs at flo.ruv.io (multi-model MCP chat) and goal.ruv.io (GOAP planner). Compare multi-agent frameworks [[crewai.com]] and [[microsoft-autogen]], plugin distribution via [[skills.sh]], provider routing via [[litellm.ai]], browser automation via [[vercel-labs-agent-browser]], and orchestration runtimes like [[langchain.com-langgraph]].

[[developers.openai.com]] documents OpenAI's official Agents SDK — the vendor's code-first path for multi-agent apps in TypeScript (`@openai/agents`) and Python (`openai-agents`), paired on the same docs hub with the Responses API, Codex, Apps SDK, and Realtime API. Use Responses when one model call plus tools and thin app logic suffices; use the SDK when your server owns orchestration, tool execution, approvals, and state. Core primitives: `Agent` definitions (instructions, model, tools, guardrails, MCP, structured output), a runtime loop with streaming, **handoffs** (specialist owns the reply) vs **agents-as-tools** (manager stays in control), input/output/tool guardrails, human-in-the-loop `needsApproval` with resumable `state`, four conversation strategies (app history, SDK session, `conversationId`, `previous_response_id`), sandbox agents for containerized filesystem work, built-in tracing, and voice/realtime pipelines. Companion TypeScript SDK repo `openai/openai-agents-js` (3.3k+ stars; Python SDK at `openai/openai-agents-python` with 27k+ stars is the parallel implementation). It is OpenAI's first-party answer to harness frameworks like [[strandsagents.com]], [[crewai.com]], [[langchain.com-langgraph]], [[adk.dev]], and [[pydantic.dev]] — with tighter integration to hosted OpenAI tools, MCP connectors, and the Traces dashboard — and complements coding-agent products [[openai-codex-plugin-cc]] and model gateways [[litellm.ai]] / [[openrouter.ai]].

[[go.aiapply.co]] documents AIApply, a vertical AI agent for job search — not a developer harness, but a packaged end-to-end co-pilot used by 1.16M+ users that tailors ATS-optimized resumes and cover letters per job description, runs **Auto-Apply** to submit hundreds of matched applications on credit, and coaches live interviews via **Interview Buddy**. GPT-4 and Azure AI power document generation; Premium unlocks unlimited writing and interview tools while Auto-Apply credits meter outbound submissions separately. It illustrates how general-purpose LLM stacks get productized into single-domain autonomous workflows (discover → customize → execute → coach) — a pattern distinct from coding agents like [[factory.ai]] or [[warp.dev]], but relevant when comparing vertical SaaS agents vs horizontal agent frameworks.

[[codeyai.space]] documents Codey, a local-first "AI development OS" from Codey Labs — desktop, TUI, CLI, and web client in one private workspace with 70+ BYOK LLM providers (Claude, Codex, Gemini, OpenRouter, Ollama, Bedrock, custom). Built-in agents split into three modes: **Co-Pilot** (Prometheus build + Athena plan + Scout/Iris subagents, free tier), **Autopilot** (Matis prompt-to-polished Next.js, Pro), and **Workpilot** (Hermes for docs/browser/sheets/slides/PDFs/n8n, Pro). Pro is $10/month for advanced modes only — model usage stays on your provider accounts. `/init` generates `AGENTS.md`; Tab toggles Build vs Plan mode; custom agents are markdown in `.codey/agents/`; skills and MCP (local + remote OAuth) extend the toolbelt. It competes with terminal agents [[warp.dev]] and [[tmuxai.dev]], enterprise harnesses [[factory.ai]], mobile session extension [[happy.engineering]], and vendor-locked ADEs [[zcode.z.ai]] — emphasizing local files, provider freedom, and multi-mode agents (code + app generation + knowledge work) in one install.

[[phuryn-pm-skills]] is Paweł Huryn's PM Skills Marketplace (22k+ stars, MIT, v2.1.0) — the largest structured product-management plugin ecosystem for Claude Code and Cowork: 68 skills and 42 slash commands across nine installable plugins from discovery and strategy through execution, GTM, analytics, and an AI Shipping Kit for auditing vibe-coded repos. Unlike generic skill lists like [[voltagent-awesome-agent-skills]] or Anthropic's reference [[anthropics-skills]], every skill encodes a named PM framework (Teresa Torres OST, lean canvas, pre-mortem, North Star) and commands chain them into lifecycle workflows (`/discover`, `/write-prd`, `/plan-launch`, `/ship-check`). The same marketplace installs on Codex CLI (skills native; commands need plain-language invocation or model-driven conversion). Skills copy to [[shareai-lab-learn-claude-code]]-style paths in Cursor and Gemini. It complements methodology packs [[obra-superpowers]] and [[github-spec-kit]], PM task tooling [[anombyte93-prd-taskmaster]], plugin distribution via [[skills.sh]], and Codex marketplace patterns in [[openai-codex-plugin-cc]] — the go-to when agents need PM rigor, not just faster documents.

[[Yeachan-Heo-oh-my-claudecode]] documents oh-my-claudecode (OMC, 37k+ stars, MIT), a Claude Code plugin that turns magic keywords and natural language into structured multi-agent orchestration with zero configuration: a `UserPromptSubmit` hook detects triggers like `autopilot`, `ralph`, and `ultrawork` and injects the matching skill, a `Stop` hook enforces persistent verify-until-done modes, and `/team` runs a staged plan→prd→exec→verify→fix pipeline across 19 specialized agents while `omc team` spawns real tmux-pane CLI workers for Codex/Gemini/Antigravity/Grok/Cursor. Compaction-resistant state (notepad, project memory, per-plan learnings/decisions/issues logs) survives context resets, split into control-plane and data-plane artifacts. Where [[obra-superpowers]] and [[gsd-build-get-shit-done]] encode disciplined workflows as skill files without a hook-driven routing layer, OMC adds automatic prompt-level detection and its own persistent-mode enforcement, putting it closer to the orchestration-harness category of [[ruvnet-ruflo]] while its `ralph` mode shares lineage with [[frankbria-ralph-claude-code]] and [[snarktank-ralph]].


[[cocoindex-io-cocoindex]] introduces CocoIndex, an open-source incremental data pipeline engine (10,613 stars, Apache 2.0, Rust core / Python API) that keeps AI agent context continuously fresh by reprocessing only the changed delta whenever source data or pipeline code evolves. Its declarative `TargetState = Transform(SourceState)` model mirrors React's state-driven re-render pattern: you describe what your target index should look like, and the Rust engine applies the minimum required inserts, updates, and deletions. The result is sub-second freshness at 10× lower embedding cost than full recompute, with end-to-end lineage tracing every vector back to its source byte. 20+ ready-to-run examples cover code embedding, PDF RAG, knowledge graphs (Neo4j/Kuzu), multi-repo summarization, structured extraction (BAML/DSPy), and CSV → Kafka. Its flagship product CocoIndex-code is an MCP server for AST-aware, incremental semantic code search that complements Milvus-backed alternatives like [[zilliztech-claude-context]], while its general pipeline engine can power multimodal RAG setups comparable to [[HKUDS-RAG-Anything]].

[[elevenlabs.io]] documents ElevenLabs, the AI audio research company behind three platforms on shared foundational models: **ElevenCreative** (no-code speech, music, dubbing, image/video studio), **ElevenAgents** (omnichannel voice+chat agents with workflows, guardrails, testing, analytics, and native LLM integration across OpenAI/Anthropic/Google with backup cascading), and **ElevenAPI** (production TTS/STT/voice/music/dubbing APIs with ~75ms Flash TTS, Scribe v2 transcription, and official Python/Node SDKs). ElevenAgents' four-component architecture — ASR, pluggable LLM, expressive TTS, proprietary turn-taking — makes it a specialized voice layer for autonomous customer-facing agents rather than a general coding harness. It complements OpenAI Realtime/Agents patterns in [[developers.openai.com]], browser agents like [[browser-use.com]], and voice-input productivity tools like [[wisprflow.ai]].

[[pushover.net]] documents Pushover, a hosted push-notification service: a three-parameter HTTP POST (`token`, `user`, `message`) delivers alerts to a user's Android, iOS, and desktop clients via native push channels (Google/APNs), with HTML formatting, image attachments, and emergency-priority retry/expire semantics for messages that must be acknowledged. Free accounts get 10,000 messages/month; "Pushover for Teams" adds per-organization billing. As a single-purpose "notify a human" primitive with no orchestration logic of its own, it is the lighter-weight counterpart to full mobile Claude Code clients like [[happy.engineering]] — useful anywhere an agent, script, or monitoring pipeline needs to page a person without building push infrastructure.

[[marketstack.com]] documents Marketstack, an APILayer REST API for worldwide stock market data — EOD, intraday, real-time US quotes (IEX), 750+ indices, splits/dividends, exchange metadata, and V2 EDGAR SEC filings. Authentication is a single `access_key` on HTTP GET; responses are paginated JSON. The free tier allows 100 requests/month with 1-year EOD history; paid plans from $9.99/month unlock intraday, sub-15-minute real-time intervals, commodities, and company fundamentals. For agentic workflows it is an external-data primitive comparable to [[pushover.net]] (notify) or [[resend.com]] (email) — wrap it as an agent tool or MCP function when agents need live or historical market prices, the same pattern illustrated in [[browser-use.com]]'s stock-price tool example. Scheduled polling fits naturally with job runners like [[trigger.dev]].

[[firecrawl.dev]] documents Firecrawl, a web data API platform (145K+ GitHub stars) offering Search, Scrape, Crawl, Map, Parse, and Interact under one API key, aimed at turning the live web into clean, LLM-ready Markdown/JSON with claimed ~93% fewer input tokens than a raw fetch. It sits in the same web-access-for-agents space as [[browserbase.com]] and [[browser-use.com]], but frames itself as a data/extraction API first rather than a persistent interactive browser platform — Interact exists for occasional multi-step flows but is explicitly not built for long-running sessions. Official SDKs (Python, Node, Go, Rust, Java, Elixir), a CLI, and an MCP server (comparable in role to [[microsoft-playwright-mcp]]) round out the agent-integration surface.

[[abacus.ai]] documents Abacus.AI, a vertically integrated "Super Assistant" platform bundling ChatLLM Teams ($10/month) with 100+ frontier LLMs and image/video generators, the general-purpose **Abacus AI Agent** (Deep Agent) for vibe-coding full-stack apps, agent swarms, browser automation, and scheduled tasks/triggers, plus **Abacus AI Desktop** (local CoWork, coding CLI, meeting Listener). Enterprise tier adds permission-aware RAG chatbots, visual AI workflow builders, SSO/SAML, SOC 2/GDPR/HIPAA, and structured ML on tabular data. The company publishes open-source LLMs (Smaug-72B with DPOP, Giraffe 32k context) and the LiveBench benchmark. It competes as an all-in-one consumer/enterprise agent product rather than a composable OSS framework — compare model gateways [[openrouter.ai]] / [[litellm.ai]], browser agents [[browser-use.com]], workflow runners [[trigger.dev]], and code-first SDKs [[developers.openai.com]] / [[crewai.com]].

[[davidondrej-skills]] is David Ondrej's personal Agent Skills repository (1.5k+ stars, MIT) — 31 skills across agent-orchestration, skill-authoring, research-and-web, thinking-and-docs, and ops-and-setup categories. It stands out among skill collections like [[anthropics-skills]], [[mattpocock-skills]], [[forrestchang-andrej-karpathy-skills]], and [[voltagent-awesome-agent-skills]] for its heavy emphasis on multi-agent orchestration: `cmux` (native macOS multi-pane terminal control and agent-to-agent delegation), `delegating-to-agents` (choosing between Pi, Codex, Claude Code, and Hermes), `agent-self-scheduling` (cron/heartbeat/loop patterns), and `goal-loop` (writing `/goal`/Ralph-loop instructions) sit alongside more familiar skill-authoring (`effective-agent-skills`, `distribute-skill-to-all-agents`) and research (`deep-research`, `youtube-transcript` via DeepAPI) content. Several skills carry `disable-model-invocation: true`, firing only on explicit trigger phrases rather than automatic selection — used for higher-stakes utilities like `cyber-audit` and `fable-safe-prompt`. The orchestration focus overlaps with [[pi.dev]]'s own multi-provider terminal-agent design.

[[0xnyk-awesome-hermes-agent]] is the community-maintained Awesome-list for the Hermes Agent ecosystem (4.5k+ stars, CC BY 4.0) — the Hermes-specific discovery layer cataloging skills, plugins, memory providers, GUIs, deployment templates, integrations, and domain applications around [[hermes-agent.nousresearch.com]]. Unlike general skill directories [[voltagent-awesome-agent-skills]] or [[skills.sh]], every entry is Hermes-native or agentskills.io-compatible, tagged production/beta/experimental, and organized into operational playbooks (nightly self-evolution, memory pressure, OpenClaw migration) and six level-up blueprints (memory stack, operator cockpit, multi-agent execution, Paperclip-managed ops). Highlights include 15+ memory backends ([[garrytan-gbrain]], [[supermemory.ai]]), operator dashboards (hermes-workspace, mission-control), token compressors (rtk-hermes), and the agentskills.io ecosystem subsection (wondelai/skills, youtube-skills, 753-skill cybersecurity pack). It is the go-to index when extending a [[hermes-agent.nousresearch.com]] deployment beyond the core runtime.

[[using-claude-code-unreasonable-effectiveness-html]] is Thariq Shihipar's essay from the Claude Code team arguing that HTML—not Markdown—should be the default agent output format when humans must stay engaged in long workflows. As specs and plans grow past ~100 Markdown lines, readability collapses; single-file HTML artifacts add tables, CSS, SVG diagrams, interactivity, and export-to-prompt buttons while leveraging Claude Code's filesystem, MCP, git, and browser context. The post catalogs six use-case families (planning grids, annotated PR reviews, design prototypes with sliders, synthesized reports, throwaway custom editors) with concrete prompts and links an examples gallery at thariqs.github.io/html-effectiveness. The core thesis: HTML keeps operators "in the loop" as agents gain autonomy—without readable artifacts, humans rubber-stamp unread plans. Complements harness/configuration patterns in [[how-claude-code-works-in-large-codebases]], methodology skills in [[obra-superpowers]], and CLI ergonomics in [[x.com-ericzakariasson-building-clis-for-agents]].

[[app.sauna.ai]] documents Sauna, a YC-backed hosted "multiplayer AI" coworker that connects to 3,000+ tools (plus custom MCP connectors), learns how you and your team work, and acts autonomously in the cloud — drafting in your voice, filing tickets, running scheduled briefings and ops jobs — while you interact from the web app, iOS, iMessage, Slack, email, or Superhuman. Its dual memory model splits durable workspace notes (`memory/` files) from episodic session recall; Spaces, Brain Access, and Shared Folders enable team coordination; and a newer Apps layer lets operators vibe-code just-in-time personal SaaS (CRM, inbox, mission-control dashboards) over live tool data without Zapier. Credit-metered plans from $99/month. It competes as a closed, omnichannel operator workspace — compare self-hosted agent OS options [[runcabinet.com]] / [[hilash-cabinet]], org-chart control planes [[paperclip.ing]], mobile Claude Code extension [[happy.engineering]], and all-in-one assistants [[abacus.ai]] / [[joinoasis.com]].

[[supabase.com]] documents Supabase, the open-source Postgres development platform (105K+ GitHub stars) that bundles a dedicated Postgres database, Auth with Row Level Security, auto-generated REST/GraphQL APIs, S3-compatible Storage, WebSocket Realtime, Deno Edge Functions, and a pgvector AI toolkit into one integrated backend — positioned as a Firebase alternative you can host, self-host, or run locally via the Supabase CLI (`supabase start` / `supabase db push`). For agentic workflows it is the default managed backend behind [[lovable.dev]] and [[bolt.new]], a common RAG/vector persistence layer (semantic + hybrid search, LangChain/LlamaIndex integrations), and the vector store [[firecrawl.dev]] migrated to from Pinecone. Docs include first-class AI-tooling guides (MCP, skills, plugins). Compare specialized vector DBs when pgvector scale is insufficient; compare [[trigger.dev]] for long-running background jobs vs Edge Functions for short webhook/LLM orchestration tasks.

[[google-adk-go]] documents ADK Go (`google.golang.org/adk/v2`, 8.4k+ stars, Apache 2.0, v2.0.0) — Google's idiomatic Go port of the Agent Development Kit for building cloud-native agent apps with streaming `runner` loops, graph-based `workflow` orchestration (branches, joins, retries, HITL), MCP and A2A tool integrations, session/memory/artifact services, and REST/A2A/Agent Engine server surfaces. Optimized for Gemini but model-agnostic; Python ADK remains the behavioral source of truth. It is the Go runtime counterpart to [[adk.dev]]'s Python-centric docs — compare multi-agent graph frameworks [[langchain.com-langgraph]], AWS's [[strandsagents.com]], Microsoft's [[microsoft-agent-framework]], and crew orchestration in [[crewai.com]] when choosing a production agent SDK.

[[vellum.ai]] documents Vellum, an MIT-licensed personal intelligence platform (`vellum-ai/vellum-assistant`, v0.10.6) — a persistent, always-on AI assistant with managed memory (workspace files + PKB + hybrid vector recall), its own identity (Agent Mail email, Slack handle, Twilio phone), 28+ bundled skills, and omnichannel reach (web, iOS, macOS desktop with computer use, CLI, Telegram, Slack, email, voice). Vellum Cloud is the default hosted path with managed OAuth; local self-host uses the same Bun+TypeScript runtime (`assistant/` + `gateway/` + credential-executor sidecar). The site explicitly positions against [[hermes-agent.nousresearch.com]] and OpenClaw on setup friction and DIY memory stacks. It competes in the hosted personal-AI coworker space with [[app.sauna.ai]], [[joinoasis.com]], and [[abacus.ai]] — but emphasizes single-user personal scope, procedural skill memory, and native Mac automation rather than team multiplayer or org-chart control planes like [[paperclip.ing]].

[[ollama-ollama]] documents Ollama (`ollama/ollama`, 175k+ stars, MIT, Go) — the default local LLM runtime for running open-weight models on macOS, Windows, and Linux. It provides model pull/run, a localhost REST API on port 11434, official Python/JS SDKs, OpenAI- and Anthropic-compatible endpoints, and `ollama launch` integrations that wire local models into coding agents (Claude Code, Codex, Copilot CLI, OpenCode) and personal assistants (OpenClaw). Inference uses llama.cpp (GGUF) and MLX (safetensors) with Metal/CUDA/ROCm/Vulkan acceleration. Ollama is the infrastructure layer beneath many agent frameworks in this wiki — [[litellm.ai]], [[strandsagents.com]], [[pydantic.dev]], [[gitlawb-openclaw]], [[adk.dev]], [[microsoft-agent-framework]], and [[crewai.com]] all list Ollama as a supported local provider — and the privacy-preserving alternative to cloud APIs for development, air-gapped deployment, and cost control.

[[vercel.com]] documents Vercel, the "Agentic Infrastructure" / AI Cloud platform for building, deploying, and scaling AI apps and agents. Its Agent Stack bundles AI SDK (unified LLM API), AI Gateway (hundreds of models, budgets, fallbacks), Sandbox (Firecracker-isolated untrusted code execution), Workflows (durable pause/resume orchestration), Passport (identity for internal agents), and eve (filesystem-first durable agent framework). Agent-native DX includes CLI, MCP server, Vercel Plugin for Claude Code/Cursor, and [[skills.sh]] Agent Skills. Core platform adds Fluid Compute (optimized serverless concurrency), global CDN, observability, and Vercel Agent for autonomous debugging/PRs. It is the deployment and agent-ops layer many frameworks target — compare model gateways [[litellm.ai]], durable background jobs [[trigger.dev]], browser isolation [[browserbase.com]], and local inference [[ollama-ollama]] when choosing infrastructure.

[[render.com]] documents Render, a unified PaaS ("the cloud for builders") for deploying web apps, APIs, and AI agents with minimal ops — web services, static sites, private services, background workers, cron jobs, managed Postgres, and Redis-compatible Key Value on a private network. For agentic workloads it adds **Render Workflows** (durable task orchestration as TypeScript/Python functions with managed queuing and on-demand instances), a hosted **MCP server** (`mcp.render.com`), official **Agent Skills** (`render-deploy`, `render-debug`, `render-monitor`), Jules PR auto-fix integration, and LLM-friendly docs (`llms.txt`, per-page `.md` URLs). Full-stack **preview environments** clone Blueprint-defined production stacks per PR. It competes as a Heroku/Railway alternative with explicit agent-hosting positioning — compare edge/serverless agent infrastructure [[vercel.com]], framework-native background jobs [[trigger.dev]], and backend-as-a-platform [[supabase.com]] when choosing deployment targets.

[[streamlit.io]] documents Streamlit, the open-source Python framework (45K+ stars, Apache 2.0, Snowflake-owned) for turning data scripts into interactive web apps in minutes — dashboards, reports, and LLM chat UIs without front-end code. Its scripting model reruns Python on each widget interaction, with Session State, `st.cache_data`/`st.cache_resource`, forms, fragments, and multipage navigation for state and performance. Deploy via free **Community Cloud** (GitHub push-to-deploy) or **Streamlit in Snowflake** for enterprise. It is a common rapid-UI layer for agent demos and internal ML tools — compare full-stack builders [[lovable.dev]], enterprise internal-app platforms [[retool.com]], and PaaS hosts like [[render.com]] that support Streamlit deployment.

[[claudemarketplaces.com]] documents Cross AI Tools (claudemarketplaces.com → crossaitools.com), an independent editor-curated directory of Claude Code skills (21,600+), plugin marketplaces (2,500+), and MCP servers (12,500+) with ~300k monthly visitors. Unlike raw aggregators, it filters skills to 500+ installs, requires active GitHub repos, runs manual editorial review, and publishes editor-written summaries plus one-command install snippets on every detail page. Automated crawlers ingest from [[skills.sh]], GitHub marketplace schemas, and MCP registries; `llms.txt`/`llms-full.txt` expose machine-readable catalogs for agents. A weekly "This week in Claude" digest tracks model releases and ecosystem moves. It is the quality-filtered Claude Code-specific counterpart to [[skills.sh]]'s full leaderboard and awesome-lists like [[voltagent-awesome-agent-skills]] — featured entries overlap with [[obra-superpowers]], [[anthropics-skills]], [[getcaveman.dev]], and [[forrestchang-andrej-karpathy-skills]].

[[anysearch.com]] is AnySearch, a search infrastructure platform built for AI agents, offering unified search-source access with intent-aware routing behind a single API, MCP server, and Skill integration. It advertises Zero Retention Execution and benchmarks itself directly against [[brave-search]] and Parallel on accuracy and latency, but its concrete API/MCP reference documentation is client-rendered and was not retrievable in static form — a thinner source than [[brave-search]]'s fully documented endpoints and public MCP server.

[[designmd.cc]] documents DesignMD, a free web tool and CLI (`@designmdcc/cli`, companion repo `adityarajdigital/designmd`) that measures a live site's DOM and CSSOM — not a screenshot — to generate a portable `DESIGN.md` spec (colors, typography, spacing, breakpoints, interaction states) in ~12 seconds, purpose-built as project-root context for coding agents. It occupies the same `DESIGN.md`-as-agent-context niche as [[voltagent-awesome-design-md]] and [[open-design.ai]], but where those distribute pre-made brand libraries, DesignMD is an on-demand live extractor for any URL — the generation counterpart to their curation, both tracing back to the [[stitch.withgoogle.com]] `DESIGN.md` specification.

[[crafterq.ai]] is CrafterQ, a no-code SaaS for website AI agents — conversational assistants trained on an organization's own content (site pages, docs, FAQs, PDFs, custom Q&A) and embedded via a single script. It targets marketing, sales, support, and e-commerce outcomes (lead capture, product recommendations, 24/7 self-service) with SOC 2 Type II compliance and tiers from a free plan through enterprise private hosting. It sits in the customer-facing embeddable-agent layer rather than developer agent frameworks; adjacent ingestion tooling like [[firecrawl.dev]] could feed custom RAG pipelines, while [[elevenlabs.io]] covers voice-first agents and [[abacus.ai]] broader enterprise chatbot/workflow stacks.

[[neon.com]] is Neon, a serverless Postgres backend platform (22.5K+ stars, Databricks-owned) built for apps and agents — lakebase architecture separating compute and storage for autoscaling, git-like copy-on-write branching, scale-to-zero, and fleet APIs that provision databases in ~120ms. Beyond Postgres it bundles Neon Auth (Better Auth in Postgres), Data API (Supabase-compatible REST), and early-access Functions, Storage, and AI Gateway — all wired for agents via `npx neon@latest init`, MCP server, Agent Skills, and editor plugins. Codegen platforms use it to deploy thousands of per-user databases that pause when idle. It is the agent-native managed Postgres layer — compare full-stack backends [[supabase.com]], deployment hosts [[render.com]]/[[vercel.com]], and framework persistence [[langchain.com]] when choosing infrastructure.

[[summio.org]] is Summio, an AI reading companion for iPhone (iOS 18+) that turns books, YouTube videos, articles, and PDFs into citation-grounded editorial summaries at four depth levels, with full-text RAG chat, audio narration, and spaced-repetition retention — source-agnostic (any URL or upload) versus catalogue-locked apps like Blinkist. Backend uses OpenAI and Gemini over Supabase/Cloudflare; no public API. It is the consumer end-user counterpart to programmatic grounded-document tools in this wiki — compare [[teng-lin-notebooklm-py]] (NotebookLM automation), [[reseek.net]] (personal knowledge MCP), and ingestion layers like [[firecrawl.dev]] when building agent pipelines versus reading on phone.

[[Chachamaru127-claude-code-harness]] is Claude Code Harness (2,951 ★, MIT, v4.16.4) — a Claude Code plugin that enforces a **Plan → Work → Review → Release** delivery loop via five verb skills (`/harness-plan`, `/harness-work`, `/harness-review`, `/harness-sync`, `/harness-release`) with `spec.md` and `Plans.md` as dual SSOT contracts. A Go-native guardrail engine enforces permission boundaries without Node.js; non-trivial planning runs sub-agent validation; review is independent from implementation; release packages only verified evidence. Multi-harness adapters exist for Codex CLI, OpenCode, and Cursor with explicit support-tier boundaries (`supported` / `internal-compatible` / `candidate` only — no false parity). It competes in the Claude Code harness/methodology space with [[obra-superpowers]] (skill-bootstrap TDD methodology), [[gsd-build-get-shit-done]] (GSD workflow), [[Yeachan-Heo-oh-my-claudecode]] (OMC harness), and spec-first tooling [[github-spec-kit]] — but emphasizes contract-driven plan approval, honest host capability matrices, and a self-referential repo that dogfoods its own loop.

[[phasr.sh]] is Phasr, a free MIT-licensed macOS desktop workspace (public beta, pre-1.0) for running multiple CLI coding agents in parallel — each task in its own Git worktree with real-time diff review and explicit human approval before merge. It is agent-agnostic (Claude Code, Codex, Gemini, Cursor, Aider, OpenCode) and provider-agnostic (per-task model routing). The companion repo implements Tauri 2 + React 19 with Rust PTYs, SQLite, and optional Clerk/Supabase sync. It sits alongside [[vibekanban.com]] and [[coleam00-archon]] in the git-worktree parallel-agent orchestration space, but as a native desktop app focused on review-first merge control rather than kanban planning or YAML workflow DAGs.

[[SnailSploit-Claude-Red]] is claude-red (2,659+ stars, MIT) — the largest domain-specific offensive-security skill library for the Claude Skills system, with 58 `SKILL.md` files across 13 categories (web, wireless, AD, cloud, mobile, IoT, infrastructure/red team, exploit dev, fuzzing, recon, AI security, utility). Each skill primes Claude with expert methodology for a specific attack surface (SQLi to shellcode, EDR evasion to ADCS abuse) and loads on demand via conversational triggers. It applies the same [[anthropics-skills]] `SKILL.md` pattern as [[obra-superpowers]] and [[davidondrej-skills]], but specialized for authorized red team, bug bounty, and CTF work rather than general agent workflows — listed in [[skills.sh]] and [[claudemarketplaces.com]], with a seven-phase roadmap targeting ~107 skills.

[[integuru.com]] is Integuru (YC-backed, 10M+ API calls/month) — a platform that generates production-ready APIs for websites lacking usable public APIs by reverse-engineering the private HTTP endpoints a platform's own frontend calls, then exposing them as documented direct-HTTP integrations with sub-3-second latency and 99.9%+ reliability. It explicitly positions against browser automation and RPA: no browsers, auth auto-healing on Production plans, 24/7 on-call maintenance, and a Full Managed Service that builds custom integrations for free sandbox trial. CLI-first (`npm install -g integuru`) with 48+ published integrations across healthcare, fintech, logistics, and property management. The companion repo `Integuru-AI/Integuru` (4.6K stars, AGPL-3.0) is the open-source v0 HAR-based agent; the hosted product automates capture, schema docs, and maintenance. It competes architecturally with [[browser-use.com]], [[microsoft-playwright-mcp]], and [[browserbase.com]] for agent tool access, but at the backend-API layer rather than the browser layer — adjacent to [[firecrawl.dev]] (HTML content extraction) for the "agents need web access" problem.

[[how-to-master-dynamic-workflows-claude-code-6-patterns-14-steps]] is an X article by @0xCodez (June 2026) distilling Claude Code Dynamic Workflows — the May 28, 2026 feature where Claude writes bespoke JavaScript harnesses to orchestrate subagents — into six composable patterns (classify-and-act, fan-out-and-synthesize, adversarial verification, generate-and-filter, tournament, loop until done) and practical controls (`ultracode`, `/goal`, `/loop`, token budgets, quarantine for untrusted inputs). It explains when one context window breaks down (agentic laziness, self-preferential bias, goal drift) and maps patterns to real use cases: migrations, deep research, triage, root-cause, sorting at scale, and lightweight evals. It complements [[how-claude-code-works-in-large-codebases]] (enterprise adoption) and [[9d5bzxVsocw-anthropic-just-dropped-the-new-blueprint]] (harness design theory) with operator-facing orchestration recipes — and shows how saved workflows ship as [[anthropics-skills]].

[[docusaurus.io]] documents Docusaurus (v3.10.1, 65K+ GitHub stars, MIT) — Meta's React static-site generator built specifically for documentation websites. It covers the MDX authoring model, `@docusaurus/preset-classic` plugin bundle (docs, blog, pages, Algolia search, sitemap), document versioning, i18n with Crowdin-friendly filesystem layouts, and deployment to Jamstack hosts like [[vercel.com]] and [[render.com]]. For this wiki, Docusaurus is the standard docs stack behind projects such as [[aaif-goose-goose]] and deployment guides referenced from [[skills.sh]] — the layer many agent frameworks use to publish human- and agent-readable documentation without building a custom site generator.

[[chopratejas-headroom]] is Headroom (57.6K+ stars, Apache 2.0, v0.30.0) — a local-first context compression layer that shrinks tool outputs, logs, RAG chunks, files, and conversation history before they reach the LLM, with reversible CCR caching so originals stay retrievable. It ships as a library (`compress(messages)`), OpenAI-compatible proxy, one-command agent wrappers (`headroom wrap claude|codex|cursor|…`), MCP server, cross-agent memory, and `headroom learn` for mining failed sessions into agent instruction files. It targets the runtime compression niche — compare [[nadimtuhin-claude-token-optimizer]] (startup doc structure for Claude Code), [[mksglu-context-mode]] (context-mode CLI), and [[zilliztech-claude-context]] (semantic codebase indexing) — but Headroom compresses live tool/RAG payloads in-flight across every major coding agent and framework integration (LangChain, Agno, LiteLLM, Vercel AI SDK).

[[agricidaniel-claude-obsidian]] is claude-obsidian (8,984+ stars, MIT, v1.9.2) — the most mature open-source Karpathy LLM Wiki implementation for Obsidian + Claude Code. Fifteen Agent Skills (`/wiki`, `/wiki-ingest`, `/wiki-query`, `/wiki-lint`, `/autoresearch`, `/save`, `/think`, `/canvas`, etc.) turn a plain-Markdown vault into a self-organizing second brain: ingest sources into typed pages, answer with wiki citations (not training data), lint orphans and dead links, and compound session context via `hot.md`. v1.7 "Compound Vault" adds Obsidian CLI transport, hybrid BM25+cosine retrieval, and multi-writer advisory locks; v1.8 ships first-class LYT/PARA/Zettelkasten/Generic methodology modes; v1.9 adds a 10-principle `/think` loop. It hard-prefers [[kepano-obsidian-skills]] as Obsidian substrate and competes in the Karpathy-wiki niche with [[6eanut-llm-wiki]] (lighter skill) and [[runcabinet.com]]/[[hilash-cabinet]] (Cabinet pattern) — but with deeper Obsidian integration, autonomous research, and the largest community in the Claude+PKM space.

[[pbakaus-impeccable]] is Impeccable (44K+ stars, Apache 2.0, Skill 3.9.1) — Paul Bakaus's design language for AI coding agents, evolved from Anthropic's `frontend-design` skill in [[anthropics-skills]]. One `/impeccable` skill exposes 23 design commands (`init`, `audit`, `polish`, `critique`, `live`, etc.), writes `PRODUCT.md` and `DESIGN.md` at setup, and ships 45 deterministic anti-pattern detector rules runnable via `npx impeccable detect` (no LLM) or provider-native design hooks on Cursor, Claude Code, Codex, and GitHub Copilot. It occupies the agent-frontend-design niche alongside [[voltagent-awesome-design-md]] and [[designmd.cc]] (DESIGN.md as agent context, both tracing to [[stitch.withgoogle.com]]), but Impeccable is an active design workflow and lint layer — not a brand library or live extractor. Config-driven build factory compiles one `skill/SKILL.src.md` into 12+ harness formats; Chrome extension and impeccable.style site share the same rule engine.

[[higgsfield.ai]] is Higgsfield, an AI-native creative suite for multi-model video, image, and audio generation (Sora 2, Kling 3.0, Veo 3.1, Seedance, Nano Banana, Soul, FLUX, and 30+ more) with 25M+ users and ~6M daily generations. For agent builders it ships **MCP & CLI** ("turn Claude into a creative engine"), **Supercomputer** (Fable 5.0 superagent for websites, marketing, UGC, games, and design pipelines), and **App Builder** (full-stack apps with Higgsfield models built in), plus an async REST API at `platform.higgsfield.ai` with webhooks and a Python SDK. It sits in the creative-tool MCP niche alongside [[canva.com]] (design MCP) and [[render.com]] (infrastructure MCP), but focused on generative media orchestration rather than static design or deployment.

[[Starmel-OpenSuperWhisper]] is an open-source (MIT, 1,959+ stars) macOS dictation app that runs real-time transcription fully locally via whisper.cpp or Parakeet, offering system-wide keyboard/mouse-triggered recording without cloud dependency — the self-hosted counterpart to hosted voice-to-text products like [[wisprflow.ai]]. Notably, the repo also bundles a small maintainer-side autonomous coding agent (`agent/`) that wraps Aider over OpenRouter (`deepseek-v4-flash`) to pick up open GitHub issues, implement fixes in the Swift codebase, and iterate on build failures until they compile — a concrete, narrow example of an LLM-driven issue-to-PR loop wired into a real open-source project's maintenance workflow.

[[axi.md]] documents AXI (Agent eXperience Interface) — a design and catalog for building agent-ergonomic CLI tools where token budget is treated as a first-class constraint. It frames the “MCP vs CLI” debate as a surface-level transport choice and argues the deeper issue is whether an agent-tool interface is shaped for reliable, discoverable next steps; AXI’s 10 principles emphasize TOON token-efficient structured output, minimal default schemas, truncation, pre-computed aggregates, definitive empty states, structured errors/exit codes, ambient context, and contextual disclosure (including concise per-subcommand `--help`). The site also points to reference implementations like `gh-axi` (GitHub operations) and `chrome-devtools-axi` (browser automation), plus an ecosystem of community AXIs built on the same interface principles.

[[GoogleCloudPlatform-knowledge-catalog]] is the official Google Cloud sample and tooling repository for Knowledge Catalog (formerly Dataplex), an AI-powered data catalog and metadata management platform. Its primary contribution is the **Open Knowledge Format (OKF)** — a vendor-neutral, markdown+YAML-frontmatter directory format for representing any knowledge corpus so that any agent, human, or tool can read and produce it without bespoke SDKs. The repo ships a reference two-pass enrichment agent (BigQuery metadata pass + LLM-as-crawler web pass), a metadata-as-code toolbox (`kcmd`/`kcagent`), and discovery/enrichment samples. OKF bundles are git-native, portable, and consumable by any markdown-aware tool — Obsidian, MkDocs, or a Google ADK agent (see [[adk.dev]]) — making them a practical building block for durable, structured agent context management pipelines alongside incremental alternatives like [[cocoindex-io-cocoindex]] and graph-based retrieval systems like [[HKUDS-RAG-Anything]].

[[coleam00-cole-medin-ai-coding]] is an Open Knowledge Format (OKF) bundle of Cole Medin's best AI-coding videos, transcript-verified and organized into five concept pages (PIV Loop, Context Engineering/PRP, AI Layer, Archon Harness Builder, MCP Integration) and five video knowledge pages — designed to be cloned directly into any AI second brain so agents can answer questions about his teaching without going to YouTube. It ships a zero-dependency Python CLI (`okf-cli.py`) for index, keyword search, and page reading. The bundle is a consumer of OKF (see [[GoogleCloudPlatform-knowledge-catalog]]) and a curated companion to [[coleam00-archon]] (the Archon harness builder itself) and [[coleam00-harness-engineering-demo]], making it the most accessible entry point into Cole's principled agentic engineering system: PIV loop → context engineering → harness engineering.

[[determinate.systems]] documents Determinate Nix Installer — the industry-standard, planner-based Rust tool for installing Nix and enabling flakes on macOS, Linux, WSL, Docker, and CI pipelines, with over 7 million installs. Backed by Determinate Systems, the installer is notable for being the only Nix installation method with a reliable single-command uninstall (`/nix/nix-installer uninstall`), macOS upgrade survival (Nix Survival Mode), and a pinnable GitHub Actions integration (`DeterminateSystems/determinate-nix-action@v3`). It installs **Determinate Nix** — extended upstream Nix with parallel evaluation, lazy trees, WebAssembly builtins, provenance, and flake schemas — and serves as the entry point to the broader Determinate platform: FlakeHub (SemVer flake registry), FlakeHub Cache (federated binary caching), and Determinate Secure Packages (enterprise-grade Nixpkgs snapshots).

[[wezterm.org]] documents WezTerm — a GPU-accelerated, cross-platform terminal emulator and multiplexer written in Rust (27,000+ GitHub stars). Its standout traits are a unified GPU renderer (Metal/Vulkan/OpenGL) for smooth high-DPI display, a Lua-based live-reloading configuration system, and built-in terminal multiplexing that connects local and remote sessions via SSH, Unix, or TLS domains — all in a single binary. Key features include a built-in libssh2 SSH client that reads `~/.ssh/config` automatically, Quick Select Mode and Copy Mode for keyboard-driven text capture, Shell Integration (OSC 7, prompt zones), iTerm Image Protocol for inline images, and hundreds of built-in color schemes. Available on macOS, Windows, Linux, FreeBSD, and NetBSD; comparable to [[tmux-tmux]] + [[tmuxai.dev]] but as a native GPU-rendered terminal rather than a pure CLI multiplexer.

[[kunchenguid-dotfiles]] is a complete, reproducible macOS developer environment managed with nix-darwin, home-manager, and nix-homebrew — designed for agentic engineering. A single `./bootstrap.sh` brings a fresh Apple Silicon Mac to a fully configured state: macOS system preferences, Homebrew casks (including `wezterm` and `claude-code`), Nix user packages, zsh shell with starship prompt and high-agency aliases (`cc` / `co`), Neovim, and a shared `AGENTS.md` symlinked simultaneously into Claude, Codex, and opencode. The repo is a reference implementation of the "one repo, one command" dotfiles pattern using Nix flakes, with `cleanup = "zap"` enforcing declarative Homebrew management. It bootstraps Nix via [[determinate.systems]] and configures [[wezterm.org]] out of the box.

[[herdr.dev]] documents Herdr — the agent multiplexer for the terminal (14,500+ stars), positioned as "to coding agents what tmux is to terminals." A single Rust binary with no Electron and no account, Herdr runs a persistent background server that keeps every coding agent alive across detach, SSH reconnects, and laptop lid closes. It provides a sidebar showing blocked/working/done/idle state across all workspaces for 18+ supported agents (Claude Code, Codex, Pi, GitHub Copilot CLI, OpenCode, Hermes Agent, and more), mouse-first pane management, a local socket API for agent/script automation, a plugin system, and native session resume after server restarts. Herdr is the operational layer between a developer and their herd of parallel coding agents — see [[tmux-tmux]] for the classic terminal multiplexer it is modeled after and [[kunchenguid-dotfiles]] for a dotfiles setup that installs and configures it.

[[kiro.dev]] documents Kiro — Amazon's coding agent available as an IDE, CLI, and web interface, and the successor to Amazon Q Developer. Kiro's defining approach is spec-driven development: feature work is formalized into three structured artifacts (requirements.md, design.md, tasks.md) and executed with dependency-graph wave scheduling that runs independent tasks concurrently. Key primitives include hooks (event-driven automations on file save, tool use, spec task execution, or user prompt submission) and steering (persistent markdown context files at workspace or global scope, compatible with the AGENTS.md standard). A unified Kiro Agent Server (CLI 3.0) powers IDE, CLI, and web from the same engine; the CLI exposes an ACP endpoint for programmatic integration. Model choices range from Claude Opus 4.8 to Qwen3 Coder Next (0.05x cost) with Auto routing. Compare with [[cognition.ai]] and [[factory.ai]] for alternative coding agent approaches, and [[herdr.dev]] for multiplexing Kiro CLI alongside other agents.

[[redis.io]] documents Redis Iris, Redis's unified real-time context engine for AI agents — a repackaging of four existing Redis capabilities (Data Integration/CDC pipelines, schema-first Context Retriever, Agent Memory, and LangCache semantic caching) under one agent-facing positioning. It's a useful data point for how incumbent infrastructure vendors are re-marketing existing primitives (change-data-capture, vector retrieval, response caching) specifically as "agent context" plumbing, distinct from the open-source memory/RAG tools in this wiki like [[supermemory.ai]] and [[zilliztech-claude-context]] — Iris is delivered as a hosted/enterprise Redis capability rather than a standalone library.

[[coleam00-redis-iris-agent]] is a small [[pydantic.dev]]-based reference implementation (Cole Medin, MIT, 6 stars) showing how to actually wire an LLM agent to two [[redis.io]] Iris primitives — Context Retriever as an auto-generated MCP toolset over live business data, and Agent Memory as plain tools for cross-session recall — with a full reproducible 134-record support-desk demo. Where [[redis.io]]'s own page states the value proposition, this repo shows the concrete tool-call sequence (`search_memory`, `get_customer_by_id`, `filter_order_by_customer_id`, ...) an agent builder would need to reproduce the pattern themselves, explicitly marked as a proof-of-concept rather than a production template.

[[duply.ai]] is a curated library of 312+ real-world design systems, each published as a copy-paste-ready `DESIGN.md` file (structured color/typography/spacing/radius/component tokens plus a written analysis) so a coding agent can reproduce a specific brand's visual language exactly. It sits in the same DESIGN.md-distribution niche as [[voltagent-awesome-design-md]] (73+ brands) and [[open-design.ai]] (150+ brands plus a full design engine), differentiating on raw library size and on shipping both per-brand raw exports and a single-fetch full-corpus file — a contrast to [[designmd.cc]]'s live DOM/CSSOM extraction approach to the same format.

[[dark-factory-autonomous-ai-codebase]] is MindStudio's explainer on "dark factories" — fully autonomous software pipelines where AI agents plan, code, test, and deploy without human intervention, borrowing the term from lights-out manufacturing. It's a conceptual framing rather than a product: it names five required components (planning, code generation, validation, deployment, orchestration agents), a Planner-Generator-Evaluator loop as the core adversarial pattern, and concrete failure modes (cascading plan failures, overprivileged agent actions, evaluation gaming, agent sprawl) worth checking against harness designs already documented in this wiki, such as [[coleam00-harness-engineering-demo]] and [[coleam00-archon]].

[[pydantic-pydantic-ai]] is the Pydantic team's Python agent framework (18,400+ stars) — the flagship product of [[pydantic.dev]], covered here in depth beyond that page's stack-wide overview. Its defining trait is static type-safety threaded through the whole agent loop: tools, dependency injection, and structured output are all generic and checkable before runtime. A capabilities system (reusable bundles of tools, hooks, instructions, model settings) is the extension point — core ships only capabilities needing deep provider integration or considered universally essential, while everything else (memory, guardrails, code execution, multi-agent orchestration) lives in a separately-versioned **Pydantic AI Harness** package that "graduates" proven capabilities into core over time. Comparable to [[langchain.com]], [[strandsagents.com]], [[adk.dev]], and [[crewai.com]] as an agent framework, differentiating on type-safety and tight Pydantic-ecosystem integration.

[[davila7-claude-code-templates]] is a large, actively maintained CLI (`npx claude-code-templates`) and catalog for Claude Code — 600+ agents, 200+ commands, 55+ MCP integrations, plus hooks, settings, skills, and autonomous "loops" — installable individually or as full stacks in one command, alongside operational tools (session analytics dashboard, mobile chat monitor, health-check, plugin/marketplace manager). It aggregates and re-licenses components from several sources already in this wiki, including [[anthropics-skills]] and [[obra-superpowers]], and competes with pure discovery sites like [[claudemarketplaces.com]] and [[skills.sh]] by also shipping its own installer and dev-tooling, not just a browsable index.

[[gemini-enterprise-agent-platform]] documents Google Cloud's rebrand of Vertex AI into a full-stack platform for building, scaling, governing, and optimizing enterprise AI agents — spanning agent building (Agent Studio, the ADK), multi-provider model access via Model Garden (200+ models including Anthropic's Claude family), and classic ML infrastructure (training, prediction, pipelines, MLOps). [[antigravity.google|Google Antigravity]] is now surfaced as an orchestration app within the platform. It's Google's entry in the same "unified agent platform" category as the open-source [[adk.dev]] / [[google-adk-go]] SDK it bundles, but scoped to GCP's managed console with consumption-based pricing and org-wide governance via a separate Gemini Enterprise app.

[[dietrichgebert-ponytail]] is a behavioral ruleset (82,500+ stars, MIT) that pushes coding agents through a strict escalation ladder — YAGNI, reuse, stdlib, native platform feature, existing dependency, one-liner, only then custom code — before writing anything, while carving out safety-critical code (validation, error handling, security, accessibility) as never subject to the laziness rule. Its distinguishing trait versus other CLAUDE.md-style guidance in this wiki, like [[x.com-mnilax-karpathys-4-claude-md-rules-cut-claude-mistakes]] and [[forrestchang-andrej-karpathy-skills]], is a reproducible in-repo benchmark (a headless Claude Code session against a real FastAPI+React codebase) showing measured cuts in LOC, tokens, cost, and time versus a no-skill baseline and two control prompts, plus adapters spanning roughly 20 agent hosts rather than a single-platform CLAUDE.md.

[[bradautomates-claude-video]] is a focused Agent Skill (`/watch`, 8,256+ stars, MIT) that gives Claude the ability to actually watch a video — downloading it, extracting scene-aware frames, pulling a timestamped transcript (free captions first, Whisper as fallback), and handing both to Claude's multimodal `Read` tool — instead of guessing from a title or a threadbare transcript. It ships measured cost tables per fidelity tier directly in its README (a real 49-minute-video benchmark) and a frame-deduplication pass that drops near-identical frames before they burn tokens. Installable the same way as broader catalogs like [[davila7-claude-code-templates]] via [[skills.sh]]'s `npx skills add`, but narrowly scoped to one capability rather than bundling many.

[[chaseai-yt-grill-me-codex]] extends [[mattpocock-skills]]'s `grill-me`/`grill-with-docs` planning skills with a cross-provider adversarial review loop: after Claude interrogates the human to lock a plan, OpenAI Codex — running strictly read-only and resumed across rounds so it remembers prior critiques — reviews and either approves or demands revision, bounded by a round cap. An optional third act flips the roles entirely: Codex writes the code from the frozen plan with full write access while Claude re-verifies the diff and runs the proof test independently before any commit. The core argument — a model can't be trusted to grade its own plan or its own code — distinguishes it from single-model plan-review skills like [[seangeng.com-plan-optimizer]] and [[shadcn-improve]] already in this wiki.

[[oso95-scroll-world]] is an Agent Skill that builds Apple-style scroll-scrubbed "fly through the world" landing pages for any brand: a camera flies into one generated diorama scene after another with no cuts, driven purely by scroll position. It's a thin orchestration layer over [[higgsfield.ai]]'s generative-media API — the skill's own contribution is the interview flow, the prompt templates, a seam-matching procedure (connector clips generated from neighbouring scenes' actual rendered frames, so transitions are frame-identical), and a portable vanilla-JS scrub engine. Sits alongside [[motionsites.ai]] and [[stitch.withgoogle.com]] in the "AI generates a marketing surface" niche, but produces pre-rendered scroll-scrubbed video rather than live UI or copy-paste prompts.

[[leonxlnx-taste-skill]] is a family of thirteen Agent Skills targeting a specific complaint about AI-built frontends — that they default to generic, boilerplate-looking layouts. Ten skills output code (a flagship `taste-skill` with three tunable 1-10 dials for layout variance, motion intensity, and visual density, plus fixed-aesthetic variants like `soft-skill`, `minimalist-skill`, `brutalist-skill`, and a Stitch-compatible `stitch-skill`) and three output design reference images only (`imagegen-frontend-web`, `imagegen-frontend-mobile`, `brandkit`), meant to be generated first and handed to a coding agent. Installable the same cross-host way as other `npx skills add`-distributed families on [[skills.sh]], and interoperable at the token-format level with [[stitch.withgoogle.com]] and [[voltagent-awesome-design-md]] via `stitch-skill`'s optional `DESIGN.md` export.

[[playwright.dev-agent-cli]] is Microsoft's shell-first alternative to [[microsoft-playwright-mcp]] for coding agents: instead of an LLM calling structured MCP tools with schemas loaded into context, the agent runs `playwright-cli` shell commands and gets back concise page-state text plus an accessibility-tree snapshot file with element refs for the next command. Both interfaces share the same underlying Playwright automation engine and capability groups — the choice is about token budget and runtime style (CLI for large-codebase coding agents, MCP for exploratory agentic loops) rather than competing automation technology. Notable features include named isolated sessions for parallel browser instances, a visual dashboard for a human to watch or take over a stuck agent session, and built-in testing primitives (`verify-element-visible`, `generate-locator`) alongside the standard navigate/click/type/snapshot command set.

[[googleworkspace-cli]] is `gws`, a Rust CLI that exposes the whole Google Workspace API surface (Drive, Gmail, Calendar, Sheets, Docs, Chat, Admin) by reading Google's own Discovery Service at runtime instead of shipping a static command list, so new API methods appear automatically. Its distinguishing trait for this wiki is the bundled 100+ Agent Skills library (one `SKILL.md` per API plus curated workflow recipes), structured-JSON-only output, and a Model Armor integration that can scan API responses for prompt injection before an agent sees them — putting it alongside [[davila7-claude-code-templates]] and [[voltagent-awesome-agent-skills]] as a large third-party Agent Skills distribution point, but scoped specifically to Google Workspace.

[[mvanhorn-last30days-skill]] is `/last30days`, an Agent Skill (52,000+ stars, MIT, GitHub Trending #1) that searches Reddit, X, YouTube, TikTok, Hacker News, Polymarket, arXiv, and a dozen other platforms in parallel for a topic, then has an agent synthesize the results into one cited brief scored by real engagement rather than editorial ranking. Its most distinctive engineering detail is the Reddit enrichment pipeline: an LLM search finds candidate threads, then a free, keyless call to Reddit's own JSON API pulls ground-truth upvote counts, comment counts, and top comments to replace the model's guesses with real numbers. Complements research-oriented tools like [[firecrawl.dev]] by focusing specifically on engagement-scored social/community signal rather than general web crawling.

[[karpathy-autoresearch]] is Andrej Karpathy's minimal harness for unattended overnight ML research: a coding agent edits exactly one file (`train.py`, a single-GPU simplified nanochat implementation), trains for a fixed 5-minute wall-clock budget, checks whether the validation metric improved, keeps or discards the change, and repeats roughly 12 times an hour. It's a narrow, single-metric sibling of the general-purpose autonomous coding loops already in this wiki — [[snarktank-ralph]], [[frankbria-ralph-claude-code]], and [[coleam00-agent-control-plane]] — but the loop target here is a measurable ML training result rather than arbitrary feature work, and the actual human-tunable surface is the agent's `program.md` instructions rather than the training code itself.

[[supabase-cli]] is the terminal companion to [[supabase.com]]: it runs the full local Supabase stack (Postgres, Auth, Realtime, Storage, Edge Functions), manages Postgres migrations and schema diffs, deploys Edge Functions, and generates TypeScript types from a live schema. The repo is mid-migration from a Go CLI (`apps/cli-go`, legacy shell wrapper) to a TypeScript/Bun CLI (`apps/cli`, the current published `supabase` command) inside an Nx-orchestrated pnpm monorepo — a useful concrete example of a widely-used dev CLI's rewrite-in-place strategy.

[[HKUDS-LightRAG]] is a lightweight, graph-based Retrieval-Augmented Generation framework (37,600+ stars, MIT, EMNLP 2025) positioned as an efficient alternative to Microsoft GraphRAG. Its dual-layer architecture indexes both a knowledge graph and vector embeddings, and its standout claim is incremental updates without full rebuilds — new documents merge into the existing graph via set merging, and deletions reuse cached LLM extraction results to rapidly recompute only the affected entity/relation descriptions. It's the base framework behind [[HKUDS-RAG-Anything]] (the same lab's multimodal extension, since merged back into this repo) and sits alongside [[zilliztech-claude-context]] in this wiki's knowledge-retrieval-for-agents space, differentiating on graph-structured retrieval versus pure vector search.

[[Houseofmvps-codesight]] (1,230+ stars, MIT) is a zero-dependency `npx` CLI that compiles a codebase into a structured context map (`CODESIGHT.md`, plus routes/schema/components/graph files) for AI coding assistants — using the TypeScript compiler API for AST precision and regex fallback across 30+ frameworks and 14 languages. Its `--wiki` mode is a deterministic, zero-LLM-call counterpart to the agent-facing wiki generators already in this wiki ([[langchain-ai-openwiki]], [[he-yufeng-RepoWiki]], [[PorunC-CodeWiki]]): rather than an LLM synthesizing documentation, codesight's own AST/regex extraction populates an `index.md` plus per-topic articles, following the same "compile once, read cheaply per session" pattern popularized by Karpathy. A `--blast <file>` command and matching MCP tool compute change impact via BFS through the import graph, and `--mode knowledge` extends the same compile-once idea to non-code artifacts (ADRs, meeting notes, retrospectives), pairing with [[nadimtuhin-claude-token-optimizer]]'s CLAUDE.md-budget focus from a structural-extraction angle instead of a prompt-hygiene one.

[[Shubhamsaboo-awesome-llm-apps]] (123,196 stars, Apache-2.0) is a large curated collection of 100+ hand-built, runnable AI agent, RAG, and LLM example applications — not a links-only awesome-list but a gallery of working, cloneable projects spanning starter single-file agents, production-style multi-agent teams, RAG pipelines, voice agents, MCP-connected agents, generative UI agents, and installable Agent Skills, across Claude, GPT, Gemini, DeepSeek, Llama, and Qwen. Its `agent_skills/` section is a smaller, hand-picked counterpart to dedicated skills registries like [[voltagent-awesome-agent-skills]], while its 20+ `rag_tutorials/` entries demonstrate patterns (hybrid search, knowledge-graph RAG, corrective RAG) comparable to standalone RAG frameworks like [[HKUDS-LightRAG]] at tutorial scale. Two dedicated framework crash courses (Google ADK, OpenAI Agents SDK) make it a useful side-by-side entry point alongside the framework pages already in this wiki ([[crewai.com]], [[adk.dev]], [[pydantic-pydantic-ai]], [[langchain.com-langgraph]]) for developers evaluating an approach before building from scratch.

[[karpathy-llm-council]] is Andrej Karpathy's local multi-model adjudication app (22,918 stars): one prompt fans out to multiple LLMs via OpenRouter, each model then ranks anonymized peer responses, and a designated chairman model synthesizes the final answer. Its value in this wiki is as a compact, concrete implementation of a three-stage "model council" loop (generation → cross-review → synthesis) that complements infrastructure pages like [[openrouter.ai]] with an end-user orchestration pattern.

[[nvidia-skillspector]] is NVIDIA's security scanner for agent skills (13,453 stars), designed to answer "is this skill safe to install?" before runtime. It combines static checks, optional LLM semantic analysis, and live OSV.dev dependency vulnerability lookups into a single risk score and recommendation (`SAFE`, `CAUTION`, `DO_NOT_INSTALL`), with JSON/SARIF outputs and an MCP server mode (`scan_skill`) for turning skill install decisions into enforceable guardrails. It sits directly between discovery catalogs like [[skills.sh]] and operational distribution pipelines like [[davila7-claude-code-templates]], adding a security-gating layer to the skills ecosystem.

[[coleam00-eve-analyst]] is a compact Eve-based data analyst agent reference implementation (TypeScript, 9 stars) that demonstrates the full “agent app” stack in one repo: read-only SQL tools with hard guards, approval-gated expensive scans, a sandboxed Python analysis tool, an on-demand `revenue-rules` skill, and an `investigator` subagent for “why did this metric change?” workflows, plus HTTP and Slack channels and evals as a deployment gate. It fits directly alongside [[coleam00-harness-engineering-demo]], [[coleam00-helpline]], and [[coleam00-archon]] as a practical harness-engineering teaching artifact, but focused specifically on analytics-agent behavior and business-rule correctness.

[[eve.dev]] captures Vercel’s `eve` framework as a filesystem-first, durability-oriented agent platform: author an agent as ordinary files (`instructions`, `tools`, `skills`, `channels`, `connections`, `subagents`, `schedules`), then run it through a built-in durable session/runtime layer with streamable HTTP flows, pause/resume, and multi-channel delivery. Its companion GitHub repo (`vercel/eve`) grounds that product story in an active TypeScript monorepo and release train (`eve@0.26.1`), with deep docs for MCP/OpenAPI connections, approval-gated tool execution, channel adapters, and eval-driven regression gates. This page is a direct framework-level complement to app-level examples like [[coleam00-eve-analyst]] and neighboring SDK/framework references such as [[adk.dev]] and [[pydantic-pydantic-ai]].

[[rtk-ai-rtk]] is a single-binary Rust CLI proxy that filters and compresses everyday shell-command output (git, cargo, test runners, docker, aws, and 100+ others) before it reaches an AI coding agent's context, via a PreToolUse hook that transparently rewrites Bash calls across 15+ agent tools (Claude Code, Copilot, Cursor, Gemini CLI, Codex, and more). It targets a narrower, more mechanical slice of the context-optimization problem than its wiki neighbors — [[nadimtuhin-claude-token-optimizer]] restructures project documentation, [[mksglu-context-mode]] sandboxes MCP tool output, and [[chopratejas-headroom]] compresses arbitrary tool/log/RAG content — by applying twelve named filtering strategies (stats extraction, error-only, grouping, deduplication, and others) strictly to raw Bash-tool command output, with a local SQLite database tracking measured token savings per command.

[[teamchong-pxpipe]] is a TypeScript proxy that compresses AI-agent request context by rendering high-volume text blocks as images before forwarding to model APIs, with an explicit emphasis on measurement discipline and known failure modes rather than blanket "free token savings" claims. It complements neighboring context-efficiency approaches in this wiki — [[nadimtuhin-claude-token-optimizer]] (doc-structure/load optimization), [[mksglu-context-mode]] (MCP output sandboxing), [[chopratejas-headroom]] (general tool/log compression), and [[rtk-ai-rtk]] (shell-output filtering) — by targeting request payload shape directly through model-specific render profiles, cache-aware rewrite logic, and per-request baseline accounting.

[[topoteretes-cognee]] is an open-source AI memory platform that ingests data in any format and continuously builds a self-hosted knowledge graph, giving agents persistent long-term memory across sessions through a four-operation SDK (`remember`, `recall`, `forget`, `improve`) plus a Claude Code plugin that hooks the full session lifecycle. Its distinguishing architectural claim is unifying the entire memory layer — graph, vector embeddings, sessions, and metadata — on a single Postgres instance instead of a multi-service stack (Neo4j + vector DB + Redis), while still supporting dedicated backends when needed. It sits alongside [[garrytan-gbrain]] and [[HKUDS-LightRAG]] as a knowledge-graph-plus-vector memory layer, and alongside [[supermemory.ai]] as a persistent cross-session memory system for agents.

[[mozilla-ai-any-llm]] is Mozilla.ai's unified Python interface for calling any LLM provider through a single `completion()`/`AnyLLM` API, covering 50+ providers under `src/any_llm/providers/`. It occupies the same provider-abstraction niche as [[litellm.ai]] and [[openrouter.ai]] but takes a distinct architectural stance: rather than reimplementing each provider's request/response translation (LiteLLM) or routing through a hosted SaaS gateway (OpenRouter), it wraps each provider's **official SDK** directly, trading a larger dependency surface for closer fidelity to provider behavior and an explicit migration path for existing LiteLLM users. It underpins Mozilla.ai's own agent framework, any-agent, and pairs with the separate Otari gateway project for budget/key management.

[[openai-symphony]] is OpenAI's open-source orchestration pattern for autonomous coding-agent execution at the issue-tracker level: a long-running service polls work, maps each issue to an isolated workspace, runs Codex sessions in app-server mode, and enforces policy through a repo-owned `WORKFLOW.md` contract instead of ad hoc operator supervision. The project combines a language-agnostic spec (`SPEC.md`) with a prototype Elixir implementation supporting multiple tracker adapters (Linear, GitHub Issues, Jira, Asana, GitLab), positioning it as a concrete "manage work, not individual agent sessions" harness that complements app-level harness patterns like [[coleam00-harness-engineering-demo]] and tool-level Codex integrations like [[openai-codex-plugin-cc]].

[[langflow-ai-langflow]] is Langflow's OSS codebase (152k+ stars), a visual flow-graph platform for building and deploying AI agents/workflows that unifies UI-first authoring with production execution surfaces: each project can expose flows as REST/API endpoints and as MCP tools. It sits at the intersection of low-code automation platforms like [[n8n.io]], framework-level orchestration primitives like [[langchain.com-langgraph]], and agent-focused development stacks like [[crewai.com]], with a distinctive emphasis on dual MCP roles (Langflow as both MCP server and MCP client) and Python extensibility through custom components.

[[cursor-plugins]] is the official Cursor plugin marketplace repo: a two-level manifest spec (`marketplace.json` + per-plugin `plugin.json`) bundling 13 plugins and 75 agent skills for CI, code review, PR workflows, learning support, and multi-agent orchestration via the Cursor SDK. Its `pstack` plugin alone contributes 38 skills, including a library of 22 standalone "principle" skills that auto-apply engineering discipline (root-cause fixes, idempotent operations, type-system discipline) rather than being explicitly invoked. As a concrete large-scale plugin marketplace with schema validation in CI, it makes a useful architectural comparison point against [[anthropics-skills]]'s skill-only distribution model.

[[microsoft-semantic-kernel]] is Microsoft's earlier agent-orchestration SDK — a polyglot (.NET/Python/Java) "kernel" that composes native-code and prompt-template plugins into pipelines and supports multi-agent triage composition. Its README now marks it deprecated in favor of [[microsoft-agent-framework]], SK's declared enterprise-ready successor (with an official migration guide), making SK's chief remaining value in this wiki architectural/historical: it is the direct conceptual predecessor to MAF's plugin and agent-composition model, alongside [[microsoft-autogen]] as Microsoft's other merged-in framework.

[[agno.com]] is an open-source Python multi-agent framework (41,000+ stars) that pairs a lightweight agent/team-authoring SDK with **AgentOS**, a self-hostable, framework-agnostic FastAPI runtime — it can serve production agents built with Agno itself, the Claude Agent SDK, [[langchain.com-langgraph]], or DSPy behind a shared API surface with JWT-based RBAC, observability, and a web control plane. Where [[crewai.com]] and [[microsoft-agent-framework]] bundle authoring and production concerns into one framework, Agno's split between "build with the SDK" and "run with AgentOS" makes it a useful comparison point for evaluating agent-runtime portability across frameworks.

[[mastra.ai]] is a Y Combinator-backed, TypeScript-native agent framework (26,600+ stars, dual Apache-2.0/Enterprise license) filling a gap this wiki's largely Python-first framework landscape otherwise leaves open — it offers typed agents, graph-based Workflows, Observational Memory, human-in-the-loop suspend/resume, MCP server authoring, and a built-in local Studio UI for building and debugging agents. Its target audience (React/Next.js/Node teams) and Workflow model make it a natural point of comparison to [[langchain.com-langgraph]]'s state-graph orchestration and to [[strandsagents.com]]'s TypeScript-plus-Python dual-language support.

[[nvidia.com]] documents NVIDIA NeMo, an "agent-first" open suite of libraries, microservices, and foundation models spanning the full agent lifecycle — data curation, fine-tuning, guardrails, deployment via NVIDIA NIM, and reinforcement-learning optimization — rather than a single agent-authoring SDK. It sits alongside [[huggingface.co]] and [[pytorch.org]] as model/infrastructure tooling in this wiki, and is explicitly complementary to (not competing with) agent frameworks like [[langchain.com-langgraph]]: its NeMo Agent Toolkit component runs workflows built with LangGraph and other frameworks rather than replacing them.

[[ai-sdk.dev]] is Vercel's provider-agnostic TypeScript toolkit (`vercel/ai`, 25,800+ stars, 18.7M weekly downloads) for building AI applications and agents — a unified model API, structured-output and tool-calling primitives, the `ToolLoopAgent` agent class, and framework-agnostic UI hooks (`AI SDK UI`). It is the SDK-level sibling to the broader [[vercel.com]] agentic-infrastructure platform already in this wiki, and a direct TypeScript-ecosystem peer to [[mastra.ai]] — where Mastra emphasizes graph-based Workflows, durable memory, and a built-in Studio UI, the AI SDK stays narrowly focused on the model-call/tool-call/UI layer and leans on adjacent Vercel products (Sandbox, Workflows, AI Gateway) for the rest.

[[docker-sandboxes]] documents Docker Sandboxes, a product for running coding agents (Claude Code, Gemini CLI, Copilot CLI, Codex, OpenCode, Kiro) inside disposable, hypervisor-isolated microVMs so they can operate in fully permissive "YOLO mode" without risking the host. It tackles the same "safe agent autonomy" problem as [[vercel.com]]'s Sandbox product, but with a distinctive twist: each Docker sandbox runs its own private Docker daemon, so the agent itself can build and run nested containers — a capability plain container-based sandboxes can't offer without sharing the host's daemon.

[[uditakhourii-adhd]] is an open-source agent skill (2,450+ stars) that reframes premature convergence in LLM reasoning as an architecture problem rather than a prompting one: it fans out N fully isolated Claude Agent SDK sessions under distinct "cognitive frame" system prompts with evaluation forbidden, then runs a separate, mechanically distinct critic pass to score, cluster, prune traps, and deepen the survivors. It installs into Claude Code, Codex, and ~50 other agents via `npx skills add`, sitting alongside [[anthropics-skills]] and other skill-ecosystem entries in this wiki, and shares its multi-branch generate-then-critique shape with [[karpathy-llm-council]] despite targeting open-ended ideation rather than ensemble answer ranking.

[[Nutlope-hallmark]] is Together AI's open-source design skill (18,900+ stars) for Claude Code, Cursor, and Codex, aimed squarely at the "every AI-generated page looks the same" problem: it picks a macrostructure per brief, applies one of twenty themes (or a from-scratch Custom branch), and runs 57 slop-test gates plus a self-critique before handing back output, with the explicit goal of structural — not just visual — variety between pages. It joins [[leonxlnx-taste-skill]], [[voltagent-awesome-design-md]], and [[open-design.ai]] as part of this wiki's growing anti-AI-slop / agent-native design cluster, sharing the portable `design.md` DNA-extraction pattern with the first two while installing as a conventional Agent Skill alongside the broader catalog in [[anthropics-skills]].

[[router-for-me-CLIProxyAPI]] is a Go proxy server (45,200+ stars) that exposes OAuth-authenticated CLI subscriptions — Claude Code, ChatGPT Codex, Antigravity, Grok Build, Kimi — as OpenAI/Gemini/Claude/Codex-compatible API endpoints, with multi-account round-robin load balancing and an embeddable Go SDK. It occupies the same "one endpoint, many models" niche as [[litellm.ai]], [[openrouter.ai]], and [[mozilla-ai-any-llm]] in this wiki, but takes a distinct mechanism — reusing existing CLI-tool OAuth logins rather than routing paid API-key traffic — and has spawned a large downstream ecosystem of desktop/tray/dashboard wrappers for multi-account quota tracking.

[[serper.dev]] is a Google Search API for agents spanning ten search verticals (web, images, news, maps, video, shopping, Scholar, patents, autocomplete) at credit-based pricing starting at $0.30/1,000 queries, with built-in integrations in LangChain, CrewAI, Jan AI, and Haystack. Unlike [[brave-search]], it wraps Google's index rather than running an independent crawler and publishes no MCP server or public GitHub repo, making it a lighter-weight, cheaper-but-less-documented alternative to [[brave-search]] and [[anysearch.com]] for teams that just need fast Google-backed search grounding.

[[serpapi.com]] is the largest and most enterprise-oriented entry in this wiki's search-API cluster: a single REST endpoint fronting 100+ named search-engine endpoints (Google's dozens of verticals plus Bing, Amazon, Yandex, YouTube, and more), full-browser CAPTCHA-solving execution, a U.S. Legal Shield (up to $2M coverage) for lawful scraping, and SOC 2/SOC 3/ISO 27001 certification. It sits alongside [[serper.dev]] and [[brave-search]] as a third take on the same problem — broader engine coverage and heavier compliance guarantees than either, at correspondingly higher price tiers — and, like [[serper.dev]], has no maintained companion GitHub repo of its own (only per-language client-library repos and an unrelated public roadmap board), unlike [[brave-search]]'s dedicated MCP server.

[[supadata.ai]] is a "web media to text" API that turns YouTube, TikTok, Instagram, X, and Facebook videos into transcripts, metadata, and AI-extracted structured data, plus general web scrape/map/crawl endpoints, backed by an official MCP server (`supadata-ai/mcp`) for direct use from Claude, Cursor, and other agent clients. It bridges two clusters already in this wiki: it overlaps [[firecrawl.dev]] on the web scrape/crawl surface while adding video/social-platform transcript extraction that Firecrawl doesn't cover, and it targets the same YouTube-transcript problem as [[meetclaras.com]] but from a programmatic-API angle rather than an end-user knowledge-extraction product.

[[coleam00-cole-medin-knowledge-base]] is a Karpathy-style LLM wiki and OKF (Open Knowledge Format) bundle synthesized entirely from Cole Medin's 198-video long-form YouTube catalog — a directly relevant methodological sibling to [[6eanut-llm-wiki]] and to this wiki's own `pin-llm-wiki` skill. Its documented build pipeline is unusually concrete about the exact problem this wiki's own ingest process solves by hand: a two-pass extract-then-canonicalize architecture with a frozen manifest barrier prevents parallel agents from inventing divergent duplicate pages for the same idea, and six independent validation checks (99% citation integrity, 0 semantic duplicates, adversarial QA with 0 hallucinations) prove the result rather than merely asserting it. It relies on [[supadata.ai]] as its underlying transcript-sync mechanism via Cole's own production database.

[[x.com-0xcodez-how-to-become-a-forward-deployed-engineer-in-10]] is a career-roadmap source rather than a tool or framework: a 10-step guide to the Forward Deployed Engineer / Applied AI Engineer role, framed around an MIT NANDA finding that 95% of 300 enterprise AI projects showed little measurable impact because deployments — not models — failed. Its most direct connection to the rest of this wiki is structural: it names MCP servers, agent skills, and subagents (the exact primitives documented in [[anthropics-skills]]) as the three concrete artifacts the job produces, and treats Claude Code fluency as both a productivity multiplier and a literal interview-evaluation criterion. It sits alongside [[santifer-career-ops]] as one of the wiki's few career-facing sources, though the two are complementary — job-search automation tooling versus a role-and-skills roadmap.

[[impeccable.style]] is the documentation and marketing site for [[pbakaus-impeccable]], covering the product from the user-facing side: the Design Context workflow (`PRODUCT.md`/`DESIGN.md`), the Live Mode browser-iteration feature, and the 64-rule Slop anti-pattern catalog that the CLI, browser extension, and `critique` command all enforce from one rule set. Together the two pages give the wiki both angles on Impeccable — the repo's build internals and provider-transformer architecture versus the site's task-oriented docs and tutorials — the same split already used for [[designmd.cc]] and its GitHub companion.

[[block-buzz]] is Block's self-hosted, Nostr-backed team workspace where AI agents are members of the same rooms as humans — every message, reaction, workflow step, review, and git event is a signed event in one log, whether the author is a person or a process. It's a distinct architectural bet from every other collaboration-adjacent tool in this wiki: instead of a chat product with an agent bolted on, agents get their own keypairs, channel memberships, and audit trail, driven through an agent-first `buzz-cli` (JSON in/out) and an ACP harness (`buzz-acp`) that bridges relay `@mentions` to [[aaif-goose-goose]], Codex, and Claude Code. Its own `buzz-agent`/`buzz-dev-mcp` pair is a small, deliberately uncoupled ACP-agent-plus-MCP-server reference implementation — two crates a senior engineer can read in a sitting, with zero unsafe code and bounded process/output/history lifetimes throughout.

[[markitdown.tech]] is an online document-to-Markdown converter (PDF, DOCX, XLSX, PPTX, HTML, CSV, JSON, images, audio) built on Microsoft's open-source MarkItDown library, offering a free web UI, a Pro batch tier, an MCP server integration for Claude Desktop and other agent hosts, and a dedicated Claude workflow guide — filling the local-file-to-Markdown preprocessing gap that [[firecrawl.dev]] (web scrape) and [[supadata.ai]] (video/social transcript) leave open.

[[aaravkashyap12-advise-project-approach]] is an evidence-driven agent skill that enforces a grounded methodology for project decisions before teams commit to stacks, architectures, vendors, or refactors — rather than handing back generic "use Django" advice, it researches comparable real-world projects, anchors recommendations to actual constraints and maintenance signals, breaks down vendor pricing into operating costs, and flags when a recommendation becomes obsolete. It operates in three modes: pre-build (green-field planning from rough ideas), mid-build (course correction on a live repo), and post-build (launch-readiness review). The skill is portable across Claude Code, Codex, and pi via its `SKILL.md` runtime spec, sits alongside [[obra-superpowers]] and [[anthropics-skills]] in this wiki's agent-skill cluster, and shares its evidence-before-vibes philosophy with the wiki's broader commitment to cited, comparable-project-grounded analysis — see [[coleam00-cole-medin-knowledge-base]], [[6eanut-llm-wiki]], and this wiki's own `pin-llm-wiki` system for methodologically similar examples.

[[uditakhourii-neuroarxiv]] is an agent skill that kills from-scratch architecture decisions by forcing Claude to check real arXiv prior art first — not as a search wrapper, but as a four-phase isolate-then-converge discipline: it fetches papers via real HTTP against `export.arxiv.org`, reads each in a parallel Agent call that sees only that one abstract (isolation prevents any paper from anchoring the reading of another), scores and clusters the results, then commits to ONE recommended path with citations, a concrete first step, and failure modes from every paper's limitations. Its defining output metric is source-skepticism: in a 5-problem cross-domain evaluation, it produced 7 flags on specific papers' over-stated or withdrawn claims, versus zero for both cold and general web+arXiv undisciplined conditions. It sits alongside [[uditakhourii-adhd]] (same author, same parallel-isolation philosophy applied to ideation) and [[aaravkashyap12-advise-project-approach]] (evidence-grounded project decisions from real GitHub comparables) as this wiki's cluster of skills that enforce evidence discipline before implementation.

[[anthropics-claude-agent-sdk-python]] is Anthropic's official Python SDK for embedding Claude Code programmatically — exposing a one-shot `query()` async iterator and a full `ClaudeSDKClient` for bidirectional, interactive sessions with custom in-process MCP servers and lifecycle hooks. Its defining contribution is the in-process SDK MCP server pattern: `@tool`-decorated Python functions register as named MCP servers running in the same process, eliminating subprocess overhead and making custom tools as simple as a Python function with a decorator. The hooks system (`PreToolUse`, etc.) gives application code deterministic veto power over every tool call before Claude acts, enabling policy enforcement, audit logging, and automated feedback loops without leaving the Python runtime. With 7,905 stars and active Anthropic maintenance (v0.2.139 as of August 2026), it bridges the gap between the [[anthropic.com-managed-agents]] managed harness and the raw [[anthropic.com-messages]] Messages API — offering library-level control for teams who want Claude Code's full toolset without managing a separate process.
